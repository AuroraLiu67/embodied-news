# 具身智能公司动态雷达 MVP 实施计划

> 版本：v0.3  
> 更新日期：2026-07-30  
> 依据：`architecture.md`、`PRD.md`、`SPEC.md`、`TECH-STACK.md`  
> 规则：每次只执行一个小任务；每个任务都必须完成对应测试；本文不包含实现代码。

## 1. AI 开发者通用指令

1. 开始任务前阅读 `architecture.md`、`PRD.md`、`SPEC.md`、`TECH-STACK.md` 和本实施计划；架构冲突以 `architecture.md` 为准。
2. Codex 是主开发与集成者。
3. Claude Code只做只读审核，不直接实现。
4. 不改变冻结的数据表、字段、公开边界或 Provider 职责；需要改变时先停止并请求确认。
5. 不覆盖其他人的未提交修改。
6. 外部服务通过 Adapter 或 Repository 调用。
7. 测试不得访问真实 OpenAI、WorkBuddy 或生产飞书。
8. 新行为必须添加自动化测试。
9. 不得通过跳过测试、降低断言或删除测试完成任务。
10. 密钥、完整原文和内部判断不得写入 Git、日志或静态网站。
11. 每次交接报告结果、文件、验证、限制和下一步。
12. 每个任务开始前先声明本次允许修改的文件；未声明的文件只读。
13. 任务只有在交付物和验证测试同时完成后才算完成，不能把“已生成代码”视为完成。
14. 需要真实账号、权限或生产密钥的步骤，由 AI 准备检查清单并等待公司管理员执行或授权，不得使用个人账号代替。

### 1.1 AI 开发任务分配

| 阶段 | 默认负责人 | 可独立分配的工作 |
|---|---|---|
| A 工程与契约 | Codex | Fixture、Mock 和独立测试 |
| B 飞书数据层 | Codex | 字段清单核对和 Mock 测试 |
| C 候选发现与处理 | Codex | WorkBuddy 导入与 OpenAI Mock 测试 |
| D 飞书审核与日报 | Codex | 飞书视图验收可由产品负责人协助 |
| E 公开网站 | Codex | 冻结公开 DTO 后的独立页面和视觉测试 |
| F GitHub 自动化 | Codex | CI 测试；生产 Workflow 由 Codex 统一集成 |
| G 稳定性与上线 | Codex | Claude Code只执行 G07 只读审核 |

### 1.2 单个任务交接格式

每个 AI 开发者交接时必须提供：

- 任务编号和完成结论。
- 实际修改的文件。
- 是否改变冻结契约。
- 新增或更新的测试。
- 验证结果。
- 已知限制和未解决问题。
- 建议执行的下一个任务。

### 1.3 冻结架构基线

- WorkBuddy负责发现国内公众号、公司、重点赛道、投资机构和 FA 的融资及公司动态候选。
- OpenAI负责海外搜索、相关性判断、结构化抽取、冲突核验和中文摘要。
- 项目飞书 CLI 是自动化和人工维护统一使用的数据操作入口。
- 飞书多维表格是唯一正式数据源，并承担审核和编辑后台。
- Next.js 根据飞书公开投影生成静态 HTML。
- GitHub Actions负责定时任务、补跑、构建和发布。
- GitHub Pages只负责托管公开静态网站。
- Codex维护实现与项目发布 Skill；GitHub Actions不依赖交互式 Codex 会话。

## 2. 阶段 A：工程与契约

### A01 初始化项目

**指令：**

- 初始化 Next.js App Router、TypeScript strict mode、pnpm 和测试框架。
- 建立 `app`、`components`、`lib`、`cli`、`scripts` 和 `tests` 目录。
- 只实现最小首页。

**验证测试：**

- 类型检查、规范检查、单元测试和静态构建通过。
- 首页可以在本地静态预览中打开。
- 仓库不包含密钥和环境文件。

### A02 建立领域类型

**依赖：** A01。

**指令：**

- 实现候选、融资事件、公司、日报、观察清单、内部判断和自动化任务类型。
- 分开公开 DTO 和内部 DTO。

**验证测试：**

- 合法领域 Fixture 通过类型与运行时校验。
- 公开 DTO 无法包含内部判断和待审核字段。

### A03 建立 Zod Schema

**依赖：** A02。

**指令：**

- 为 WorkBuddy 输入、OpenAI 输出、飞书记录、公开导出和错误响应建立 Schema。
- 限制 URL、文本、数组、金额和日期。

**验证测试：**

- 每个 Schema 至少有合法、缺字段、非法类型和超长输入测试。
- 危险 URL 和未知字段被拒绝。

### A04 建立环境配置

**依赖：** A01。

**指令：**

- 区分本地、测试和生产配置。
- 建立飞书、OpenAI、通知、时区和 GitHub Pages base path 配置。
- 服务端密钥与公开配置分离。

**验证测试：**

- 缺少必需配置时产生明确错误。
- 客户端构建和静态产物不包含服务端密钥。

### A05 建立 Fixture 和 Mock

**依赖：** A02、A03。

**指令：**

- 创建国内、海外、未披露金额、多来源、冲突、重复和低置信度 Fixture。
- 实现飞书、OpenAI、WorkBuddy和通知 Mock。

**验证测试：**

- Fixture 全部通过 Schema。
- 断网运行测试时不存在外部请求。
- Mock 支持成功、空结果、限流、超时和非法输出。

### A06 扩展公司动态与日报领域契约

**依赖：** A02、A05。

**指令：**

- 增加技术、产品和商业化公司动态领域对象。
- 将原始信息来源建模为可被融资事件和公司动态关联的独立对象。
- 为融资事件和公司动态增加 1–5 重要性评分与评分理由。
- 将日报改为今日融资、技术与产品、商业化进展三个固定板块。
- 将观察项类型扩展为重点公司和重点赛道。

**验证测试：**

- 三类公司动态分别通过 Schema，非法类别失败。
- 无原始来源的可发布融资事件或公司动态验证失败。
- 重要性超出 1–5 范围验证失败。
- 日报三个板块按重要性降序并允许显式人工顺序覆盖。
- 公开 DTO 中每条内容包含自己的来源链接，不存在独立来源板块。

## 3. 阶段 B：飞书数据层

### B01 创建飞书多维表格设计

**依赖：** A02。

**指令：**

- 定义研究候选、融资事件、公司动态、信息来源、公司、日报、观察清单、内部战投备注和自动化任务表。
- 观察清单必须区分重点公司和重点赛道。
- 日报固定三个板块，并要求每条内容关联原始来源和 1–5 重要性评分。
- 明确字段类型、关联、选项和公开性。
- 形成字段 ID 映射清单。

**验证测试：**

- 逐表检查 PRD 必填字段。
- 确认内部战投备注与公开字段分离。
- 用公司、融资、公司动态、来源和日报样例验证关联、三个板块和视图。

### B02 创建公司飞书自建应用

**依赖：** B01。

**指令：**

- 创建归公司租户的自建应用。
- 只申请多维表格读写和文本通知所需权限。
- 设置至少两名公司管理员。

**验证测试：**

- 应用可读取指定表、写入测试记录并删除该测试记录。
- 应用无法访问未授权多维表格。
- 管理员交接测试通过。

**当前验收状态（2026-07-31）：**

- 指定正式多维表格的读取、测试记录写入、回读、删除及删除后缺失检查已通过。
- 未授权多维表格隔离测试已执行但未通过：同一租户、同一所有者和同一上级空间中的测试表格未添加文档应用，应用身份仍可读取记录。用户已明确接受将该项延期到上线前安全验收，不得将其记录为已通过。
- 当前应用仅由用户本人管理；“至少两名公司管理员”和管理员交接测试按用户当前决定延期到生产交接前完成。
- B03 可以在固定正式 Base Token、禁止任意 Base Token 输入且测试继续使用 Mock 飞书的约束下开始；生产发布前必须补齐上述两项延期测试。

### B03 实现飞书客户端

**依赖：** A03、A04、A06、B02。

**指令：**

- 封装凭据获取、分页、批量读写、限流重试和错误映射。
- 不加入业务规则。

**验证测试：**

- Mock 验证分页、批量写入、限流、超时和权限错误。
- 日志不包含应用密钥或访问令牌。

### B04 实现字段映射验证

**依赖：** B03。

**指令：**

- 按字段 ID 读取和写入。
- 启动时验证表、字段和字段类型。
- 字段被删除或改类型时快速失败。

**验证测试：**

- 正确映射通过。
- 缺字段、错表和错字段类型分别产生稳定错误。

### B05 实现飞书 Repository

**依赖：** B04。

**指令：**

- 为九张表实现查询、幂等创建、更新和分页。
- 使用稳定业务 ID。
- 增加乐观并发和审计字段。

**验证测试：**

- 验证创建、读取、更新、分页和不存在记录。
- 重复创建不会产生第二条记录。
- 旧版本更新被拒绝。

### B06 实现项目飞书 CLI 骨架

**依赖：** B05。

**指令：**

- 建立统一命令入口、机器可读输出、错误码和帮助信息。
- 先提供连接检查和 Schema 检查。

**验证测试：**

- 合法配置返回成功摘要。
- 无权限、缺字段和无网络返回不同错误码。
- 标准输出不包含密钥。

## 4. 阶段 C：候选发现与处理

### C01 实现 WorkBuddy 候选格式

**依赖：** A03。

**指令：**

- 定义标题、URL、来源、发布时间、查询词、摘要和发现时间。
- 编写面向 WorkBuddy 的导出说明。

**验证测试：**

- 合法国内公众号样本通过。
- 缺 URL、危险 URL、超长摘要和非法日期被拒绝。

### C02 实现 WorkBuddy 导入

**依赖：** B06、C01。

**指令：**

- CLI 接收候选文件。
- 校验、canonicalize、生成候选 ID、去重并写入候选表。
- 不允许导入正式事件和发布状态。

**验证测试：**

- 合法文件创建候选。
- 重复文件不会重复写入。
- 越权字段被拒绝或忽略并记录。

### C03 实现 OpenAI Provider

**依赖：** A03、A04、A05。

**指令：**

- 实现海外搜索、相关性、结构化抽取、冲突比较和中文摘要。
- 模型和预算来自配置。
- 所有输出经过 Schema。

**验证测试：**

- Mock 验证成功、限流、超时、非法 JSON 和服务错误。
- 重试不超过上限。
- 日志不包含 API Key 和完整响应。

### C04 实现安全内容获取

**依赖：** A03。

**指令：**

- 只抓取允许的公网 HTTP/HTTPS 地址。
- 阻止本机、私网、元数据地址、危险重定向和超大响应。
- 提取并清洗必要正文。

**验证测试：**

- 私网、元数据、循环重定向、超时和超大内容被拒绝。
- 正常中英文页面能提取安全正文。

### C05 实现相关性判断

**依赖：** C03、C04。

**指令：**

- 按 PRD 行业范围判断相关性。
- 保存结果、理由、模型和置信度。
- 低置信度保留待查。

**验证测试：**

- 明确相关、明确无关和边界 Fixture 分类正确。
- 低置信度不会被自动丢弃或发布。

### C06 实现融资字段抽取

**依赖：** C03、C05。

**指令：**

- 抽取公司、轮次、金额、币种、投资方、日期、地区和技术方向。
- 未披露信息保持未知。
- 重要事实关联来源。

**验证测试：**

- 覆盖已披露、未披露、多币种、多投资方和模糊日期。
- 缺失字段不会被虚构。

### C07 实现公司归一化

**依赖：** B05、C06。

**指令：**

- 使用中英文名、别名和官网域名识别公司。
- 模糊匹配只提出建议。

**验证测试：**

- 已知别名正确合并。
- 相似但不同公司不会自动合并。

### C08 实现事件去重与冲突

**依赖：** C06、C07。

**指令：**

- 按 canonical URL、公司、日期、轮次和金额识别重复。
- 保留全部来源。
- 冲突事实进入待复核。

**验证测试：**

- 同事件转载被合并。
- 金额或轮次冲突不被静默覆盖。
- 重跑不会创建重复候选。

### C09 实现置信度与摘要

**依赖：** C08。

**指令：**

- 按来源等级、数量、一致性和原文可访问性计算置信度。
- 生成只基于证据的公开摘要。

**验证测试：**

- 高、中、低样本得到预期等级。
- 增加权威来源不会降低评分。
- 摘要事实可追溯到来源。

## 5. 阶段 D：飞书审核与日报

### D01 实现候选审核视图

**依赖：** B01、C09。

**指令：**

- 在飞书建立今日待审核、高置信度、低置信度、重复和待查视图。
- 展示来源和冲突。

**验证测试：**

- 不同状态进入正确视图。
- 内部字段不出现在候选公开列中。

### D02 实现审核转正式事件

**依赖：** B05、D01。

**指令：**

- 将审核通过候选幂等转换为融资事件。
- 拒绝、待查和重复候选不进入正式事件。

**验证测试：**

- 五种审核状态行为正确。
- 重复执行不会创建第二个事件。

### D03 实现日报生成

**依赖：** D02。

**指令：**

- 按 Asia/Shanghai 日期选择事件。
- 生成唯一日报草稿和初始排序。
- 支持空日报。

**验证测试：**

- 固定时钟验证日期边界。
- 同一天重复生成保持一条日报。
- 排序与 Fixture 预期一致。

### D04 实现飞书文本通知

**依赖：** A05、D03。

**指令：**

- 实现审核提醒、发布通知、失败和恢复通知。
- 只发送文本与普通链接。
- 仅私聊配置的个人 `open_id`，不发送到群聊。

**验证测试：**

- 四种通知内容正确。
- 输出不存在消息卡片结构。
- 群聊 `chat_id` 不能通过通知配置或发送接口。
- 通知失败不改变正式数据。

### D05 执行首版真实日报受控试跑

**依赖：** C02、C03、C09、D01、D02、D03、D04。

本任务是首次真实日报验收，不是长期自动调度的替代品。试跑按小步骤依次执行；每一步必须先通过离线自动化测试和用户验收，才能进入下一步。真实外部调用只能在用户明确授权后执行。

#### D05.1 发现指定日期候选并写入研究候选

**指令：**

- 为 OpenAI 海外 Web Search 增加按 Asia/Shanghai 业务日期运行的 CLI 入口。
- 搜索结果必须限定为指定日期发布，发布日期不明确的结果不得冒充当日候选。
- 将通过严格 Schema 的海外结果幂等写入飞书“研究候选”，固定为 `OVERSEAS + OPENAI + PENDING`。
- WorkBuddy 国内结果继续使用 C01 固定 JSON 和 C02 `workbuddy-import` 导入，不为试跑建立旁路格式。
- 两类 Agent 都只能创建研究候选，不得创建正式事件、公司动态、日报或发布状态。
- 首次真实试跑业务日期固定为 `2026-08-02`；自动化实现不得写死该日期。

**验证测试：**

- Mock OpenAI 验证指定日期查询、合法结果创建候选、空结果、发布日期不匹配、非法输出和供应商失败。
- 相同来源和相同业务日期重跑不会创建第二条候选，也不会覆盖飞书中已有候选。
- CLI 写入前执行飞书 Schema 校验，输出创建数、重复数和拒绝数，不输出密钥、完整模型响应或完整网页正文。
- 测试不得访问真实 OpenAI、WorkBuddy 或生产飞书。
- 用户验证自动化测试通过后，才允许执行一次 `2026-08-02` 的真实发现和候选写入。

#### D05.2 在飞书审核首批真实候选

**指令：**

- 产品负责人在飞书候选视图核对标题、内容类型、原始来源、发布日期、置信度和冲突。
- 对每条候选设置 `APPROVED`、`REJECTED`、`NEEDS_RESEARCH` 或 `DUPLICATE`。
- 批准融资候选时补齐正式转换所需公司、来源、摘要、重要性和事实字段；批准公司动态时补齐类别及对应公开字段。
- 本步骤不自动修改审核决定，不生成公开内容。

**验证测试：**

- 用户确认所有试跑候选均已完成审核或明确保留待复核。
- 非批准候选不会进入后续正式转换。
- 每条批准候选至少存在一个可访问原始来源链接。

#### D05.3 将批准候选转换为正式数据

**指令：**

- 从飞书读取已批准候选，按内容类型幂等写入公司、信息来源、融资事件或公司动态。
- 补齐技术、产品和商业化动态的人工授权转换，不把 D02 的融资事件转换错误复用于其他类型。
- 正式内容固定为不公开草稿；批准候选不等于允许公开。
- 同一候选重跑不得创建第二条公司、来源、融资事件或公司动态。

**验证测试：**

- 融资、技术、产品和商业化四类批准候选分别进入正确正式表。
- 拒绝、待复核和重复候选不产生正式写入。
- 公司与来源关联正确，原始来源缺失时转换失败且不产生部分正式数据。
- 自动化测试使用 Mock Repository，不访问生产飞书；用户验证后才执行受控真实转换。

#### D05.4 生成并私聊首版真实日报

**指令：**

- 从飞书正式融资事件和公司动态读取 `2026-08-02` 内容，生成唯一三板块日报草稿。
- 板块内按重要性降序，同分使用稳定业务 ID；空板块保持为空，不使用其他日期内容填充。
- 日报只保存在飞书并私聊发送给已配置的个人 `open_id`，不发群聊、不发布网站。
- 通知只在日报成功保存后发送；发送失败不得改变日报正式数据。

**验证测试：**

- 真实日报只引用用户批准且属于指定业务日期的正式内容。
- 每个条目关联原始来源，三个板块分类和顺序正确。
- 相同日期重跑保持一条日报，不产生重复私聊成功记录。
- 用户在飞书确认日报记录和个人私聊内容正确后，D05 才算完成。

## 6. 阶段 E：公开网站

### E01 实现公开字段投影

**依赖：** B05、D03。

**指令：**

- 将飞书正式记录转换为公开 DTO。
- 仅导出已发布且允许公开的内容。
- 使用敏感字段拒绝列表。

**验证测试：**

- 内部备注、待审核候选、操作人和飞书记录 ID 无法导出。
- 非法公开记录导致构建失败。

### E02 实现网站数据导出

**依赖：** E01。

**指令：**

- 生成最新日报、归档、事件、公司和看板公开 JSON。
- 输出稳定排序和稳定 ID。

**验证测试：**

- 同一飞书快照生成完全一致的输出。
- 空数据生成合法网站数据。

### E03 实现网站设计系统

**依赖：** A01。

**指令：**

- 建立 Tailwind、共享布局、颜色、排版、卡片、状态标签和空状态。
- 支持中英文和移动端。

**验证测试：**

- 组件渲染和键盘焦点测试通过。
- 窄屏和桌面宽度无关键内容溢出。

### E04 实现首页

**依赖：** E02、E03。

**指令：**

- 展示最新日报、重点事件、最新事件、数据概览和归档入口。

**验证测试：**

- 正常和空数据状态正确。
- 所有详情链接有效。

### E05 实现日报与归档

**依赖：** E02、E03。

**指令：**

- 实现日报详情、月份归档、永久日期链接和前后期导航。
- 展示自动发布和更正说明。

**验证测试：**

- 跨月、空月份、非法日期和前后期边界正确。
- 未发布日报不会生成页面。

### E06 实现融资详情

**依赖：** E02、E03。

**指令：**

- 展示融资事实、摘要、来源、置信度和冲突提示。
- 不展示完整原文。

**验证测试：**

- 已披露、未披露、多来源和冲突状态正确。
- 页面无内部字段。

### E07 实现公司公开档案

**依赖：** E02、E03。

**优先级：** MVP 后续增强，不阻塞最小闭环验收。

**指令：**

- 展示公司中英文名称、别名、官网、地区、技术方向、公开简介和已发布融资历史。
- 公司融资历史只引用允许公开的融资事件。

**验证测试：**

- 中英文别名、多轮融资和无融资公司状态正确。
- 同一融资事件不会重复展示。
- 页面不包含内部关注等级、跟进状态或战投备注。

### E08 实现简单数据看板

**依赖：** E02、E03。

**优先级：** MVP 只需基础统计；丰富图表与筛选属于后续增强。

**指令：**

- 使用 ECharts 展示数量、披露金额、轮次、技术方向和地区。

**验证测试：**

- 聚合值与固定 Fixture 一致。
- 未披露金额不计入金额总额。
- 无数据状态正确。

### E09 配置静态导出与 base path

**依赖：** E04 至 E08。

**指令：**

- 配置 Next.js 静态导出。
- 支持 GitHub Pages 项目路径和静态资源。
- 增加 404。

**验证测试：**

- 在非根 base path 下打开全部页面。
- CSS、JavaScript、图表和导航无 404。

### E10 创建 Codex 发布 Skill

**依赖：** B06、E09。

**指令：**

- 创建仓库级 Skill，描述字段检查、导出、构建、测试和交接流程。
- Skill 不自动提交或推送，除非用户明确要求。

**验证测试：**

- 在干净 Fixture 上执行 Skill 能完成构建与测试。
- 缺字段或敏感字段时 Skill 停止发布。

## 7. 阶段 F：GitHub 自动化

### F01 配置 CI

**依赖：** A05、E09。

**指令：**

- Pull Request 执行规范、类型、单元、集成、静态构建、链接、安全和 E2E。
- PR 不使用生产 Secrets。

**验证测试：**

- 临时失败断言能阻止 CI，恢复后通过。
- CI 日志无密钥和内部数据。

### F02 实现自动化任务状态机

**依赖：** B05。

**指令：**

- 按业务日期和任务类型建立幂等任务。
- 支持锁、重试、成功、失败和人工处理。

**验证测试：**

- 重复触发只执行一次。
- 并发 Runner 只有一个获得锁。
- 超时任务可安全恢复。

### F03 配置周期调度

**依赖：** F02、C03、D03。

**指令：**

- 周期检查到期且未完成的发现、处理、审核稿、发布和通知任务。
- 支持手动触发。

**验证测试：**

- 固定时钟覆盖 07:00、08:00、09:00 前后。
- 延迟一次后下一周期会补跑。
- 手动触发不重复数据。

### F04 配置 GitHub Pages 发布

**依赖：** E09、F01。

**指令：**

- 默认分支构建网站 artifact 并发布 Pages。
- 设置最小权限和单一发布并发。

**验证测试：**

- 测试环境完成一次 Pages artifact 构建。
- 发布失败时不修改飞书数据。
- 上一个成功版本仍可访问。

### F05 串联每日发布

**依赖：** D04、F03、F04。

**指令：**

- 09:00 任务验证日报、导出公开数据、构建、测试、发布和通知。
- 任一步失败记录状态并停止后续危险操作。

**验证测试：**

- 已审核和未审核自动发布两种流程通过。
- 构建失败不发送成功通知。
- 重试不会生成重复日报。

## 8. 阶段 G：稳定性与上线

### G01 实现成本和配额

**依赖：** C03、F03。

**指令：**

- 限制每日查询、候选、内容长度、重试和预算。
- 记录估算成本。

**验证测试：**

- 每项限制边界前允许、边界后拒绝。
- 达到硬上限后不再发起收费调用。

### G02 实现日志与告警

**依赖：** F05。

**指令：**

- 记录任务、阶段、数量、耗时和简化错误。
- 对连续失败、候选异常和未发布告警。

**验证测试：**

- 成功、失败和恢复日志完整。
- 重复故障不会产生告警风暴。
- 敏感值不会进入日志。

### G03 实现 90 天回填

**依赖：** C09、D02、G01。

**优先级：** MVP 后续增强；七天真实闭环试运优先。

**指令：**

- 分批回填，可暂停、恢复和防重。
- 当日任务优先。

**验证测试：**

- 缩短范围验证分批、暂停和恢复。
- 重跑不产生重复事件。
- 当日发布不被回填阻塞。

### G04 实现备份和恢复

**依赖：** B05。

**指令：**

- 定期导出飞书正式数据到受保护位置。
- 记录 Schema 版本和恢复步骤。

**验证测试：**

- 在隔离测试表完成恢复演练。
- 关联、业务 ID、日报和内部字段完整。
- 备份不会进入 Pages artifact。

### G05 七天真实试运行

**依赖：** F05、G01、G02。

**指令：**

- 在受控预算内运行七天。
- 记录候选、有效来源、重复、漏报、人工采纳和失败恢复。

**验证测试：**

- 每天使用已知事件对照集抽检。
- WorkBuddy缺席时海外链路和网站仍发布。
- 外部输入无法越权写入正式事件。

### G06 公司交接演练

**依赖：** G04、G05。

**指令：**

- 由非原开发者完成一次手动补跑、重新构建和 Pages 发布。
- 验证至少两名管理员权限。

**验证测试：**

- 不使用原开发者电脑或个人密钥完成流程。
- 密钥轮换后系统恢复运行。

### G07 Claude Code 只读审核

**依赖：** G06。

**指令：**

- 审核安全边界、飞书权限、公开字段、幂等、并发、错误处理和测试缺口。
- 不修改任何文件。

**验证测试：**

- 确认审核过程无代码改动。
- Codex复现每条有效发现并增加回归测试。

### G08 最终验收

**依赖：** G07。

**指令：**

- 修复确认问题。
- 对照 PRD P0、SPEC Definition of Done 和生产操作手册验收。

**验证测试：**

- 全量 CI 通过。
- 飞书审核、日报、通知和 Pages 网站端到端通过。
- 网站敏感字段扫描通过。
- 公司管理员可独立补跑和重新发布。

## 9. 可并行关系

在 A02、A03 和飞书字段映射冻结后：

- C01 与 C03 可并行。
- E03 可与 C05 至 C09 并行。
- D04 可与 E04 至 E08 并行。
- G01 与 G04 可并行。

以下文件或契约只能由 Codex统一集成：

- 领域类型和 Zod Schema。
- 飞书字段映射。
- 公开 DTO。
- GitHub Actions 生产 Workflow。
- 环境变量与 Secrets 名称。

## 10. 明确排除

- Cloudflare、Vercel、Neon、PostgreSQL、Drizzle。
- 独立管理后台。
- 常在线个人电脑。
- Obsidian。
- Coze。
- 飞书消息卡片。
- 微信登录自动化和反爬绕过。
- 用户账号、评论、收藏和 CRM。
- 外部 Agent 直接发布或读取内部战投备注。
