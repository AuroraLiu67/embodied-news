# Embodied News — MVP实施计划

> 状态：v1.0
> 目标：2026-08-07交付可每日真实使用的MVP
> 依据：[`design-document.md`](./design-document.md)、[`TECH_STACK.md`](./TECH_STACK.md)
> 架构约束：[`architecture.md`](./architecture.md)
> 协作规则：[`AGENTS.md`](./AGENTS.md)
> 进度台账：[`progress.md`](./progress.md)
> 本文只包含实施指令，不包含代码

## 1. 使用方式

本计划供多个AI开发者分工执行。主Agent负责调度、文件Owner、依赖顺序、验收和冲突处理。

任务状态只能是：

- `待开始`
- `进行中`
- `待验证`
- `完成`
- `阻塞`

每个任务开始前，执行者必须：

1. 完整阅读`architecture.md`和`design-document.md`；
2. 阅读`AGENTS.md`、`progress.md`及任务相关领域文档；
3. 在`progress.md`中把任务标记为`进行中`；
4. 声明Owner、负责文件、共享接口和Non-goals；
5. 检查工作区，确认没有其他Agent并发修改同一目标；
6. 确认前置任务已经完成。

每个任务结束时，执行者必须更新`progress.md`，再按`AGENTS.md`交接模板报告并提供实际测试结果。未运行测试不得标记完成。

## 2. 并行与Owner规则

- Schema、Alembic Migration、OpenAPI生成源、生成的TypeScript Client分别只能有一个Owner；
- 同一文件同一时间只能有一个写入Owner；
- 前端不得自行发明后端字段或状态；
- 后端不得自行改变产品流程；
- Consumer只能基于已确认并合并的契约开发；
- 任何影响MVP范围、成本、隐私、统计口径或Hermes权限的变化必须先获得用户批准；
- 每完成一个重大功能或里程碑，Architecture Owner必须更新`architecture.md`。

推荐工作流Owner：

- Coordinator：主Agent；
- Architecture/Data：架构与数据模型Agent；
- Backend：API与应用服务Agent；
- Ingestion：采集Agent；
- Intelligence：去重、分类、核验与模型Agent；
- Frontend：Web Agent；
- Delivery：PDF、Hermes和飞书Agent；
- Quality/Ops：测试、部署、监控、备份Agent；
- Research/Data：来源清单和31家公司回溯Agent。

## 3. 计划节奏

| 日期 | 主要目标 |
| --- | --- |
| 7月27日 | 决策清障、仓库骨架、开发环境 |
| 7月28日 | 核心Schema、Migration、认证、基础API |
| 7月29日 | 收件箱、公开采集、运行审计 |
| 7月30日 | Event处理、融资抽取、核验与统计 |
| 7月31日 | 审核、Brief快照与09:00发布 |
| 8月1–2日 | Web核心页面与融资看板 |
| 8月3日 | PDF、Hermes工具和飞书私聊 |
| 8月4日 | 部署、备份、告警、完整数据回填 |
| 8月5–6日 | 故障演练、端到端验收和修复 |
| 8月7日 | 上线验收与真实使用 |

日期用于排优先级。任何延期先压缩来源数量或视觉复杂度，不得牺牲证据、审核、安全、幂等和失败可见性。

---

## 波次0：决策清障与任务基线

### 0.1 初始化实施进度台账

- Owner：Coordinator
- 前置：无
- 指令：以`progress.md`作为唯一执行进度台账，登记当前批次任务的Owner、状态、依赖、负责文件和验收结果；后续任务按实际启动情况增量登记。
- 产物：所有Agent可读取且不存在重复状态源的进度台账。
- 验证：随机检查五个跨工作流任务，均能追溯到唯一Owner、依赖和测试要求。
- 禁止：不得让两个Agent同时拥有同一Schema、Migration或生成文件。

### 0.2 冻结MVP范围基线

- Owner：Product/Docs
- 前置：0.1
- 指令：对照产品设计，将所有功能标记为M0、MVP、POST_MVP或UNDECIDED，并把后置功能从开发任务中移除。
- 产物：任务台账中的范围标签。
- 验证：确认Notes、通用Timeline、完整搜索、关系网络、保存对比、部门权限均没有MVP开发任务。
- 禁止：不得删除长期需求文档，只能将其移出MVP。

### 0.3 确认首批Source Registry

- Owner：Research/Data
- 前置：0.2
- 指令：提出一份8月7日前可稳定接入的最小来源清单，覆盖公司官网/RSS、arXiv、主要海外新闻和公开融资/监管来源；为每个来源标记优先级、采集方式、频率、条款风险和预期产量。
- 产物：待用户确认的Source Registry文档。
- 验证：清单至少覆盖产品设计要求的四类自动来源，并明确哪些来源不承诺MVP自动化。
- 禁止：用户未确认前不得将来源写死为正式生产配置。

### 0.4 确认汇率服务方案

- Owner：Architecture/Data
- 前置：0.2
- 指令：比较可用汇率来源的成本、历史汇率、许可、稳定性和可替换性，提出一个MVP默认方案和一个后备方案。
- 产物：汇率决策提案或ADR。
- 验证：使用至少三种币种和两个历史日期验证能取得汇率，并能保存来源、日期和版本。
- 禁止：不得只保存人民币结果而丢失原币与汇率证据。

### 0.5 完成Hermes/飞书可行性验证

- Owner：Delivery
- 前置：0.2
- 指令：验证Hermes能接入飞书私聊、接收消息、主动发送消息并调用一个模拟只读工具；记录所需权限、凭证、部署条件和限制。
- 产物：Hermes/飞书验证报告与部署决策提案。
- 验证：在测试飞书私聊中完成一次用户提问和一次主动推送；确认工具调用不具有写权限。
- 禁止：不得让Hermes连接生产数据库或获得Admin Token。

### 0.6 确认MVP模型与固定评测策略

- Owner：Intelligence
- 前置：0.2
- 指令：提出OpenAI主模型、低成本路径和中文候选模型的配置策略；定义结构化抽取、摘要、核验各自的固定评测样本。
- 产物：模型配置提案与评测集说明。
- 验证：每类任务至少有成功、缺字段、冲突、恶意正文四类样本，并定义通过标准。
- 禁止：不得把具体模型名称写死在领域逻辑中。

### 0.7 确认自动发布初始门槛

- Owner：Product/Intelligence
- 前置：0.6
- 指令：将已确认的一手来源和双独立可信来源规则转换为可配置的MVP门槛，列明必须人工审核的情况。
- 产物：门槛配置说明。
- 验证：对官方公告、双媒体一致、单一KOL、金额冲突、实体不明五个案例得到符合产品文档的分流结果。
- 禁止：不得用重要性总分替代事实核验。

### 0.8 更新架构开放项

- Owner：Architecture/Data
- 前置：0.3–0.7
- 指令：将已经确认的来源、汇率、Hermes和模型决定同步到`architecture.md`与对应ADR；未确认项继续标记开放。
- 产物：更新后的架构文档。
- 验证：跨文档搜索不存在同一事项既“待定”又“已确定”的矛盾。
- 禁止：不得顺带修改无关产品范围。

---

## 波次1：仓库骨架与开发环境

### 1.1 创建Monorepo目录骨架

- Owner：Architecture/Data
- 前置：0.2
- 指令：按`TECH_STACK.md`建立Web、Backend、Worker、共享Client、UI、Scripts和Docs目录。
- 产物：与架构文档一致的目录结构。
- 验证：逐项对照`architecture.md`目录章节，确认没有第二套后端或重复Worker业务目录。
- 禁止：不得创建Notes、独立搜索服务或微服务目录。

### 1.2 初始化TypeScript Workspace

- Owner：Frontend
- 前置：1.1
- 指令：初始化pnpm Workspace、Next.js应用、TypeScript严格检查、ESLint和Prettier。
- 产物：可启动的最小Web应用。
- 验证：安装、类型检查、Lint、测试和生产构建均成功；首页能在本地打开。
- 禁止：不得引入第二套UI组件库或最终品牌样式。

### 1.3 初始化Python Workspace

- Owner：Backend
- 前置：1.1
- 指令：使用uv和标准`pyproject.toml`初始化FastAPI、领域包、测试包和Worker入口。
- 产物：可启动的最小API和Worker进程。
- 验证：Python依赖锁定成功；类型检查、Lint和最小API测试通过；Worker能启动并等待任务。
- 禁止：不得复制Backend业务代码到Worker。

### 1.4 建立本地基础设施

- Owner：Quality/Ops
- 前置：1.1
- 指令：配置Docker Compose运行本地PostgreSQL和Redis，使用独立开发数据卷与健康检查。
- 产物：可重复启动的本地基础设施。
- 验证：全新环境可启动；PostgreSQL和Redis健康；停止后重启数据行为符合说明。
- 禁止：不得连接或复制生产数据库。

### 1.5 建立统一Make命令

- Owner：Architecture/Data
- 前置：1.2–1.4
- 指令：提供setup、dev、lint、test、migrate、worker等统一入口，并保证本地与CI调用同一底层流程。
- 产物：根目录Makefile。
- 验证：在全新工作区逐一执行所有入口；错误时返回非零状态并显示可理解提示。
- 禁止：不得让CI依赖一套本地无法复现的隐藏命令。

### 1.6 建立环境变量契约

- Owner：Architecture/Data
- 前置：1.2–1.4
- 指令：整理Web、API、Worker、R2、Supabase、模型、Hermes和飞书需要的变量名、所属服务、是否必需和安全级别。
- 产物：不含真实Secret的环境变量示例和说明。
- 验证：Secret扫描确认没有真实凭证；缺少必需变量时服务明确拒绝启动。
- 禁止：不得把Secret默认值、Cookie或Webhook写入仓库。

### 1.7 建立基础CI

- Owner：Quality/Ops
- 前置：1.2–1.6
- 指令：配置GitHub Actions执行Python与TypeScript安装、Lint、类型检查、测试、Migration检查、生产构建和镜像Smoke Test。
- 产物：基础CI工作流。
- 验证：正常分支全部通过；故意制造一项Lint错误和一项失败测试时CI正确失败。
- 禁止：普通CI不得实时调用付费模型或外部采集站点。

### 1.8 建立结构化日志基础

- Owner：Quality/Ops
- 前置：1.3
- 指令：统一API与Worker的JSON日志格式和关联ID字段，并配置Secret过滤。
- 产物：日志配置与测试。
- 验证：模拟请求与任务后能按`request_id`和`run_id`串联；Authorization、Cookie和Token不出现在日志。
- 禁止：不得记录完整正文、隐藏推理或个人Browser Profile。

### 1.9 更新架构里程碑

- Owner：Architecture/Data
- 前置：1.1–1.8
- 指令：将实际目录、进程和本地运行方式同步至`architecture.md`。
- 产物：更新后的架构文档。
- 验证：新开发者只阅读文档即可正确启动骨架。
- 禁止：不得把尚未实现的组件标记为已完成。

---

## 波次2：数据模型与首个Migration

### 2.1 冻结领域枚举与状态表

- Owner：Architecture/Data
- 前置：0.7、1.3
- 指令：整理通用事件、融资核验、审核、发布、统计资格、Brief、任务和来源健康状态，标明允许转换。
- 产物：领域枚举与状态转换文档。
- 验证：覆盖待证实、冲突、撤回、纠正、被替代和部分发布；不存在一个字段同时表达四类融资状态。
- 禁止：不得由前端或数据库自由文本代替权威状态。

### 2.2 设计身份与权限表

- Owner：Architecture/Data
- 前置：2.1
- 指令：设计User、Role、Permission、设备Credential和Agent Credential边界，MVP只启用Admin。
- 产物：身份与授权Schema设计。
- 验证：证明Admin、WorkBuddy设备和Hermes使用不同Scope；Hermes没有写权限。
- 禁止：不得把Supabase身份直接当作完整业务授权。

### 2.3 设计Source与运行审计表

- Owner：Architecture/Data
- 前置：2.1
- 指令：设计Source、原始对象引用、内容哈希、Source Registry、Checkpoint、IngestionRun和ProcessingRun。
- 产物：采集与运行Schema设计。
- 验证：同一内容重复提交可识别；来源失败、未采集和无新内容可分别表达。
- 禁止：不得把Redis任务ID作为唯一运行记录。

### 2.4 设计Event、Entity与Topic表

- Owner：Architecture/Data
- 前置：2.3
- 指令：设计Event与Source多对多关系、Entity别名、Topic和合并/拆分历史。
- 产物：核心情报Schema设计。
- 验证：一个Event可关联多个Source；一个Source可支持多个Event；合并后可追溯旧ID。
- 禁止：不得将文章直接当作Event。

### 2.5 设计Assessment与模型运行表

- Owner：Architecture/Data
- 前置：2.4
- 指令：设计分项评分、解释、规则版本、Prompt版本、模型版本、证据引用、成本和验证状态。
- 产物：Assessment与Agent运行Schema。
- 验证：可回答“该分数由什么产生、使用哪些来源、花费多少、谁修改过”。
- 禁止：不得只存综合分或隐藏推理。

### 2.6 设计融资聚合表

- Owner：Architecture/Data
- 前置：2.4
- 指令：设计FinancingEvent、交易类型、轮次、日期、用途及其与Event和Company Entity的关系。
- 产物：融资事件Schema。
- 验证：同一Event可以扩展融资信息；非融资Event不需要融资空字段。
- 禁止：不得把全部融资字段塞进Event。

### 2.7 设计融资断言与参与方表

- Owner：Architecture/Data
- 前置：2.6
- 指令：设计FinancingAmount、ValuationAssertion、InvestorParticipation和FieldAssertion，支持区间、未知、多币种和冲突。
- 产物：融资断言Schema。
- 验证：一个事件可同时保存媒体金额、官方金额和冲突证据；未披露不等于0。
- 禁止：不得只保存人民币换算值。

### 2.8 设计赛道分类表

- Owner：Architecture/Data
- 前置：2.4、2.6
- 指令：设计CompanySectorAssignment，支持唯一主赛道、多辅助标签、证据、置信度、分类版本和历史。
- 产物：赛道Schema。
- 验证：数据库层能阻止同一公司同时存在两个有效主赛道；分类变化保留历史。
- 禁止：不得把31家公司名单硬编码进业务逻辑。

### 2.9 设计审核表

- Owner：Architecture/Data
- 前置：2.1、2.4、2.6
- 指令：设计ReviewBatch、ReviewItem和ReviewDecision，支持通用事件和融资字段审核、批量决定及前后值。
- 产物：审核Schema。
- 验证：能够重建某天08:00批次、未处理项、自动发布后果和人工修改历史。
- 禁止：不得覆盖原AI结果。

### 2.10 设计Brief与递送表

- Owner：Architecture/Data
- 前置：2.4、2.9
- 指令：设计Brief、不可变BriefVersion、BriefItem、CoverageStatement、PDF状态和DeliveryAttempt。
- 产物：发布Schema。
- 验证：发布后纠正会产生新版本；PDF和飞书失败可独立记录；Web版本不被覆盖。
- 禁止：不得将PDF文件本身作为唯一Brief记录。

### 2.11 创建首个Alembic Migration

- Owner：Architecture/Data
- 前置：2.2–2.10
- 指令：将已审查Schema实现为一组按依赖排序的初始Migration，并写清恢复路径。
- 产物：可审查Migration。
- 验证：空数据库可升级到最新；Schema约束生效；可在测试环境完成降级或明确恢复验证。
- 禁止：Migration合并后不得原地修改。

### 2.12 建立Schema集成测试

- Owner：Quality/Ops
- 前置：2.11
- 指令：为唯一约束、外键、状态、软删除、版本和融资金额边界建立数据库测试。
- 产物：Schema集成测试集。
- 验证：有效数据成功；双主赛道、重复幂等键、无来源正式发布等非法数据被拒绝。
- 禁止：不得只在SQLite上验证PostgreSQL行为。

### 2.13 更新架构数据模型

- Owner：Architecture/Data
- 前置：2.11–2.12
- 指令：用实际聚合、关系和状态更新`architecture.md`，记录与初稿的差异。
- 产物：架构里程碑更新。
- 验证：架构图、Migration和领域命名一致。
- 禁止：不得记录尚未合并的Schema。

---

## 波次3：基础应用服务、认证与API契约

### 3.1 建立统一错误契约

- Owner：Backend
- 前置：1.3
- 指令：定义API错误code、安全message、request_id、retryable和字段错误格式。
- 产物：错误Schema与文档。
- 验证：认证失败、参数错误、冲突、未找到和可重试故障返回稳定且不泄密的结构。
- 禁止：不得把堆栈或数据库错误直接返回客户端。

### 3.2 接入Supabase Auth

- Owner：Backend
- 前置：2.2、3.1
- 指令：验证Supabase身份并映射内部User与Role，只允许Invite-only Admin进入MVP后台。
- 产物：认证与用户同步流程。
- 验证：有效Admin可访问；未邀请用户、过期Token和伪造Token被拒绝。
- 禁止：不得只靠前端隐藏实现授权。

### 3.3 建立授权中间层

- Owner：Backend
- 前置：3.2
- 指令：实现用户、WorkBuddy设备、内部任务和Hermes的独立Scope检查。
- 产物：可复用授权策略。
- 验证：Hermes只读、WorkBuddy只提交、Admin可审核；越权测试全部被拒绝并审计。
- 禁止：不得共享一个万能API Key。

### 3.4 建立Repository与事务边界

- Owner：Backend
- 前置：2.11
- 指令：为核心聚合建立Repository接口和应用事务边界，Worker与API复用同一服务。
- 产物：Repository与Unit of Work约定。
- 验证：事务失败完整回滚；Worker路径和API路径产生相同审计行为。
- 禁止：Route和任务入口不得散落直接写库逻辑。

### 3.5 建立健康检查API

- Owner：Backend
- 前置：3.1、3.4
- 指令：提供进程、PostgreSQL、Redis和对象存储的分层健康状态。
- 产物：健康检查能力。
- 验证：分别断开各依赖，状态能区分健康、降级和不可用。
- 禁止：不得在公开健康接口泄露连接信息。

### 3.6 建立Event只读API

- Owner：Backend
- 前置：3.2–3.4
- 指令：提供Event列表、详情、关联Source、Entity、Topic、Assessment和版本读取能力。
- 产物：Event读取契约。
- 验证：Cursor Pagination稳定；每个发布Event可取得至少一个Source；权限测试通过。
- 禁止：不得返回模型隐藏推理或不必要完整正文。

### 3.7 建立融资只读API

- Owner：Backend
- 前置：3.4
- 指令：提供融资列表、详情、筛选、统计、全栈专题和2–5家公司即时对比契约。
- 产物：融资读取契约。
- 验证：待证实默认不入正式统计；未披露金额不显示0；不同交易类型统计隔离。
- 禁止：不得实现保存对比组合。

### 3.8 建立审核写入API

- Owner：Backend
- 前置：3.3、3.4
- 指令：提供通过、修改后通过、待证实、驳回、合并、重新处理和批量通过能力，使用乐观并发。
- 产物：审核写入契约。
- 验证：并发修改时旧版本被拒绝；每次决定保存操作者、时间、前后值和依据。
- 禁止：不得允许Hermes或WorkBuddy调用。

### 3.9 建立Source Health与运行API

- Owner：Backend
- 前置：3.4、3.5
- 指令：提供来源状态、最后成功、连续失败、当日产量、覆盖影响和任务重试能力。
- 产物：运行状态契约。
- 验证：无新内容、未采集和采集失败返回不同状态。
- 禁止：不得返回Secret或本地Cookie状态详情。

### 3.10 生成并验证OpenAPI Client

- Owner：Backend，生成文件Owner唯一
- 前置：3.1–3.9
- 指令：生成TypeScript Client并让Web使用生成类型。
- 产物：版本化API Schema和生成Client。
- 验证：Schema生成可重复；前端类型检查通过；破坏性差异能被CI发现。
- 禁止：消费者不得手工编辑生成文件。

---

## 波次4：统一收件箱与公开采集

### 4.1 冻结Ingestion Submission契约

- Owner：Ingestion
- 前置：2.3、3.3
- 指令：定义URL、文本、PDF、CSV/JSON和WorkBuddy API统一提交结构、版本、大小限制和错误语义。
- 产物：Ingestion契约文档。
- 验证：五种入口都能映射到相同Submission状态机。
- 禁止：不得包含个人Cookie、Session或Browser Profile字段。

### 4.2 实现URL提交

- Owner：Ingestion
- 前置：4.1
- 指令：接收单个URL，完成规范化、安全校验、重复提示、提交记录和异步处理入队。
- 产物：URL收件入口。
- 验证：有效URL成功；重复URL返回已有记录；内网地址、非法协议和超长URL被拒绝。
- 禁止：不得允许服务端请求内部网络资源。

### 4.3 实现文本提交

- Owner：Ingestion
- 前置：4.1
- 指令：接收粘贴文本及来源元数据，保存原始引用并进入统一处理。
- 产物：文本收件入口。
- 验证：缺少任何来源依据时只生成待核实候选，不能直接发布。
- 禁止：不得把文本中的指令当作系统指令。

### 4.4 实现PDF提交

- Owner：Ingestion
- 前置：4.1、对象存储可用
- 指令：校验PDF类型、大小、页数和哈希，保存私有对象并进入提取队列。
- 产物：PDF收件入口。
- 验证：正常PDF可处理；重复文件去重；损坏、伪装或超限文件被安全拒绝。
- 禁止：不得公开R2对象地址。

### 4.5 实现CSV/JSON批量提交

- Owner：Ingestion
- 前置：4.1
- 指令：提供校验预览、行级错误、部分成功和处理状态。
- 产物：批量收件入口。
- 验证：混合有效/无效文件只接收有效行，并准确返回失败行与错误原因。
- 禁止：不得因单行失败回滚全部有效行，除非文件级Schema无效。

### 4.6 实现WorkBuddy设备接入

- Owner：Ingestion
- 前置：3.3、4.1
- 指令：使用独立设备Credential接收结构化线索、采集时间、原始链接、平台和内容哈希。
- 产物：受限WorkBuddy Ingestion API。
- 验证：有效设备可提交；撤销、过期、越权和重放Token被拒绝并记录。
- 禁止：不得提供远程读取本地Cookie的能力。

### 4.7 实现SourceAdapter基础接口

- Owner：Ingestion
- 前置：2.3
- 指令：实现discover、fetch、extract、checkpoint、health统一边界与错误分类。
- 产物：Adapter接口和测试替身。
- 验证：模拟成功、无新内容、限流、解析失败和永久失败，均得到明确状态。
- 禁止：Adapter不得执行Event合并、评分或发布。

### 4.8 实现首个RSS/Atom Adapter

- Owner：Ingestion
- 前置：4.7、0.3
- 指令：选择一个已批准高优先级来源，实现发现、Checkpoint、内容提取和健康记录。
- 产物：首个真实公开采集器。
- 验证：使用录制Fixture稳定重放；连续运行不会重复创建Source。
- 禁止：CI不得依赖来源实时在线。

### 4.9 实现arXiv Adapter

- Owner：Ingestion
- 前置：4.7、0.3
- 指令：按主题查询或Feed采集论文元数据、摘要、作者、发布时间和原始链接。
- 产物：arXiv采集器。
- 验证：分页、重复论文、更新版本和无结果情况正确处理。
- 禁止：不得把论文摘要自动视为行业重大事件。

### 4.10 实现公司官网/公开公告Adapter模板

- Owner：Ingestion
- 前置：4.7、0.3
- 指令：建立可配置的RSS/Sitemap/静态页面模板，先接入少量高优先级公司。
- 产物：官网采集模板及首批配置。
- 验证：至少两个结构不同的官网成功采集；页面改版时记录解析失败而非返回空新闻。
- 禁止：不得绕过访问控制或验证码。

### 4.11 建立来源健康计算

- Owner：Ingestion
- 前置：4.7–4.10
- 指令：根据最后成功、连续失败、预期频率和异常零产量生成健康状态与覆盖影响。
- 产物：Source Health服务。
- 验证：正常、降级、失败、暂停、未接入和无新内容六种情况可区分。
- 禁止：不得把异常零产量标记为正常无新闻。

### 4.12 更新采集架构里程碑

- Owner：Architecture/Data
- 前置：4.1–4.11
- 指令：将实际Ingestion契约、Adapter和设备边界更新至`architecture.md`。
- 产物：架构更新。
- 验证：WorkBuddy开发者能仅依据文档构造合法提交。
- 禁止：不得在文档中加入真实Token或Cookie示例。

---

## 波次5：情报处理与模型网关

### 5.1 实现内容规范化

- Owner：Intelligence
- 前置：4.1
- 指令：规范标题、正文、作者、平台、时间、URL和语言，同时保存原始值。
- 产物：规范化处理器。
- 验证：中英文、缺失字段、时区和异常日期Fixture均得到明确结果或错误。
- 禁止：不得覆盖原始内容。

### 5.2 实现URL与内容哈希去重

- Owner：Intelligence
- 前置：5.1
- 指令：按规范URL、内容哈希和来源ID处理重复Source。
- 产物：Source去重服务。
- 验证：相同URL参数变体、相同内容不同URL、不同内容同URL更新均按规则处理。
- 禁止：不得将相似但不同事件直接删除。

### 5.3 实现Event候选聚类

- Owner：Intelligence
- 前置：2.4、5.2
- 指令：使用实体、时间、标题和文本相似度生成Event候选，输出合并理由与置信度。
- 产物：Event Resolver第一版。
- 验证：同一融资多报道合并；不同公司同类新闻不合并；低置信进入审核。
- 禁止：不得仅依赖向量相似度自动合并。

### 5.4 实现Entity解析与别名

- Owner：Intelligence
- 前置：2.4
- 指令：解析公司、机构、投资方、模型和技术，支持中英文别名及同名冲突。
- 产物：Entity Resolver。
- 验证：31家公司中英文名称、常见简称和同名机构Fixture正确解析。
- 禁止：无法消歧时不得自动创建重复权威Entity。

### 5.5 实现Topic与赛道分类

- Owner：Intelligence
- 前置：2.8、5.4
- 指令：输出Topic、唯一主赛道、辅助赛道、证据、置信度和分类版本。
- 产物：分类服务。
- 验证：19家全栈正样本通过固定Fixture；同一公司只有一个有效主赛道。
- 禁止：不得仅根据公司名单返回分类而忽略证据。

### 5.6 实现Assessment分项评分

- Owner：Intelligence
- 前置：2.5、0.7
- 指令：计算相关性、影响、来源可信度、新颖性、Watchlist和多来源验证，保存解释与版本。
- 产物：评分服务。
- 验证：每个分项可独立检查；改变规则版本不会覆盖旧评分。
- 禁止：不得将总分作为事实确认。

### 5.7 实现ModelGateway

- Owner：Intelligence
- 前置：0.6、1.6
- 指令：隔离模型提供商，统一结构化请求、Schema验证、超时、重试、成本和版本审计。
- 产物：模型网关。
- 验证：主模型成功、超时、无效Schema、后备模型和预算拒绝均有确定结果。
- 禁止：不得把供应商SDK对象泄露到Domain层。

### 5.8 实现中文摘要与标题生成

- Owner：Intelligence
- 前置：5.7
- 指令：从证据生成中文标题、一句话结论和摘要，保留原文标题、链接和专名。
- 产物：结构化摘要服务。
- 验证：外文Fixture输出中文摘要；专名不被强行翻译；无来源时拒绝生成发布事实。
- 禁止：不得添加证据中不存在的数字或结论。

### 5.9 实现Verifier

- Owner：Intelligence
- 前置：5.3–5.8
- 指令：判断来源独立性、关键字段一致性、事实状态和是否需要人工审核。
- 产物：核验服务。
- 验证：官方、双独立媒体、单一KOL、互相转载、字段冲突五类Fixture正确分流。
- 禁止：不得把两个转载同一原始消息的媒体视为独立来源。

### 5.10 建立处理Pipeline与审计

- Owner：Intelligence
- 前置：5.1–5.9
- 指令：用确定性应用服务串联规范化、去重、聚类、实体、分类、评分、摘要和核验。
- 产物：端到端Processing Pipeline。
- 验证：同一输入重放不重复产生业务记录或模型费用；每阶段可追溯输入、版本和终态。
- 禁止：不得让Agent自由决定下一步系统动作。

---

## 波次6：融资处理、统计与31家公司回溯

### 6.1 实现融资候选识别

- Owner：Intelligence/Financing
- 前置：5.10
- 指令：从Event候选识别股权、战略投资、债务/授信、补贴、IPO再融资、并购和其他交易。
- 产物：融资分类服务。
- 验证：各交易类型至少两个Fixture；普通合作新闻不被误判为融资。
- 禁止：不得将所有“投资”字样当作融资。

### 6.2 实现融资字段抽取

- Owner：Intelligence/Financing
- 前置：6.1、5.7
- 指令：抽取公司、日期、轮次、金额、币种、估值、投资方、角色、用途和原文证据。
- 产物：结构化融资抽取服务。
- 验证：精确金额、模糊金额、未披露、多个金额口径和缺字段Fixture正确处理。
- 禁止：不得在缺失时猜测金额、轮次或估值。

### 6.3 实现同轮融资合并

- Owner：Intelligence/Financing
- 前置：6.2、5.4
- 指令：根据公司、时间、轮次、金额、投资方和来源关系合并同一交易，并支持后续补充披露。
- 产物：融资Event Resolver。
- 验证：同轮多报道只累计一次；后续官方金额更新当前投影且保留旧断言。
- 禁止：不得把相邻但不同轮次自动合并。

### 6.4 实现汇率换算

- Owner：Backend/Financing
- 前置：0.4、6.2
- 指令：保存原币断言，并按事件日期或明确规则生成人民币派生值。
- 产物：FX服务与换算记录。
- 验证：多币种、周末、缺失历史汇率和重新计算场景正确；换算可追溯。
- 禁止：不得覆盖原币数据或使用未记录日期的当前汇率。

### 6.5 实现字段证据与冲突检测

- Owner：Intelligence/Financing
- 前置：6.2–6.4
- 指令：为关键字段建立FieldAssertion，比较来源并标记一致、缺失或冲突。
- 产物：字段级证据服务。
- 验证：金额、轮次、日期和投资方冲突可独立显示；一项冲突不抹掉其他已确认字段。
- 禁止：不得用单一事件级置信度替代字段证据。

### 6.6 实现融资发布资格判断

- Owner：Backend/Financing
- 前置：0.7、6.5
- 指令：独立计算verification、review、publication和include-in-statistics状态。
- 产物：融资状态服务。
- 验证：一手来源和双独立可信来源可正式发布；待证实和冲突不进统计。
- 禁止：不得复用一个枚举表达全部状态。

### 6.7 实现融资统计Read Model

- Owner：Backend/Financing
- 前置：6.6
- 指令：计算事件数、股权与战略投资总额、其他交易分卡、月度趋势、赛道/地区/轮次分布和未披露数量。
- 产物：可重算统计服务。
- 验证：删除缓存后可从权威表重建；待证实不计入；未披露不当0；不同类型不混加。
- 禁止：不得手工维护累计总额。

### 6.8 建立31家公司种子清单

- Owner：Research/Data
- 前置：2.8
- 指令：根据融资规格录入31家公司的标准名称、别名、初始赛道、证据和回溯状态。
- 产物：可审查Seed数据。
- 验证：总数准确为31；19家全栈、5家大脑、7家本体；每家公司只有一个主赛道。
- 禁止：不得把种子名单硬编码在应用逻辑。

### 6.9 建立历史回溯任务

- Owner：Research/Data + Ingestion
- 前置：0.3、6.8
- 指令：按公司和时间窗口创建2025-01-01至今的可暂停、可重放回溯任务。
- 产物：回溯任务与进度记录。
- 验证：中断后从Checkpoint继续；同一Source和融资不会重复；进度显示完成、部分或未开始。
- 禁止：不得将未回溯显示为无融资。

### 6.10 执行首批回溯与人工抽查

- Owner：Research/Data
- 前置：6.9
- 指令：分批运行31家公司回溯，对高金额、模糊金额和重复融资进行人工抽查。
- 产物：首批融资数据和覆盖报告。
- 验证：每家公司显示覆盖状态；随机抽查至少每类赛道和交易类型，来源、金额、时间和状态一致。
- 禁止：不得为追求完成率隐藏失败公司或来源。

### 6.11 建立融资回归集

- Owner：Quality/Ops
- 前置：6.2–6.10
- 指令：固定同轮补充、官方纠正、模糊/未披露金额、多币种、同名实体、交易隔离、待证实、主赛道和公司对比Fixture。
- 产物：融资回归测试集。
- 验证：每次融资逻辑变更都会运行完整回归；故意破坏一项统计规则时测试失败。
- 禁止：不得依赖实时网页作为普通测试输入。

### 6.12 更新融资架构里程碑

- Owner：Architecture/Data
- 前置：6.1–6.11
- 指令：将实际融资状态、断言、FX、统计投影和回溯机制同步至`architecture.md`。
- 产物：架构更新。
- 验证：架构描述与实际Migration、应用服务和统计口径一致。
- 禁止：不得把关系网络或保存对比写成MVP已实现。

---

## 波次7：审核、Brief与调度

### 7.1 实现审核优先级计算

- Owner：Backend
- 前置：5.10、6.6
- 指令：根据影响、证据风险、冲突、疑似漏报和自动发布时限排序审核候选。
- 产物：审核优先级服务。
- 验证：必须处理区最多15条；相同Event聚合；高风险融资优先于普通候选。
- 禁止：不得简单按创建时间截取15条。

### 7.2 实现08:00融资ReviewBatch

- Owner：Backend
- 前置：2.9、7.1
- 指令：生成可审计的每日融资审核快照，记录候选版本和未处理后果。
- 产物：08:00 ReviewBatch任务。
- 验证：重复运行产生同一批次或安全新版本；08:00后新增疑点默认进入下一批。
- 禁止：不得将08:00当作采集截止。

### 7.3 实现通用审核动作

- Owner：Backend
- 前置：3.8、7.1
- 指令：将通过、修改、待证实、驳回、合并、重处理和批量通过连接到领域状态与审计。
- 产物：完整审核应用服务。
- 验证：所有动作可撤销或通过新决定纠正；旧版本并发写入被拒绝。
- 禁止：不得直接覆盖原始Source或AI输出。

### 7.4 实现Brief候选查询

- Owner：Backend/Publishing
- 前置：5.10、7.3
- 指令：按截止时间、栏目、状态、重要性和Watchlist生成候选，排除重复Event。
- 产物：Brief候选服务。
- 验证：待核实独立分区；低优先级进入更多动态；同一Event只出现一次。
- 禁止：不得因篇幅限制删除数据库中的低优先级Event。

### 7.5 实现08:30不可变快照

- Owner：Backend/Publishing
- 前置：2.10、7.4
- 指令：在08:30冻结候选版本、来源覆盖和处理版本，形成不可变BriefVersion。
- 产物：Brief快照任务。
- 验证：重复运行幂等；发布后修改Event不会无痕改变旧Brief。
- 禁止：不得从动态查询直接渲染已发布Brief。

### 7.6 实现Brief编排

- Owner：Backend/Publishing
- 前置：7.5、5.8
- 指令：编排3–5条今日重点和总计约15–25个主要Event，按产品栏目输出结构化内容。
- 产物：Brief Composer。
- 验证：栏目顺序、条数、来源、核验状态和覆盖说明符合设计文档。
- 禁止：不得为了凑足15条加入明显无关内容。

### 7.7 实现发布前校验

- Owner：Quality/Ops + Backend
- 前置：7.6
- 指令：检查来源追溯、重复、待核实分区、摘要Schema、链接、覆盖声明和版本完整性。
- 产物：发布Validator。
- 验证：缺来源、重复Event、错误统计或无效链接会阻止完整发布并给出可操作错误。
- 禁止：不得因单个非关键来源失败阻塞部分发布。

### 7.8 实现09:00 Web发布任务

- Owner：Backend/Publishing
- 前置：7.5–7.7
- 指令：将通过校验的BriefVersion标记发布，记录完整或部分状态，并触发递送和PDF任务。
- 产物：09:00发布任务。
- 验证：同一版本重放不重复发布或递送；部分来源失败时发布状态为partial并显示缺口。
- 禁止：不得等待PDF或飞书成功后才发布Web。

### 7.9 实现发布后纠正

- Owner：Backend/Publishing
- 前置：7.8
- 指令：支持纠正、撤回或补充事件，创建新的BriefVersion并保留历史。
- 产物：Brief修订服务。
- 验证：旧链接仍能查看旧版本；当前版本显示更新标记和变更原因。
- 禁止：不得原地覆盖已发布快照。

### 7.10 建立早间调度集成测试

- Owner：Quality/Ops
- 前置：7.2、7.8
- 指令：使用可控时钟验证08:00融资审核、08:00 Alert汇总、08:30快照和09:00发布是独立幂等任务。
- 产物：调度集成测试。
- 验证：改变任务顺序、重复执行和单任务失败时不会产生重复或错误状态。
- 禁止：不得依赖真实等待到指定时间测试。

### 7.11 更新发布架构里程碑

- Owner：Architecture/Data
- 前置：7.1–7.10
- 指令：把实际调度、Brief状态和降级路径同步至`architecture.md`。
- 产物：架构更新。
- 验证：文档可准确解释09:00发布失败、部分发布和重放行为。
- 禁止：不得把尚未完成的飞书/PDF标记成功。

---

## 波次8：Web核心页面

### 8.1 建立Web应用壳与Admin登录

- Owner：Frontend
- 前置：1.2、3.2、3.10
- 指令：建立主导航、登录态、受保护页面、统一加载和错误边界。
- 产物：可登录的Web壳。
- 验证：Admin可进入；未登录和非Admin被阻止；手机与桌面导航可用。
- 禁止：不得显示Notes等后置功能的可点击假入口。

### 8.2 实现今日早报页

- Owner：Frontend
- 前置：7.8、8.1
- 指令：展示日期、覆盖区间、状态、3–5条重点、栏目、待核实和覆盖缺口。
- 产物：Brief页面。
- 验证：完整、部分、更新、无事件和生成失败Fixture均正确显示。
- 禁止：不得把待核实信息与已确认事实混排。

### 8.3 实现Brief事件展开

- Owner：Frontend
- 前置：8.2、3.6
- 指令：展示多来源、评分分项、事实/AI判断、冲突、版本和原文入口。
- 产物：事件展开交互。
- 验证：每个发布事实至少可点击一个Source；键盘与手机触控可操作。
- 禁止：不得默认展开完整第三方正文。

### 8.4 实现Event详情页

- Owner：Frontend
- 前置：3.6、8.1
- 指令：展示事件概览、Entity、Topic、Source、Assessment、状态和修订历史。
- 产物：Event详情页。
- 验证：已确认、待核实、冲突、纠正和软删除状态语义清楚。
- 禁止：不得把AI摘要标记为原文。

### 8.5 实现融资事件表

- Owner：Frontend
- 前置：3.7、8.1
- 指令：提供公司、赛道、地区、时间、轮次、交易类型、金额、投资方和状态筛选。
- 产物：融资列表页。
- 验证：筛选可组合；Cursor翻页不重复/漏项；未披露显示文字而非0。
- 禁止：不得默认将待证实纳入正式列表统计。

### 8.6 实现融资事件详情

- Owner：Frontend
- 前置：3.7、8.5
- 指令：展示原币、人民币、汇率、估值、投资方、用途、赛道、字段证据、冲突和版本。
- 产物：融资详情页。
- 验证：每个关键字段可追到Source或显示未披露；修改历史清晰。
- 禁止：不得隐藏冲突来源。

### 8.7 实现融资首页统计

- Owner：Frontend
- 前置：6.7、8.1
- 指令：展示事件数、股权/战略总额、其他交易分卡、月度趋势、赛道、地区、轮次和覆盖说明。
- 产物：融资看板首页。
- 验证：每张卡显示时间、口径、状态、未披露数和更新时间。
- 禁止：不得把不同交易类型无说明混加。

### 8.8 实现全栈融资专题

- Owner：Frontend
- 前置：6.7、6.8、8.7
- 指令：展示19家全栈公司的趋势、排名、轮次、融资间隔、新玩家和覆盖状态。
- 产物：全栈专题页。
- 验证：只使用有证据的全栈分类；待证实事件状态明显且默认不进金额。
- 禁止：不得实现投资关系网络。

### 8.9 实现2–5家公司即时对比

- Owner：Frontend
- 前置：3.7、8.5
- 指令：允许搜索并选择2–5家公司，在统一时间轴展示轮次、人民币金额、估值、投资方和核验状态。
- 产物：即时对比页面。
- 验证：少于2家提示选择；超过5家被阻止；待证实和未披露语义清楚；节点可进入详情。
- 禁止：不得实现保存或分享组合。

### 8.10 实现审核中心

- Owner：Frontend
- 前置：3.8、7.3、8.1
- 指令：实现必须处理、可选查看、已处理三区，显示证据、冲突、截止时间和批量操作。
- 产物：审核页面。
- 验证：必须区最多15条；并发冲突提示清楚；批量动作前显示影响范围。
- 禁止：不得默认全选高风险待证实内容。

### 8.11 实现采集收件箱

- Owner：Frontend
- 前置：4.2–4.6、8.1
- 指令：支持URL、文本、PDF、CSV/JSON提交与状态查看。
- 产物：收件箱页面。
- 验证：重复提示、上传进度、批量部分成功、错误行和重试状态正确。
- 禁止：不得在浏览器保存WorkBuddy设备Secret。

### 8.12 实现Source Health页面

- Owner：Frontend
- 前置：3.9、4.11、8.1
- 指令：展示来源状态、最后成功、失败、产量、覆盖影响和重试入口。
- 产物：来源健康页。
- 验证：未采集、无新内容和失败状态视觉与文字不同。
- 禁止：不得把零产量一律显示正常。

### 8.13 实现管理与运行状态页

- Owner：Frontend
- 前置：3.9、8.1
- 指令：展示Watchlist、调度、模型成本、备份、PDF、Hermes/飞书连接和Secret配置状态。
- 产物：Admin设置页。
- 验证：Secret只显示已配置/未配置；失败任务可安全重试；成本数据可归因。
- 禁止：不得返回Secret原文。

### 8.14 完成响应式与无障碍检查

- Owner：Quality/Ops + Frontend
- 前置：8.2–8.13
- 指令：验证手机、常见桌面宽度、键盘导航、焦点、语义标签和对比度基础要求。
- 产物：UI可访问性检查报告。
- 验证：核心页面在手机上无需横向滚动；审核和来源链接可用键盘操作。
- 禁止：不得以最终UI风格未定为由跳过可用性。

---

## 波次9：PDF

### 9.1 建立Brief打印视图

- Owner：Delivery
- 前置：7.6、8.2
- 指令：基于同一BriefVersion建立A4单栏打印视图，保留来源链接和覆盖说明。
- 产物：稳定打印页面。
- 验证：中文、英文专名、长链接、分页、待核实区和部分覆盖状态正确。
- 禁止：不得维护第二套独立内容数据。

### 9.2 实现PDF后台任务

- Owner：Delivery
- 前置：9.1、R2可用
- 指令：使用Playwright异步生成PDF，记录模板版本、哈希、对象引用、状态和错误。
- 产物：PDF生成任务。
- 验证：重复任务不重复存储；失败可重试；PDF失败不改变Web发布状态。
- 禁止：不得在Vercel函数中运行PDF任务。

### 9.3 实现稳定PDF入口

- Owner：Backend + Frontend
- 前置：9.2
- 指令：提供稳定入口，在生成中、完成、失败和重试时显示不同状态。
- 产物：PDF状态与下载页。
- 验证：09:00尚未完成时显示生成中；完成后同一入口可下载；未授权用户不能下载。
- 禁止：不得假设09:00消息一定附带PDF文件。

### 9.4 验证PDF视觉与文件完整性

- Owner：Quality/Ops
- 前置：9.2–9.3
- 指令：渲染多类Brief并检查页面、字体、链接、文件哈希和R2权限。
- 产物：PDF验收记录。
- 验证：抽取PDF文本与BriefVersion一致；链接可点击；私有对象不可匿名枚举。
- 禁止：不得只检查任务返回成功。

---

## 波次10：Hermes与飞书Agent

### 10.1 冻结Agent只读工具契约

- Owner：Delivery + Backend
- 前置：0.5、3.6、3.7、3.9
- 指令：定义get_daily_brief、search_events、get_event_sources、get_company_financing、compare_company_financing和get_coverage_status的输入、输出、错误、Scope和审计字段。
- 产物：Agent Tool Contract。
- 验证：每个工具只返回回答所需字段，并始终包含来源、状态、覆盖和深链能力。
- 禁止：不得包含审核、Watchlist编辑或任意SQL工具。

### 10.2 实现Agent Tool Gateway

- Owner：Backend
- 前置：10.1、3.3
- 指令：为Hermes提供白名单只读入口，执行鉴权、限流、输入验证、字段授权和审计。
- 产物：只读工具网关。
- 验证：有效工具成功；未知工具、写请求、超限、过期Token和越权字段被拒绝。
- 禁止：不得复用Admin权限。

### 10.3 实现Hermes工具适配

- Owner：Delivery
- 前置：10.2
- 指令：让Hermes调用白名单工具，并把结构化事实组织为中文回答。
- 产物：Hermes工具配置。
- 验证：六类工具均可调用；工具错误时明确说明不确定或覆盖不足。
- 禁止：不得让Hermes通过网页搜索替代系统数据库事实。

### 10.4 配置飞书Admin私聊

- Owner：Delivery
- 前置：0.5、10.3
- 指令：完成第三方智能体授权、Admin绑定、消息权限和生产/测试凭证隔离。
- 产物：飞书私聊连接。
- 验证：只有指定Admin能使用MVP Agent；群聊和未授权用户按设计拒绝或不响应。
- 禁止：不得把飞书凭证写入仓库或数据库正文。

### 10.5 实现09:00主动推送

- Owner：Delivery
- 前置：7.8、9.3、10.4
- 指令：发送早报前三条重点、可信状态、Web链接和稳定PDF入口，记录DeliveryAttempt。
- 产物：飞书主动递送。
- 验证：同一Brief/用户幂等；失败有限重试；Web发布不等待推送。
- 禁止：不得因PDF生成中而阻塞消息。

### 10.6 实现只读问答

- Owner：Delivery
- 前置：10.3–10.4
- 指令：支持今日/历史事件、公司/技术/赛道、融资查询和最多5家公司即时对比。
- 产物：飞书问答能力。
- 验证：预设问题集回答均包含来源、状态、覆盖范围和网站深链；未知问题明确说不知道。
- 禁止：不得编造数据库没有的融资或来源。

### 10.7 执行Hermes安全测试

- Owner：Quality/Ops
- 前置：10.2–10.6
- 指令：测试Prompt Injection、越权工具、写入请求、超长输入、速率限制和Token撤销。
- 产物：Hermes安全测试报告。
- 验证：所有写入和越权尝试失败且有审计；恶意Source正文不能改变工具权限。
- 禁止：不得只测试正常对话。

### 10.8 更新Hermes架构里程碑

- Owner：Architecture/Data
- 前置：10.1–10.7
- 指令：将最终Hermes部署、凭证、工具和故障路径同步至`architecture.md`。
- 产物：架构更新与运行手册链接。
- 验证：运维人员可按文档轮换凭证、检查状态和处理失败。
- 禁止：不得在文档中写真实凭证。

---

## 波次11：部署、监控、导出与备份

### 11.1 配置R2私有存储

- Owner：Quality/Ops
- 前置：1.6
- 指令：建立MVP所需私有Bucket、对象前缀、生命周期、签名下载和最小权限凭证。
- 产物：R2环境与StorageService配置。
- 验证：授权服务可读写；匿名访问失败；跨租户/错误前缀访问失败。
- 禁止：不得公开Bucket或使用永久公共URL。

### 11.2 部署Supabase开发与生产环境

- Owner：Quality/Ops
- 前置：2.11、3.2
- 指令：建立隔离的开发/生产项目或等效隔离，执行Migration并配置Auth。
- 产物：可用数据库与身份环境。
- 验证：生产Migration记录正确；本地环境无法误连生产；备份连接使用独立凭证。
- 禁止：不得手工修改生产Schema。

### 11.3 部署Railway服务

- Owner：Quality/Ops
- 前置：1.3、1.7、7.8
- 指令：从同一Python镜像部署API、Worker/Scheduler和Redis，设置健康检查、资源限制与Secret。
- 产物：Railway MVP环境。
- 验证：API健康；Worker消费任务；Scheduler只有一个有效实例；重启不丢业务事实。
- 禁止：不得把Redis当作唯一状态。

### 11.4 部署Vercel Web

- Owner：Quality/Ops + Frontend
- 前置：8.1–8.14
- 指令：部署Next.js，配置环境变量、Auth回调、API地址和生产构建。
- 产物：可访问Web。
- 验证：Admin登录、核心页面、手机访问和Source链接正常；未授权访问被拒绝。
- 禁止：不得在前端Bundle暴露服务端Secret。

### 11.5 接入Sentry与脱敏

- Owner：Quality/Ops
- 前置：1.8、11.3–11.4
- 指令：接入Web、API和Worker异常，配置采样和敏感字段过滤。
- 产物：错误监控。
- 验证：模拟异常能关联版本和request_id；Token、Cookie、正文和Prompt不出现在事件中。
- 禁止：不得启用包含私人内容的Session Replay。

### 11.6 实现Admin运行看板

- Owner：Quality/Ops + Frontend
- 前置：3.9、4.11、7.8、10.5
- 指令：展示来源、队列、ProcessingRun、发布、PDF、飞书、成本和失败重试状态。
- 产物：运行看板。
- 验证：能从一次Brief追到采集、处理、发布和递送状态；终态失败可见。
- 禁止：不得只依赖Railway日志。

### 11.7 实现数据导出

- Owner：Backend
- 前置：3.4、6.7
- 指令：导出Source、Event、Entity、融资、证据、Brief、Assessment和版本，使用可迁移格式。
- 产物：核心数据导出。
- 验证：导出数量与数据库一致；重新读取可恢复关键关系；无权限用户不能导出。
- 禁止：不得把个人Secret或登录态包含在导出中。

### 11.8 实现数据库备份

- Owner：Quality/Ops
- 前置：11.2
- 指令：每日生成加密PostgreSQL逻辑备份并复制到独立故障域，实施7日/8周/12月保留。
- 产物：自动备份任务。
- 验证：从备份恢复到空测试数据库，并核对关键表数量和约束。
- 禁止：不得把本地Mac作为唯一备份。

### 11.9 实现R2增量备份

- Owner：Quality/Ops
- 前置：11.1
- 指令：按SHA-256清单复制新增关键对象到独立位置，并记录校验结果。
- 产物：对象增量备份。
- 验证：新增、重复、损坏和缺失对象场景均正确；随机对象可恢复且哈希一致。
- 禁止：不得每天无差别复制所有对象。

### 11.10 建立成本与预算保护

- Owner：Quality/Ops
- 前置：5.7、11.3
- 指令：归集模型、转录、存储和基础设施成本，设置提醒和非关键任务熔断。
- 产物：成本看板与预算规则。
- 验证：模拟超过阈值时，历史回溯或高成本深度处理停止，09:00关键流程继续。
- 禁止：不得自动提高预算上限。

### 11.11 更新部署架构里程碑

- Owner：Architecture/Data
- 前置：11.1–11.10
- 指令：将真实部署拓扑、Secret边界、备份位置、健康检查和恢复流程同步至`architecture.md`。
- 产物：生产架构与运行说明。
- 验证：新运维人员可根据文档判断各服务位置、依赖和故障处理。
- 禁止：不得记录真实Secret或内部敏感地址。

---

## 波次12：验收、故障演练与上线

### 12.1 建立端到端验收数据集

- Owner：Quality/Ops
- 前置：5.10、6.11
- 指令：准备涵盖正常、重复、冲突、待证实、撤回、多币种、来源失败和恶意正文的固定数据。
- 产物：E2E验收Fixture。
- 验证：Fixture可重复导入并产生确定结果；不包含真实私人凭证。
- 禁止：不得依赖线上实时内容才能运行验收。

### 12.2 验证每日情报闭环

- Owner：Quality/Ops
- 前置：7.8、8.2–8.4
- 指令：从采集Fixture运行到Web Brief，检查15–25条、3–5重点、来源、栏目、待核实和覆盖提示。
- 产物：情报闭环验收记录。
- 验证：所有设计文档Daily Intelligence验收项通过。
- 禁止：不得只验证API而跳过用户页面。

### 12.3 验证融资闭环

- Owner：Quality/Ops
- 前置：6.1–6.11、8.5–8.9
- 指令：从融资Source运行到抽取、核验、审核、统计、详情和公司对比。
- 产物：融资闭环验收记录。
- 验证：待证实不进统计、同轮不重复、人民币可追溯、交易分卡、2–5家公司对比均通过。
- 禁止：不得用手工修改数据库绕过Pipeline。

### 12.4 验证审核时限

- Owner：Product/Quality
- 前置：7.1–7.3、8.10
- 指令：用接近真实数量的候选进行一次人工审核演练。
- 产物：审核可用性记录。
- 验证：必须处理不超过15条，Admin在30分钟内完成关键决定。
- 禁止：不得用明显简单的全高置信样本替代真实风险分布。

### 12.5 验证PDF与稳定入口

- Owner：Quality/Ops
- 前置：9.1–9.4
- 指令：测试09:00时PDF生成中、完成和失败三种状态。
- 产物：PDF验收记录。
- 验证：Web和飞书不被阻塞；同一入口最终可下载或显示可重试失败。
- 禁止：不得要求09:00必须已有PDF文件。

### 12.6 验证Hermes/飞书闭环

- Owner：Quality/Ops
- 前置：10.1–10.7
- 指令：验证09:00推送、今日事件、公司动态、融资查询、来源查询和最多5家公司对比。
- 产物：飞书Agent验收记录。
- 验证：答案含来源、状态、覆盖和深链；写入请求全部被拒绝。
- 禁止：不得跳过未授权用户和Prompt Injection测试。

### 12.7 执行来源失败演练

- Owner：Quality/Ops
- 前置：4.11、7.8
- 指令：让一个高优先级来源失败并运行完整早报流程。
- 产物：来源降级演练记录。
- 验证：09:00 Web仍发布；页面显示失败来源、最后成功和覆盖影响。
- 禁止：不得把失败伪装为无新内容。

### 12.8 执行Redis重启与任务重放演练

- Owner：Quality/Ops
- 前置：11.3
- 指令：在非生产演练环境重启Redis并根据PostgreSQL审计恢复未完成任务。
- 产物：队列恢复记录。
- 验证：业务事实不丢失；重放不重复Event、融资金额、Brief或递送。
- 禁止：不得在没有备份和隔离的生产环境首次演练。

### 12.9 执行模型失败与预算演练

- Owner：Quality/Ops
- 前置：5.7、11.10
- 指令：模拟模型超时、无效Schema、提供商不可用和预算超限。
- 产物：模型降级记录。
- 验证：系统重试、降级或转人工；不生成看似确定的虚假内容；关键发布继续。
- 禁止：不得无限重试或绕过成本限制。

### 12.10 执行安全与Secret检查

- Owner：Quality/Ops
- 前置：所有MVP功能
- 指令：扫描仓库、构建产物、日志、Sentry、API和导出文件中的Secret与私人数据。
- 产物：安全检查报告。
- 验证：无真实Secret、Cookie、Browser Profile、隐藏推理或不必要私人正文。
- 禁止：发现泄露后不得继续上线。

### 12.11 执行性能与关键窗口测试

- Owner：Quality/Ops
- 前置：所有MVP功能
- 指令：模拟典型候选量、31家公司查询、PDF任务和Hermes问答，观察08:00–09:00队列。
- 产物：容量与延迟报告。
- 验证：高优先级任务不被回溯/PDF挤占；09:00前完成Web发布；超时有降级。
- 禁止：不得为了性能关闭证据、审计或权限检查。

### 12.12 运行连续发布观察

- Owner：Quality/Ops
- 前置：12.2–12.11
- 指令：尽早开始每日自动运行，持续记录09:00发布、来源、审核、PDF和飞书状态。
- 产物：连续运行记录。
- 验证：上线验收时提供实际连续运行证据；未达到7天时明确已有天数和剩余验证，不得伪造。
- 禁止：不得把上线后四周质量指标误作8月7日阻塞条件。

### 12.13 完成上线清单

- Owner：Coordinator
- 前置：12.1–12.12
- 指令：逐项核对`design-document.md`第13节、成本、备份、回滚、监控、Owner和值班方式。
- 产物：签字式上线清单。
- 验证：每个验收项都有测试、截图、日志或人工记录；未通过项有明确风险接受者。
- 禁止：存在Secret泄露、数据库不可恢复、Hermes越权或正式融资统计错误时不得上线。

### 12.14 更新最终架构与文档

- Owner：Architecture/Data + Product/Docs
- 前置：12.13
- 指令：将实际完成范围、部署、数据流、故障处理和已知限制同步到`architecture.md`、`progress.md`及相关文档。
- 产物：MVP最终文档集。
- 验证：跨文档搜索不存在日期、范围、渠道、31家公司、Hermes权限和融资口径冲突。
- 禁止：不得把未完成任务写成已交付。

### 12.15 进行8月7日上线验收

- Owner：用户 + Coordinator
- 前置：12.13–12.14
- 指令：向用户演示每日情报、融资看板、审核、收件箱、PDF、飞书问答、来源健康、导出和故障降级。
- 产物：上线验收结论与后续清单。
- 验证：用户明确接受MVP，或列出必须修复的阻塞项。
- 禁止：不得用内部API演示替代最终用户体验。

---

## 4. 里程碑门

### Gate A：基础设施可开发

必须完成：

- 波次0；
- 波次1；
- 首个Migration与Schema测试；
- 基础认证和健康检查。

通过标准：

- 全新环境可启动；
- CI可阻止Lint、类型和测试错误；
- 无Secret进入仓库；
- `architecture.md`已更新。

### Gate B：首个端到端情报闭环

必须完成：

- 一个真实公开SourceAdapter；
- 收件箱至少支持URL与WorkBuddy契约；
- Event聚合、Entity、Topic、Assessment和Verifier；
- 审核、Brief快照和Web发布。

通过标准：

- 一条真实或固定来源能从采集进入Web Brief；
- 全链路有证据与运行审计；
- 重放不重复。

### Gate C：融资闭环

必须完成：

- 融资抽取、合并、FX、字段证据和状态；
- 正式统计与待证实隔离；
- 融资表、详情、统计和公司对比；
- 31家公司种子与回溯状态。

通过标准：

- 一条融资可从Source进入看板；
- 同轮多报道不重复；
- 统计口径正确；
- 2–5家公司可即时对比。

### Gate D：交付闭环

必须完成：

- 09:00 Web发布；
- PDF异步生成与稳定入口；
- Hermes飞书私聊推送和只读问答；
- DeliveryAttempt与失败重试。

通过标准：

- 任一递送下游失败不阻塞Web；
- Hermes无法执行写操作；
- 所有回答带来源与状态。

### Gate E：上线

必须完成：

- 波次12全部阻塞项；
- 备份恢复；
- Secret和权限检查；
- 成本与预算保护；
- 文档一致性更新。

通过标准：

- `design-document.md`第13节上线验收通过；
- 用户明确接受；
- 上线后四周质量验证开始计时。

## 5. 明确不在本计划中的内容

- Notes、划线、双向链接；
- 通用公司/技术/赛道Timeline；
- 完整全局搜索和语义搜索；
- 投资机构关系网络；
- 保存或分享公司对比组合；
- 复杂估值分析；
- 所有音视频全文转录；
- X、LinkedIn、中国App全平台自动扫描；
- 飞书群聊、部门推广、写操作和复杂权限；
- 公开访问、PWA和原生App；
- 最终UI视觉风格；
- 自主执行外部行动的研究Agent。

上述事项默认进入MVP后Backlog，不得由执行Agent自行加入8月7日范围。
