# WorkBuddy 微信核验状态纠偏指令

> 修正对象：`workbuddy-domestic-observation-list.md`、`workbuddy-verification-report.md`  
> 本轮只修正核验事实、启用状态和数量，不扩充名单，不调整六模块框架，不执行正式搜索，不写入飞书。

## 已确认的事实

用户明确表示：**没有在微信 App 内逐个检查或确认54个公众号存在且活跃。**

因此，当前文件中所有“用户已确认”“微信内已确认”“54个账号确认存在且活跃”等表述均无事实依据，必须撤回。不得根据用户曾经打开微信、WorkBuddy 或系统设置推断其已完成核验。

## 必须修改

1. 删除核验报告中“用户在微信 App 内确认54个账号存在且活跃”的表述。
2. 将核验方法改为：本轮只完成公开网页核验和名单整理，尚未完成用户微信 App 内人工核验。
3. 对没有微信内证据的对象，删除“微信内已确认（ID待回填）”“微信内已确认（主体待回填）”等文字。
4. 此类对象的准确名称、认证主体和核验状态统一改为实际状态，例如“待微信内核验”或“部分核验（仅完成公开网页核验）”。
5. 没有满足“账号准确名称 + 认证主体 + 当前有效入口 + 近期活跃度”四项要求的公众号，不得设置为“启用”。启用状态改为“待核验”或“暂不启用”。
6. P0/P1/P2 观察优先级可以保留。P0 表示值得重点核验和监控，不等于已经获准启用。
7. 重新统计以下数量，并保证报告、正式清单和摘要一致：
   - P0 观察对象数量。
   - 名称级已核验数量。
   - 部分核验数量。
   - 微信 App 内完整核验数量。
   - 当前正式启用数量。
8. 严格完整核验数量当前应为0；如果没有其他完整证据，正式启用数量也应为0。
9. 删除“可启用26个”的错误计算。公司官号已经包含在P0信源总数中，不得重复相加。
10. 修正名称级已核验名单遗漏“优必选”以及“23个名单实际只列22个”等列举错误。

## 用户补充信源的处理

用户补充的信源可以继续保留在正式观察清单，但必须区分：

- 已列入观察清单。
- 已完成公开网页初步核验。
- 已完成微信 App 内身份核验。
- 已正式启用。

在没有微信 App 内证据时，不得仅因为用户提供了名称，就写成“用户已确认”或“已启用”。

`疯狂BP`、`投行并购圈`、`张通社`等参考型信源建议先保留P1或P0待评估状态，不要仅凭账号名称直接成为每日正式信源。最终优先级若暂不调整，至少必须保持“暂不启用”。

`Founder Park`的主体与产品定位、`财联社 AI Daily`和`科创板日报`的关系，如果没有可靠依据，应写“待核验”，不得作为已确认事实。

## 修正后的报告应明确写出

1. 用户微信 App 内人工核验：0个。
2. 严格完整核验：0个。
3. 当前正式启用：0个；或者仅列出确实同时具备四项证据的对象，并逐项给出证据。
4. 现有94个信源是观察候选池，不等于94个已启用来源。
5. 下一步建议只核验首批5至10个最高价值公众号，不要求用户一次检查全部54个。

## 不得修改

- 不改变65家公司、94个信源和35个赛道的整体框架，除非修正重复或明显事实错误。
- 不新增观察对象。
- 不重写查询矩阵。
- 不执行正式新闻搜索。
- 不写入飞书。

## 回传要求

完成后回传：

1. 修正后的 `workbuddy-domestic-observation-list.md`。
2. 修正后的 `workbuddy-verification-report.md`。
3. 不超过5行的摘要，明确写出人工核验数、严格完整核验数和正式启用数。
