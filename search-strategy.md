# 融资与资本动态搜索策略

> 版本：v6
> 确认日期：2026-08-07
> 当前范围：搜索 MVP 只覆盖国内融资与广义资本动态
> 执行角色：Codex 项目内 Research Operations

## 1. 当前职责和渠道

- 国内主渠道是 `domestic-website-search-list.md` 中的公开媒体网站。
- Research Operations 使用站内公开搜索/列表、Agent Reach 当前可用的网页搜索与阅读后端、Jina Reader 和原站公开 HTML 执行只读搜索。
- 执行前运行 `agent-reach doctor --json`，记录能力状态；不得输出 Cookie、Token 或登录态信息。
- WorkBuddy 当前暂停，既有 C01/C02 文件与导入器仅作历史兼容。
- 海外搜索当前暂停；未来恢复后也由 Research Operations 执行，并另建海外网站清单和任务单。
- OpenAI 不再承担地域发现主责，继续用于候选后的相关性判断、事实抽取、冲突核验和摘要。
- `domestic-website-search-list.md` 仍是国内网站主覆盖清单；微信公众号公开搜索可作为补充发现渠道，但不能替代清单网站逐站覆盖。公众号补充必须记录公众号名称、搜索结果显示时间、文章 URL 和证据状态；签名链接可能失效时必须标记 URL 稳定性风险，不得把搜索摘要冒充已核验正文。
- wechat-mp-rss 尚未验证，只在产生可访问条目后作为额外补充；RSS 为空、延迟或受频控不影响网站主任务完成状态。

## 2. 四层数据边界

1. **原始线索（raw）**：以召回和覆盖审计为主，允许字段不全、正文日期不明、正文不可访问、行业弱相关和广义资本事件。非目标日或日期无法判定的命中可保留在 raw 审计记录中，但不因此获得当日收录资格。
2. **清洗后待核验事件（clean）**：只保留可确认属于用户指定业务日的内容，再执行确定性清洗、URL 规范化、重复处理、事件组合并和字段提取；一行代表一个建议事件，行下保留全部来源、逐字段证据、冲突和机器建议值。
3. **研究候选**：用户核验事件边界、字段、主来源和收录决定后，经业务日、来源、Schema、事实和安全 QA 转换。
4. **正式数据**：人工在飞书审核批准后形成融资事件；飞书多维表格仍是唯一正式数据源。

原始线索不得直接写飞书、批准、生成周报或发布。搜索 Agent 不能读取或修改内部战投备注。

## 3. 搜索范围

当前搜索 MVP 收录融资与广义资本动态：

- 融资、获投、投资、战略投资、股权投资。
- 领投、参投、跟投、增资、入股。
- 并购、收购、合资。
- IPO、上市、递表及其他明确资本市场进展。
- 已完成、正在推进、已启动、寻求或开放融资。

“国内搜索”限定的是当前发现渠道为 `domestic-website-search-list.md` 中的国内媒体网站，不限定交易主体的注册地、总部或国别。正文日期符合目标业务日、事件属于上述融资或广义资本范围且来源满足规则时，即使公司、买方、卖方或其他交易主体位于境外，也可进入 clean 和后续人工核验；不得仅因主体为境外公司自动排除。地域相关性可以作为后续分类字段，但不是本阶段的准入门禁。

当前不主动搜索技术发布、新产品、量产、订单、交付或其他非资本公司动态。既有相关领域模型和三板块产品能力暂不删除；整个产品 MVP 是否收缩，另行对齐。

## 4. 网站高召回方法

1. 对清单内每家媒体逐站建立覆盖记录，不以抽样代替清单覆盖。
2. 优先使用网站页面上可见的站内搜索框，先输入宽词 `融资`。提交后必须等待异步结果完成加载；若立即显示空结果、搜索失败或页面无变化，等待后再检查一次，并至少重试一轮，确认最终页面状态后才能转用其他入口。不得把尚未加载完成的首屏当作零结果。
3. 再分别宽搜 `获投`、`并购`、`收购`、`IPO`，查询中不加日期。必要时补充投资、领投、参投、增资、入股、合资、上市、递表等词；不展开公司×事件词全量矩阵。
4. 同时检查普通文章、快讯、融资快报、标签、栏目或列表等不同内容入口。36氪必须覆盖普通“文章”和快讯/融资快报；投资界必须直接检查“融资”和“并购”栏目，并结合“最新”等时间入口；存在类似专门资本栏目的网站均不得只依赖通用搜索框。
5. 站内搜索或栏目完成后，必要时使用限定域名网页搜索补漏；外部索引的空结果不能推翻原站可见内容。
6. 持续翻页至明确越过目标日，或连续一整页没有目标日结果。搜索排序不严格按时间时，必须继续检查到足以说明停止范围，不能因前几条无关就停止。
7. 记录站点、渠道、查询词、等待/重试情况、翻页或文章范围、停止原因、访问限制和可计数检查量。
8. 不设数量上限，不把达到 20 条或任何数量作为停止条件，也不得承诺绝对零遗漏。

### 4.1 日期读取与召回纠错

- 原站正文肉眼可见的发布日期优先于 Reader、搜索引擎摘要和自动提取元数据。36氪等动态页面若 Reader 抽取时间与页面可见时间不一致，必须用浏览器可见页面或原始 DOM 复核；不得因 Reader 把抓取时间误当发布时间而排除线索。
- 只有来源明确给出时区的时间戳才可换算至 `Asia/Shanghai`。只有日期而没有时区时，不得自行假设 UTC 后跨日换算。
- raw 中公司名或日期未提取成功的融资标题不得静默消失。每条排除都必须有可复查的业务日或内容证据；人工发现漏报后，以追加纠错记录或审计说明保留原错误与修正依据，不静默覆盖历史。
- 人工提供的清单外公开来源可作为补充线索加入 raw，并明确标记“清单外人工补充”；它不计入清单网站覆盖完成度，也不能用来掩盖清单内站点漏搜。

### 4.2 2026-08-05 漏报复盘

- 若创科技：36氪页面实际显示 2026-08-05，但自动 Reader 时间解析错误，旧 raw 将其误判为 8 月 6 日。核心问题是过度信任自动元数据，没有以正文可见日期完成最终门禁。
- 无界量子（无界开物）：量子客公众号在目标日发布融资消息，但旧策略排除了公众号公开搜索，导致补充渠道漏报。修正为公众号可作补充发现，但必须保持证据状态和 URL 稳定性标记。
- 星际荣耀：搜狐覆盖过早停止在噪声或空结果，且未充分等待站内动态搜索；用户另在搜狐、亿欧发现线索。修正为站内搜索等待、重试、翻页和专栏检查均需留下审计记录，不能把一次搜索失败视为当日无消息。
- 投资界：通用搜索不能替代其融资、并购等专门栏目。后续逐日搜索必须直接覆盖这些栏目并翻页至越过目标日。

## 5. 业务日门禁与原始线索收录

- 用户指定搜索哪一个业务日，clean 清洗结果和当日交付就只能保留可确认属于该日的内容；业务日固定按 `Asia/Shanghai` 判定。
- 搜索或列表显示“昨天”、`N小时前`、`1天前` 等相对时间时，必须结合本次实际执行时间换算为上海时区的日期，记录换算依据，并打开正文核对页面可见发布日期。
- 正文可见发布日期与搜索/列表时间冲突时，以正文可见日期作为业务日归属依据；不得把其他日期改写成目标日。
- 正文明确属于其他日期的条目必须从 clean 和当日交付中排除，不将这类普通跨日误收交给用户人工判断；可仅在 raw 覆盖审计中保留原始时间、正文时间、URL 和“非目标日排除”结论。
- 相对时间跨日边界、正文无日期或经过实际执行时间换算与正文核验后仍无法可靠判定时，标记“日期不确定”并询问用户；在用户确认前不得进入 clean 或当日交付。
- 搜索/列表时间、正文发布时间、事件时间分别记录，不得互相改写。
- 正文日期冲突：raw 中保留并标记 `DATE_CONFLICT`；若正文日期已明确为非目标日，clean 必须排除。
- 正文无明确日期：标记 `DATE_UNCERTAIN` 和 `NEEDS_HUMAN`，询问用户后再决定业务日归属；其他核心字段不全可单独标记 `NEEDS_HUMAN`，不影响已确认的业务日。
- 正文不可访问但有搜索/列表时间和文章 URL：raw 中保留并标记 `BODY_UNAVAILABLE`；无法可靠确认发布日期时同时标记 `DATE_UNCERTAIN`，不得默认进入 clean。
- 标题、摘要和正文冲突：保留并标记 `CONTENT_MISMATCH`。
- 广义资本边界事件：标记 `BROAD_CAPITAL_EVENT`。
- 公司全名、金额、轮次或投资方缺失时写“未披露/未明确”，不得猜测。

融资状态必须区分：

- `已完成`：来源明确称完成融资、获得投资或资本动作已经发生。
- `进行中`：正在融资、正在推进或已启动融资。
- `融资意向`：寻求融资、开放融资或计划引入投资。

不得把“进行中”或“融资意向”改写成“已完成”。

## 6. 来源与访问边界

- 每条原始线索保留实际文章或公告 URL；搜索摘要不能冒充正文事实。
- 公司、投资方、FA、监管、政府及可靠财经、科技、产业媒体均可提供线索。
- 转载可保留，但必须如实记录当前页面的发布主体和证据层级。
- 不绕过登录、验证码、付费墙、证书错误或访问控制，不使用个人 Cookie 或未授权账号。
- 不保存完整正文、完整模型响应、密钥或平台内部数据。

## 7. URL 与事件重复

- 完全相同 URL 和安全规范化后相同的 URL 自动合并，并保留全部命中站点、栏目、查询词和渠道。
- URL 规范化只删除已确认无业务意义的 `utm_*` 等追踪参数、fragment、尾部 `/` 及已验证等价的移动/桌面形式；未知查询参数不得擅自删除，标题相同不能证明 URL 相同。
- 搜索阶段不做事件合并。搜索完成后，同一公司且至少存在轮次、金额、投资方、同一公告或核心事实等一个强匹配信号，并且不存在轮次、状态、主体等明确冲突时，合并为一个待核验事件行，行下保留全部来源。
- 无法确定是否为同一事件时保持为不同行，并用 `POSSIBLE_DUPLICATE` 相互关联；宁可多留，不得自动误合并。
- 主来源优先公司、投资方或监管正式公告，其次是采访当事方的原创报道，再次是信息完整且正文可访问的首发媒体；机器只提出建议，用户最终确认。
- 允许多个来源共同补全事件，但每个字段必须保留自己的来源和必要证据片段；来源冲突不得自行裁决，主来源也不代表所有事实都必须由其支持。
- 与飞书历史正式事件一致的新转载标记 `HISTORICAL_DUPLICATE`、关联原事件且不创建新候选。披露新增金额、投资方或交易进展时标记历史事件补充，由用户决定更新原事件或创建后续事件。
- 相同事件不得重复形成正式融资事件或重复计入 WVID。

自动步骤只能确定性清洗、生成事件行和提供建议；事件是否相同、主来源、最终字段以及是否进入候选，均由用户核验。

## 8. 清洗分类、提取与审计

清洗不执行不可恢复的自动删除，输出分为：

- `KEEP`：明确的资本动态。
- `NEEDS_HUMAN`：边界、字段缺失、日期或事实冲突。
- `SUGGEST_REJECT`：明显不属于资本事件，但仍保留在覆盖审计中并允许用户恢复。
- `ACCESS_FAILED`：正文无法访问，但标题、列表日期或 URL 仍可能构成有效线索。

“不执行不可恢复的自动删除”只表示 raw 审计证据必须保留，不表示所有 raw 命中都可进入 clean。非目标日条目在 raw 中标记 `SUGGEST_REJECT` 和排除原因；日期不确定条目在 raw 中标记 `DATE_UNCERTAIN` / `NEEDS_HUMAN` 并询问用户。两者均不得在未满足业务日门禁时进入 clean。

每个事件组先由机器提取建议值，再由用户核验。MVP 必提字段包括公司原始名称和标准名称建议、资本事件类型、融资状态、轮次、金额与币种、投资方、事件日期、各来源及发布日期、逐字段证据、主来源建议、冲突、缺失项和人工待办。有披露时再提取领投、跟投、财务顾问、资金用途、公司简介、估值、累计融资额、并购双方、IPO 阶段和交易进展。不得猜测缺失字段，也不在此阶段生成投资判断、市场规模、竞争格局或完整公司档案。

人工核验分开保存机器建议值、人工确认值和逐字段证据，并记录修改人、时间、前后值及原因。机器重跑不得覆盖人工确认值，新来源只能形成更新建议或冲突提示。

日期字段必须分开：`searchTargetDate` 表示搜索目标日，`firstSeenAt` 表示首次发现时间，`sourcePublishedAt` 属于各来源，`eventDate` 表示实际发生或宣布日期，`approvedAt` 表示人工批准时间，`weeklyIssue` 表示最终周报归属。迟到批准的候选进入批准后的下一期周报，不改写真实事件或来源日期，也不自动重写已发布历史周报。

正式事件的后续变化分为：`ENRICHMENT`（补充未披露信息）、`CORRECTION`（纠正错误事实）和 `FOLLOW_UP_EVENT`（新的交易进展或事件）。不得静默覆盖已发布事实；必须保留来源和审计记录，是否单独通知由用户决定。

## 9. 交付格式与人工门禁

机器 raw 审计 JSON 至少包含：

- 清单媒体覆盖表和 Agent Reach 能力状态。
- 原始命中、实际检查量、目标日线索数和唯一 URL 数。
- 标题、URL、来源、列表时间、正文时间、访问状态和融资状态。
- 缺失字段、异常标记、疑似重复事件组。
- 每个渠道的检查范围、停止原因和访问限制。
- 非目标日和日期不确定命中的完整审计信息；raw 不直接作为人工审核视图。

每日 clean JSON 必须包含目标业务日、Asia/Shanghai 实际执行时间、相对时间换算依据、正文日期核验结果、非目标日排除数量和日期不确定排除数量。clean 中不得包含已确认为其他日期或仍为 `DATE_UNCERTAIN` 的来源与事件。

人工审核 Markdown 只能展示正文发布日期已确认属于目标业务日的来源和 clean 事件：

- 非目标日命中只在 raw JSON 中保留完整审计信息；Markdown 只报告排除数量，不得列出其标题、URL、R 编号，也不得要求用户逐条审核。
- `DATE_UNCERTAIN` 不得混入当日人工审核表。Agent 应在 Markdown 外用最小问题单独询问用户；确认属于目标日前不得进入 clean 或人工审核表。
- 人工审核任务只包括目标日事件的事件合并、主来源选择、广义资本边界和关键字段冲突，不把已经确定的业务日排除问题转交用户。

每个搜索日同时交付机器可处理的 JSON 和从同一数据生成的人工审核 Markdown。原始搜索 JSON 保持不可变；清洗结果使用独立 JSON；Markdown 只是审核视图，不是正式数据源。不得把完整网页正文或完整模型响应写入 Git。

每日处理顺序固定为：Research Operations 搜索 → raw 审计 → 业务日门禁 → 对日期合格内容执行确定性清洗、URL 去重、事件组合并与字段提取 → 单独询问日期不确定项 → 用户审核 clean 事件 → 正式候选转换。未经用户对具体批次和真实写入的明确授权，不调用飞书写入。

候选进入飞书后不做每日催促。每周五 17:00（Asia/Shanghai）只检查并私聊汇总一次当周仍待处理的候选；无待审核项不发送，同一自然周补跑不得重复发送。未审核候选不自动批准或进入周报。

## 10. 前两周召回率校准

- 用户对相同业务日执行独立人工搜索。
- 合并 Agent 与人工结果，经用户确认并完成事件级去重后形成联合人工基准集。
- Agent 独立发现且经用户确认有效的事件也进入基准集，不作为错误处理。
- `Agent 召回率 = Agent 找到且经人工确认的唯一有效事件数 ÷ 联合人工基准集中的唯一有效事件总数`。
- 按唯一事件而不是 URL 计数；跨媒体同事件只算一次。无关或虚假线索、`SUGGEST_REJECT`、尚未确认的 `NEEDS_HUMAN`、无法验证的访问失败和历史重复不进入分子或分母，后续核验有效时再补计。
- 逐日记录 Agent 命中、人工独立命中、共同命中、Agent 独立有效命中和最终唯一事件数，连续运行两个自然周后再设目标值。

## 11. 重要性与周报准入

- 重要性由机器给出 1–5 分建议、用户最终确认；金额不是唯一依据，早期小额但技术、团队或产业影响显著的事件也可以获得较高分。
- 5 分代表行业重大交易；4 分代表重要公司或显著影响细分格局的融资；3 分代表与目标行业明确相关且值得纳入周报；1–2 分为较弱或边缘相关事件。
- 重要性与置信度分开计算。周报只收录人工批准且重要性不低于 3 的正式事件，默认按重要性降序，再按事件日期和稳定事件 ID 排序。

## 12. 定时化条件

Codex 定时任务可用于当前方法的试验，但不能因为“能够按时触发”就视为满足生产 SLA。进入无人值守生产前必须同时满足：

1. 运行环境归公司所有并可由至少两名管理员接管。
2. 不依赖个人电脑、交互式会话、个人浏览器登录态或个人 Cookie。
3. Agent Reach 与所需网页后端可在该环境稳定安装、调用和升级。
4. 每次运行有固定业务日期、幂等运行 ID、覆盖审计、失败状态和安全日志。
5. 访问失败可补跑，补跑不重复创建候选或正式事件。
6. 外部输入经过长度、URL、安全和运行时 Schema 校验。

在这些条件完成前，搜索属于人工触发或实验性定时任务；网站与飞书现有数据流程不得依赖它按时成功。
