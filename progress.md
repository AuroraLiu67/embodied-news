## 2026-07-30

- 提取并冻结 MVP 系统架构，新增 `architecture.md`。
- 明确研究发现、领域处理、飞书数据与审核、公开发布、自动化及安全边界。
- 确认飞书多维表格为唯一正式数据源，GitHub Actions负责自动化，GitHub Pages负责静态网站托管。
- 以 `architecture.md` 为最高参考完成全部 Markdown 一致性检查。
- 修正 WorkBuddy 不可用时的降级边界，保持 OpenAI 主责海外研究。
- 为实施计划补充公司公开档案任务；当前共 47 个小任务，全部包含验证测试。
- 新增仓库级 `AGENTS.md`，强制所有开发者在编码前阅读 `architecture.md` 和 `PRD.md`，并在完成重大功能后更新 `progress.md`。
- 新增 `agent-coordination.md`，统一 Codex、Claude Code、WorkBuddy、OpenAI、GitHub Actions、辅助开发 Agent 和人工团队的职责、任务分配、文件所有权与交接流程。

## 2026-07-31

### A01 初始化项目

- 完成 Next.js App Router、TypeScript strict mode、pnpm、ESLint 和 Vitest 工程初始化。
- 建立 `app`、`components`、`lib`、`cli`、`scripts` 和 `tests` 目录。
- 实现仅用于验证工程链路的最小静态首页，没有接入飞书、OpenAI、WorkBuddy 或任何业务逻辑。
- 将 Next.js 配置为静态导出，构建产物写入可由 GitHub Pages 托管的 `out/`。
- 锁定经过验证的兼容版本：Next.js 16.2.12、React 19.2.8、TypeScript 6.0.3、ESLint 9.39.5、Vitest 4.1.10 和 pnpm 11.9.0。
- 增加依赖、构建产物、本地缓存、环境文件、密钥文件和日志的 Git 忽略规则。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过。
- `pnpm test` 通过，共 1 个测试文件、1 个测试。
- `pnpm build` 通过，成功静态生成 `/` 与 `/_not-found`。
- 本地静态预览返回 HTTP 200，首页 CSS 和 JavaScript 资源正常加载。
- 用户于 2026-07-31 根据终端输出确认 A01 测试通过。
- 仓库扫描未发现 `.env` 文件或常见密钥格式。

**已知限制与下一步：**

- 当前首页只是工程连通性页面，不代表最终视觉设计。
- 当前测试只覆盖 A01 最小站点元数据；领域契约和业务测试尚未开始。
- A02 已于 2026-07-31 完成并通过用户验收。

### A02 建立领域类型

- 建立候选、融资事件、公司、日报、观察清单、内部判断和自动化任务七类领域对象。
- 建立金额字符串、日期、URL、来源、置信度、审核状态和发布状态等共享值类型与固定枚举。
- 将公开融资事件、公开公司、公开日报和公开来源 DTO 独立声明，不从内部对象自动继承字段。
- 使用互斥联合类型约束内部判断只能关联公司或融资事件之一。
- 新增合法脱敏 Fixture，覆盖七类内部领域对象与三类公开 DTO。
- 新增编译期契约测试，保证公开 DTO 不具备内部判断、候选审核、原始片段和内部证据字段。
- 新增运行时测试，检查合法 Fixture 的关键不变量和公开对象的敏感字段缺失。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 2 个测试文件、3 个测试。
- `pnpm build` 回归通过，静态首页继续正常生成。
- A02 新增文件敏感值扫描未发现问题。
- 用户于 2026-07-31 确认 A02 测试通过。

**已知限制与下一步：**

- A02 只冻结 TypeScript 领域契约；外部未知数据尚不能依靠 TypeScript 自动变为可信数据。
- 完整 URL、文本、金额、日期和未知字段运行时校验属于 A03。
- A03 已于 2026-07-31 完成并通过用户验收。

### A03 建立 Zod Schema

- 引入并精确锁定 Zod 4.4.3。
- 为 WorkBuddy 候选输入、OpenAI 结构化输出、七类飞书记录、公开网站导出和稳定错误响应建立严格 Schema。
- 为 A02 的七类领域对象建立运行时 Schema，使合法脱敏 Fixture 可以从编译期契约进入运行时信任边界。
- 统一限制稳定 ID、标题、摘要、正文片段、数组、金额、币种、日期和时间。
- 金额使用非负十进制字符串，禁止浮点输入；金额披露状态与金额、币种必须保持一致。
- 所有对象使用严格模式，未知字段不被静默剥离或接受。
- URL 仅允许 HTTP/HTTPS，拒绝本机、私网、链路本地、云元数据、保留地址、带凭据 URL 和非 Web 协议。
- 公开导出 Schema 逐字段声明白名单，不从完整内部记录生成 Schema。
- 测试发现并修复正式融资事件缺少金额披露一致性校验的问题，保留原测试断言。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 4 个测试文件、32 个测试。
- 五类边界 Schema 均覆盖合法、缺字段、错误类型、超长输入和未知字段。
- 危险 URL 与正常公开 HTTPS URL 测试通过。
- A02 七类合法领域 Fixture 均通过运行时 Schema。
- `pnpm build` 回归通过，静态首页继续正常生成。
- 环境文件和敏感值扫描未发现问题。
- 用户于 2026-07-31 确认 A03 测试通过。

**已知限制与下一步：**

- 当前 URL Schema 负责单个输入 URL 的第一层校验；DNS 解析、重定向链和响应体限制属于 C04。
- 当前飞书记录使用领域层记录包络；真实表 ID、字段 ID 和字段类型映射将在 B01、B04 冻结。
- 当前 Schema 不执行 HTML 正文清洗，安全正文获取属于 C04。
- A04 已于 2026-07-31 完成并通过用户验收。

### A04 建立环境配置

- 建立 `local`、`test`、`production` 三种明确运行环境。
- 建立 `mock` 与 `live` 两种 Provider 模式；本地默认使用 Mock，不依赖真实账号和密钥。
- 测试环境强制使用 Mock Provider，避免自动化测试误连真实 OpenAI 或飞书。
- 生产环境强制使用 Live Provider，并要求飞书应用、多维表格、通知和 OpenAI 配置完整。
- 服务端配置与公开配置使用不同加载入口；公开配置只包含 GitHub Pages 站点 base path。
- 配置错误只报告缺失字段和稳定说明，不输出已经提供的密钥值。
- 验证 IANA 时区和 GitHub Pages base path 格式。
- `next.config.ts` 接入经过校验的公开 base path，支持项目路径静态构建。
- 新增 `.env.example`，只包含变量名、模式说明和空值，不包含真实凭据。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 5 个测试文件、41 个测试。
- 覆盖本地默认配置、完整生产配置、生产缺失配置、测试环境 Live 禁止、生产环境 Mock 禁止、非法时区和 base path。
- 使用专门的假飞书及 OpenAI 密钥完成生产模式静态构建。
- 静态产物扫描确认所有假服务端密钥均未进入 `out/`。
- GitHub Pages 项目 base path 正确写入 CSS 和 JavaScript 资源地址。
- 仓库未出现真实 `.env`、`.env.local`、`.env.test` 或 `.env.production` 文件。
- 用户于 2026-07-31 确认 A04 测试通过。

**已知限制与下一步：**

- `.env.example` 只是配置清单，生产值仍必须由公司管理员放入 GitHub Secrets。
- A04 不验证真实账号和权限；飞书真实连接从 B02、B03 开始，OpenAI 真实连接从 C03 开始。
- GitHub Pages 完整非根路径页面验收属于 E09。
- A05 已于 2026-07-31 完成并通过用户验收。

### A05 建立 Fixture 和 Mock

- 建立国内、海外、金额未披露、多来源、来源冲突、重复和低置信度七类脱敏业务场景。
- 所有 Fixture 使用虚构或公开示例标识，不包含真实内部战投信息、账号或密钥。
- 为 OpenAI、WorkBuddy、飞书和通知服务建立测试 Mock。
- 四类 Mock 均支持成功、空结果、限流、超时和非法输出五种场景。
- 限流和超时使用立即返回的稳定测试错误，不进行真实等待。
- 非法输出场景故意返回错误形状，供后续 Provider 验证 Schema 失败路径。
- Mock 飞书记录写入历史、Mock 通知发送历史可供后续集成测试断言副作用。
- 断网测试替换全局 `fetch`，确认全部 Mock 在本地和 CI 中不访问外部网络。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 6 个测试文件、49 个测试。
- 七类业务 Fixture 全部通过对应领域 Schema。
- OpenAI、WorkBuddy 和飞书成功输出全部通过对应边界 Schema。
- 空结果、限流、超时和非法输出场景行为符合预期。
- `pnpm build` 回归通过，静态首页继续正常生成。
- Mock 网络客户端静态扫描未发现网络调用。
- Fixture 与 Mock 敏感值扫描未发现问题。
- 用户于 2026-07-31 确认 A05 测试通过。

**已知限制与下一步：**

- A05 Mock 是测试替身，不定义真实供应商 SDK 的最终接口。
- 真实飞书分页、批量写入、权限和字段映射行为将在 B03 至 B05 实现。
- OpenAI 重试、预算和响应处理将在 C03 实现。
- 阶段 A 原始五项已完成；B01 九张多维表格设计也已完成并通过用户验收。
- 产品范围在 B01 评审中扩展到技术、产品和商业化动态，因此新增的领域契约补充工作单独列为 A06，尚未实现。

### B01 创建飞书多维表格设计（修订版）

- 将飞书唯一正式数据源设计冻结为九张表：研究候选、融资事件、公司动态、信息来源、公司、日报、观察清单、内部战投备注和自动化任务。
- 观察清单明确支持重点公司、重点赛道、重点公众号、关键词、投资机构和 FA；来源可信等级与搜索优先级分开记录。
- 日报固定为“今日融资”“技术与产品”“商业化进展”三个板块。
- 融资事件与公司动态增加 1–5 重要性评分和评分理由，公开视图默认按重要性降序。
- 每条融资或公司动态必须关联至少一个可访问的原始信息来源；来源随对应条目展示，不建立独立来源板块。
- 公司与融资事件、公司动态建立一对多关联，支持同一公司长期融资历史和动态时间线。
- 为九张表生成稳定的 table ID、field ID 配置键清单；仓库未保存真实飞书 ID 或凭据。
- 增加五条脱敏设计记录，验证公司、融资事件、公司动态、信息来源和日报之间的业务 ID 关联。
- 将受影响的产品、规格、技术栈、架构和实施计划统一为“具身智能公司动态雷达”，并新增待实施任务 A06。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 7 个测试文件、61 个测试。
- 自动测试验证九张表、必填字段、关联目标、视图字段、字段 ID 映射、三个日报板块、逐条原始来源、重点公司与重点赛道以及重要性降序。
- `pnpm build` 回归通过，静态首页继续正常生成。
- 格式和旧版“七张表”残留检查通过。
- 用户于 2026-07-31 确认 B01 修订版测试通过。

**已知限制与下一步：**

- B01 只冻结本地设计，尚未在真实公司飞书租户创建应用或多维表格。
- A02–A05 的既有领域代码仍以融资为主；A06 将增加公司动态、独立来源、重要性和三板块日报运行时契约。
- B02 涉及真实飞书公司账号、权限和管理员交接，需要在用户明确授权并准备公司租户后执行。
- 在 A06 或 B02 获得明确授权前，不开始下一项实现。

### B02 创建公司飞书自建应用（带延期验收项）

- 在公司飞书租户创建自建应用“具身智能公司动态雷达”，启用机器人能力。
- 仅开通多维表格读写和机器人文本消息所需权限。
- 创建并配置正式多维表格，应用以“可编辑”文档应用身份访问；正式数据仍只保存在飞书多维表格。
- 本地 `.env.local` 已配置应用身份和正式 Base Token，文件权限为仅当前用户可读写，并由 Git 忽略。
- 真实凭据、访问令牌和记录内容均未写入仓库、测试输出或静态产物。

**验证结果：**

- 应用身份鉴权成功。
- 正式 Base 数据表列表读取成功。
- 在“自动化任务”表完成测试记录创建、回读、删除和删除后缺失检查，未留下测试记录。
- 对未添加文档应用的测试 Base 执行了表结构与记录读取隔离检查；应用身份仍能成功读取，因此“未授权 Base 不可访问”尚未通过。
- 用户于 2026-07-31 接受将权限隔离和管理员交接延期到上线前安全验收，并允许在明确限制下继续后续开发。

**已知限制与下一步：**

- B03 及后续飞书客户端必须固定使用配置中的正式 Base Token，不得接受命令行或外部输入传入任意 Base Token。
- 自动化测试继续使用 Mock 飞书，不访问真实生产 Base。
- 上线前必须使用不同所有者、未共享且未添加应用的 Base 重新执行读取与写入隔离测试。
- 当前应用仅由用户本人管理；生产交接前必须增加至少一名公司管理员并完成凭据与管理权限交接测试。
- B02 按“核心连接验收通过、两项安全/交接测试延期”记录，延期项不得被误报为通过。

### A06 扩展公司动态与日报领域契约

- 新增技术、产品和商业化三类 `CompanyDevelopment` 领域对象及运行时 Schema。
- 新增独立 `InformationSource` 领域对象；融资事件和公司动态通过稳定 `sourceIds` 关联正式来源。
- 为融资事件和公司动态增加 1–5 重要性评分与评分理由。
- 可公开且处于可发布状态的融资事件或公司动态如果没有原始来源，运行时校验会失败。
- 日报固定为今日融资、技术与产品、商业化进展三个板块，并分别保存融资事件与公司动态引用。
- 新增板块排序规则：默认按重要性降序，显式人工顺序优先。
- 公开 DTO 新增公司动态，并要求日报每条内容内联自己的来源；公开导出不存在独立来源板块。
- 飞书记录运行时边界补充“公司动态”和“信息来源”，并将候选表名称统一为“研究候选”。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 7 个测试文件、65 个测试。
- 三类公司动态合法样本全部通过，非法类别被拒绝。
- 无来源的可公开融资事件和公司动态被拒绝。
- 重要性低于 1、高于 5 或非整数时被拒绝。
- 日报默认重要性排序和人工顺序覆盖均通过测试。
- 公开日报三个板块的每条内容均携带来源，且不存在独立来源集合。
- `pnpm build` 通过，静态首页和 404 页面成功生成。
- 用户于 2026-07-31 确认 A06 测试通过。

**已知限制与下一步：**

- A06 只冻结领域和运行时契约，不实现飞书 API、字段映射或 Repository。
- 独立信息来源与融资事件、公司动态的真实飞书字段映射将在 B04 验证。
- 日报生成和人工排序的持久化逻辑分别属于 D03 与后续飞书 Repository。
- 下一项可执行任务为 B03 飞书客户端；仍须固定正式 Base Token，并继续使用 Mock 完成自动化测试。

### B03 实现飞书客户端

- 精确锁定飞书官方 Node.js SDK `@larksuiteoapi/node-sdk@1.70.0`，并将其必要的 `protobufjs` 构建脚本加入仓库依赖构建白名单。
- 在官方 SDK 外建立项目级窄接口，使后续 Repository 不直接依赖供应商 SDK 类型。
- 客户端构造时必须注入唯一 Base Token；分页与批量方法不接受临时 Base Token，落实 B02 的固定正式 Base 补偿控制。
- 实现多页记录读取，并检测缺失或重复分页标记，防止异常响应造成死循环。
- 实现批量创建和批量更新，单次最多 500 条，超出时按顺序自动分批。
- 实现有上限的指数退避；仅限流、超时、HTTP 429 和临时服务端/网络错误可重试。
- 将鉴权、权限、限流、资源不存在、非法请求、异常响应、网络和一般 API 错误映射为稳定项目错误码。
- 客户端日志只记录操作、结果、尝试次数和稳定错误码，不记录 App Secret、访问令牌或飞书原始错误信息。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 8 个测试文件、73 个测试。
- Mock 测试覆盖完整分页、异常分页保护、501 条记录拆分、限流重试、临时网络重试、超时重试上限、权限错误不重试和输入预校验。
- 脱敏测试确认 App Secret 与访问令牌哨兵值不会进入错误或日志。
- `pnpm build` 通过，静态首页和 404 页面成功生成。
- 全部自动化测试均使用注入的 Mock Transport，没有访问或修改真实飞书。
- 用户于 2026-07-31 确认 B03 测试通过。

**已知限制与下一步：**

- B03 只处理飞书 OpenAPI 基础调用，不包含业务表、字段类型、领域对象或 Repository 规则。
- 客户端当前实现记录读取、批量创建和批量更新；删除能力将在出现明确业务用例和安全约束的后续任务中增加。
- 真实 table ID、field ID 及字段类型验证属于 B04。
- 未授权 Base 隔离与管理员交接仍按 B02 记录延期到上线前完成。

### B04 实现字段映射验证

- 建立九张表的运行时 table ID 与 field ID 映射对象，并根据 B01 自动生成的配置键清单从服务端环境加载。
- 缺少任一 table ID 或 field ID 时快速失败，只报告缺失配置键，不输出配置值。
- 扩展飞书客户端与官方 SDK Adapter，支持分页读取数据表和字段元数据。
- 启动校验完全按 ID 工作，不依赖可被人工修改的表名和字段显示名。
- 校验 table ID 是否存在、field ID 是否存在、字段类型、主字段、关联目标和关联多值设置。
- 检测重复 table ID 与重复 field ID，避免多个领域表或字段错误指向同一飞书资源。
- 所有 Schema 问题使用稳定问题代码汇总后失败，不自动修改真实飞书结构。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 9 个测试文件、82 个测试。
- Mock 测试覆盖正确映射、配置缺失、错误 table ID、缺失 field ID、错误字段类型、错误关联目标和显示名变更。
- 客户端表与字段元数据分页测试通过，并继续固定使用客户端配置中的 Base Token。
- `pnpm build` 通过，静态首页和 404 页面成功生成。
- 全部自动化测试使用 Mock 元数据，没有访问或修改真实飞书。
- 用户于 2026-07-31 确认 B04 测试通过，并批准进入 B05。

**已知限制与下一步：**

- B04 提供映射加载和校验能力，不负责自动发现后写回生产配置。
- 真实公司 Base 的完整 ID 配置与只读启动校验需要在生产配置准备阶段执行。
- 字段显示名允许调整；字段 ID、类型、主字段和关联目标变化会阻止启动。
- 下一项为 B05：在已验证映射和 B03 客户端之上实现九表 Repository。

### B05 实现飞书 Repository

- 在 B03 客户端和 B04 字段映射之上建立九张表共用的 `FeishuTableRepository`。
- 所有编码与解码均使用 field ID；中文字段显示名不进入读写路径。
- 支持分页列表、按稳定业务 ID 查询、记录不存在错误以及重复业务 ID 检测。
- 实现幂等 `createOrUpdate`：首次创建、内容不变返回 `unchanged`、内容变化进入受版本保护的更新。
- 创建自动写入版本 1、创建时间和更新时间；更新递增版本、保留创建时间并刷新更新时间。
- 更新必须携带与当前记录一致的预期版本；缺失或旧版本均拒绝覆盖。
- 写入前拒绝未知业务字段；读取时拒绝缺少业务 ID、版本或审计时间的损坏记录。
- 提供九表 Repository 工厂，保证冻结的每张表都使用相同安全规则。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 10 个测试文件、88 个测试。
- 测试覆盖九表工厂、field ID 编码、创建、读取、列表、更新、幂等重复创建、版本递增、旧版本拒绝、记录不存在、重复业务 ID、未知字段和损坏记录。
- `pnpm build` 通过，静态首页和 404 页面成功生成。
- 全部 Repository 测试使用内存 Mock Client，没有访问或修改真实飞书。
- 用户于 2026-07-31 确认 B05 测试通过，并批准进入 B06。

**已知限制与下一步：**

- 飞书不提供传统数据库的原子条件更新；当前乐观并发通过读时版本检查降低覆盖风险，后续任务锁处理高并发自动化写入。
- Repository 是通用基础设施，不替代领域 Schema；调用者仍须在写入前完成 A03/A06 校验。
- B05 不提供删除操作，避免在缺少明确业务和恢复流程时扩大破坏面。
- 下一项为 B06：建立统一 CLI、连接检查和 Schema 检查命令。

## 2026-08-01

### B06 实现项目飞书 CLI 骨架

- 建立可执行的 TypeScript 飞书 CLI，提供 `help`、`connection-check`、`mapping-bootstrap` 和 `schema-check`。
- CLI 默认输出简洁中文摘要，并支持 `--json` 机器可读结果，供后续 GitHub Actions 复用。
- 建立稳定退出码：配置或用法错误为 2、权限错误为 3、网络或限流错误为 4、Schema 错误为 5。
- 将飞书客户端、映射和 Schema 异常转换为安全诊断，只显示业务表 key、字段 key 和问题代码，不输出密钥、Token、table ID 或 field ID。
- 新增飞书 CLI 独立配置加载器，使连接和 Schema 检查只依赖飞书应用身份与固定 Base Token。
- 新增首次映射初始化：按九表契约、允许的显示名和明确同义别名只读发现 table ID 与 field ID；缺失或重名时整体停止，不写入部分配置。
- 映射写入采用临时文件与原子替换，保留 `.env.local` 中已有 App Secret、OpenAI Key 和其他配置，并保持仅当前用户可读写。
- 兼容已确认的飞书显示名调整，包括带书名括号的表名、“最后更新时间”和“关联产品/技术/商业化动态”；常规读写仍完全按 ID 工作。
- 规范化飞书主字段元数据，以字段列表第一页首字段作为唯一主字段，消除供应商 `is_primary` 标记在重复读取中的不稳定结果。
- 将日报稳定业务主键修正为 `digestId`；`digestDate` 保留每日业务日期和归档筛选职责，与实际飞书表和 `SPEC.md` 契约对齐。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 12 个测试文件、100 个测试。
- 测试覆盖文本与 JSON 成功输出、帮助命令、pnpm 参数分隔符、权限错误、网络错误、Schema 错误、稳定退出码和密钥脱敏。
- 映射初始化测试覆盖允许别名、缺表停止、已有映射替换、追加映射以及 App Secret 与 OpenAI Key 保留。
- `pnpm build` 通过，静态首页和 404 页面成功生成。
- 真实只读 `connection-check` 通过，可读取 9 张表。
- 真实 `mapping-bootstrap` 成功映射 9 张表和 148 个契约字段，未修改任何飞书表、字段或记录。
- 真实 `schema-check` 通过，共检查到 9 张表和 159 个实际字段；多出的统计和展示字段被保留但不进入程序契约。
- 用户于 2026-08-01 确认终端显示“飞书 Schema 检查通过：9 张表，159 个字段”，B06 正式验收通过。

**重要限制与后续工作：**

- `mapping-bootstrap` 是首次配置辅助命令，依赖显示名唯一匹配；生产日常运行不得依赖显示名。
- 本地 `.env.local` 中的映射不会提交 Git；部署 GitHub Actions 前需要将同一组配置安全迁移到公司 GitHub Secrets 或受保护环境。
- B02 延期的未授权 Base 隔离测试和第二管理员交接测试仍未完成，必须在生产发布前补齐。
- B06 不包含业务记录导入、候选处理、日报生成或网站发布命令。

### C01 实现 WorkBuddy 候选格式

- 建立版本化 WorkBuddy JSON 候选文件契约，当前固定为 `schemaVersion: "1"`。
- 文件顶层只允许版本和候选数组，每个文件限制为 1–500 条候选。
- 复用 A03 单条 WorkBuddy 输入 Schema，固定标题、原始 URL、来源名称、内容类型、来源类型、来源等级、发布时间、查询词、初步摘要和发现时间；三个分类字段由用户在 C02 开始前确认补为必填。
- 发布时间允许未知并使用 `null`；发现时间必须存在，非空时间必须是带时区的 ISO 8601。
- 候选文件严格拒绝未知字段，WorkBuddy 不能提供发布状态、正式事件状态、内部备注或其他越权控制字段。
- 新增面向 WorkBuddy 操作者的导出说明，包含合法 JSON 示例、字段表、公众号搜索要求、去重建议、安全限制和错误处理。
- 明确 WorkBuddy 只输出未经审核的国内研究候选；候选 ID、canonical URL、去重和飞书写入属于 C02。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 13 个测试文件、113 个测试。
- 合法微信公众号候选样本通过运行时 Schema。
- 缺少来源 URL、localhost、私网地址、非 HTTP URL 和包含凭据的 URL 均被拒绝。
- 超过 2000 字的初步摘要、缺时区日期、自然语言日期、越权字段和超过 500 条候选均被拒绝。
- `pnpm build` 通过，静态首页和 404 页面成功生成。
- `git diff --check` 通过。
- 用户于 2026-08-01 确认 C01 测试通过。

**重要限制与后续工作：**

- C01 只定义文件格式和导出说明，不读取文件、不生成候选 ID、不 canonicalize URL、不去重，也不写飞书。
- 文件字节大小、JSON 解析错误和批次导入行为将在 C02 CLI 导入入口中实现。
- 下一项为 C02：通过项目飞书 CLI 校验、归一化、去重并写入“研究候选”表。

### C02 实现 WorkBuddy 导入

- 根据用户确认修订 C01 候选契约，新增必填 `contentType`、`sourceType` 和 `sourceTier`，避免 CLI 猜测内容类别和来源可信度。
- 为项目飞书 CLI 新增 `workbuddy-import <file>` 命令，并支持文本与 `--json` 导入计数。
- 输入只接受最大 5 MiB 的普通 UTF-8 JSON 文件；不可读路径、目录、超限文件和非法 JSON 使用稳定输入错误失败。
- 整个文件通过 C01 严格 Schema 后才开始写入；任一候选包含未知或越权字段时拒绝整个文件。
- 实现 URL 规范化：移除 fragment、常见追踪参数、分享参数和应用参数，排序剩余业务参数并清理非根路径尾斜杠。
- 使用 canonical URL 的 SHA-256 摘要生成稳定 `candidate-wb-*` 候选 ID，不使用标题或模型推断作为身份。
- 在单文件内按稳定候选 ID 去重，并在写入前查询飞书已有候选；重复项只计数并跳过，不覆盖既有记录。
- 合法候选固定转换为国内、WorkBuddy 发现、待审核状态，只能写入“研究候选” Repository。
- 将来源名称与命中查询词以受控 JSON 保存到现有“初步抽取字段”，避免当前飞书候选表缺少独立列时丢失输入信息。
- 真实写入前执行完整飞书 Schema 校验；映射或表结构不符合契约时停止导入。
- 新增 `CLI_INPUT_INVALID`，只报告稳定文件错误码和最多 20 个安全字段路径，不输出文件内容或底层异常。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 14 个测试文件、121 个测试。
- 合法文件创建一条待审核国内 WorkBuddy 候选，并完整保留来源类型、等级、内容类型、来源名称和查询词。
- 带不同追踪参数的同一公众号 URL 生成相同 canonical URL 和稳定候选 ID。
- 同一文件重复 URL 只写一次；相同文件第二次导入不产生第二条记录。
- 发布状态等越权字段在 Repository 调用前被拒绝，非法 JSON 和超过 5 MiB 的文件不会产生写入。
- CLI 参数、JSON 成功输出、帮助信息和缺少文件路径错误均通过测试。
- `pnpm build` 通过，静态首页和 404 页面成功生成。
- `git diff --check` 通过。
- 用户于 2026-08-01 确认 C02 测试通过。

**重要限制与后续工作：**

- C02 自动化测试全部使用临时文件和内存 Repository，没有访问或修改真实飞书。
- 尚未使用真实 WorkBuddy 导出文件做生产 Base 试导入；应在准备首批 5–10 个重点公众号并进入真实试运行时执行。
- 当前来源名称和查询词保存在“初步抽取字段”；如未来飞书候选表增加独立列，需要通过受控 Schema 迁移调整映射。
- 下一项为 C03：实现 OpenAI 海外研究 Provider，继续只产生候选或建议。

### C03 实现 OpenAI 海外研究 Provider

- 建立基于 OpenAI Responses API 的海外研究 Provider，并启用 Web Search；模型名称不写死，由服务端环境配置提供。
- 对模型输出同时执行严格 JSON Schema 和本地 Zod Schema 校验，未知字段、非法 JSON、缺失字段及不符合领域约束的数据均不能进入候选层。
- 输出覆盖相关性判断、融资事实、实际引用来源、结构化多来源冲突、中文公开摘要和市场意义。
- 对拒答和不完整响应给出稳定错误；对限流、超时和服务异常执行有上限的重试，非法输出不重试。
- 新增每日请求数、输入字符数、最大输出 Token、重试次数和单次超时配置，并在启动配置阶段校验预算值。
- Provider 日志只记录安全的结果类型、尝试次数、错误码和耗时，不记录 API Key、搜索正文或完整模型响应。
- 引入精确锁定的 OpenAI 官方 SDK；自动化测试通过 Transport 接口注入替身，不访问真实 OpenAI。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 16 个测试文件、134 个测试。
- Mock 测试覆盖成功、429 限流、503 服务错误、超时、非法 JSON、Schema 错误、拒答、不完整响应、输入预算和请求预算。
- 验证瞬时错误的调用次数不超过 `maxRetries + 1`，结构错误和拒答不重试。
- 验证日志和错误不包含 API Key、底层服务错误正文或完整模型输出。
- 严格 JSON Schema 测试确认每层对象拒绝额外字段并要求全部声明字段，冲突至少包含两个来源值。
- `pnpm build` 通过，静态首页和 404 页面成功生成。
- 用户于 2026-08-01 确认 C03 测试通过。

**重要限制与后续工作：**

- C03 只提供海外研究能力和经校验的结构化建议，不直接写入飞书正式事件、公司动态或发布状态。
- 当前内存请求计数器适合单次 GitHub Actions 任务的硬上限；跨任务的自然日累计成本统计需由后续自动化任务记录实现。
- 单元与普通集成测试未调用真实 OpenAI；生产密钥、真实模型和费用上限应在公司 GitHub Secrets 与受保护环境中配置。
- C03 当前结构化输出沿用 A03 的融资事实契约；技术、产品和商业化动态尚不能作为 C03 的专用结构化结果，需在后续对应处理任务中完善事实契约后接入。
- 下一项为 C04：实现带 SSRF、重定向、响应大小和超时保护的安全内容获取。

### C04 实现安全内容获取

- 建立独立的安全内容获取模块，只允许访问公网 HTTP/HTTPS URL，并拒绝包含凭据的 URL。
- 在每次请求和每次重定向前执行 DNS 解析与地址策略校验；只要任一解析结果属于本机、私网、链路本地、云元数据、保留、组播或文档地址，就拒绝整个请求。
- 原生 HTTP Transport 使用已经通过校验的 IP 建立连接，同时保留原始 Host 与 HTTPS SNI，避免在连接阶段重新解析主机名造成 DNS rebinding。
- 手动处理重定向，支持相对地址，限制跳转次数，检测循环，并对每个新目标重新执行完整 URL 与 DNS 校验。
- DNS 解析和正文传输共用单次访问超时；超时会中止底层请求并返回稳定、可重试错误。
- 同时限制流式响应大小、声明的 Content-Length、最终正文字符数和重定向次数。
- 只接受 HTML、XHTML 与纯文本；请求明确要求 identity 编码，拒绝未支持的压缩响应和二进制内容。
- 对 HTML 提取独立标题与必要正文，移除脚本、样式、模板、iframe、导航和不可见控制字符，不保存完整页面结构。
- 所有错误使用稳定错误码和安全摘要，不输出带查询凭据的 URL、底层网络详情或响应正文。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 17 个测试文件、164 个测试。
- 测试覆盖中文与英文 HTML 正文提取、脚本和导航清理、直接私网与云元数据地址、混合公网/私网 DNS 结果、私网重定向、循环重定向、DNS 超时、传输超时、声明与实际超大响应、二进制及压缩内容。
- 地址策略测试覆盖 IPv4、IPv6、IPv4-mapped IPv6 的私网、链路本地、保留、组播和文档地址，以及合法公网地址。
- 验证请求使用已校验 IP，不把原始 URL Token 或底层异常写入稳定错误。
- 所有测试通过注入 Resolver 与 Transport 完全离线运行，没有访问真实网站。
- `pnpm build` 通过，静态首页和 404 页面成功生成。
- `git diff --check` 通过。
- 用户于 2026-08-01 确认 C04 测试通过。

**重要限制与后续工作：**

- C04 不绕过登录、验证码、付费墙、robots 限制或站点反爬措施。
- 当前只解码 UTF-8，并通过 `Accept-Encoding: identity` 获取未压缩正文；忽略该请求头而强制返回压缩内容的来源会被安全拒绝。
- C04 提供安全抓取与基础正文清洗，不判断行业相关性、不抽取融资事实，也不写飞书；这些职责从 C05 开始由后续流水线承担。
- 单元测试没有访问真实外网；真实网站兼容性应在后续受控 Smoke Test 中按允许来源验证。
- 下一项为 C05：实现行业相关性判断。

### C05 实现行业相关性判断

- 建立候选相关性领域服务，接收研究候选和 C04 已安全清洗正文，通过可替换分类器输出相关性判断。
- 将 PRD 行业范围写入 OpenAI 分类 Adapter：覆盖人形与通用机器人、各类行业机器人、机器人基础模型与 VLA、机器人学习、具身数据与仿真、关键零部件及 Physical AI 基础设施。
- 明确纯消费电子、传统自动化和无实体交互的通用 AI 默认无关；证据不足或关联牵强时必须返回 `UNCERTAIN`。
- 相关性结果固定保存判断、理由、置信度和实际模型名称；领域契约新增 `relevance.model`，便于后续追踪和复核。
- 分类器输出经过严格运行时 Schema，拒绝未知字段、非法决策、越权发布字段、超长理由和非法置信度。
- 置信度等级为 LOW 或分数低于阈值时，统一转为 `UNCERTAIN` 并进入 `NEEDS_RESEARCH`，不能自动丢弃或发布。
- `RELEVANT` 和 `NOT_RELEVANT` 均保持 `PENDING`，只记录模型判断，最终接受或拒绝仍由飞书人工审核决定。
- 已标记为 `DUPLICATE` 的候选不会被相关性服务恢复为其他审核状态。
- 模型异常转换为稳定、脱敏且可重试错误，不输出底层模型响应；传入分类器的正文再次执行字符上限。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 18 个测试文件、172 个测试。
- 明确相关的具身基础模型与机器人样本得到 `RELEVANT + PENDING`。
- 明确无关的在线办公软件样本得到 `NOT_RELEVANT + PENDING`，没有绕过人工审核自动拒绝。
- 仅提到边缘 AI 芯片但缺少机器人证据的边界样本被强制保留为 `UNCERTAIN + NEEDS_RESEARCH`。
- 测试验证重复状态保护、模型名称保存、正文长度限制、非法输出拒绝和模型异常脱敏。
- OpenAI Adapter 测试确认请求包含 PRD 行业范围、排除范围、证据不足规则及 C04 清洗正文，全程使用 Mock Provider。
- `pnpm build` 通过，静态首页和 404 页面成功生成。
- `git diff --check` 通过。
- 用户于 2026-08-01 确认 C05 测试通过。

**重要限制与后续工作：**

- C05 只判断行业相关性，不抽取融资字段、不归一化公司、不去重，也不写入正式事件。
- 当前模型判断保存在领域候选的 `relevance` 对象中；后续候选持久化流水线需要把模型和理由映射到可审计的飞书字段或受控结构化字段。
- OpenAI 分类 Adapter 复用 C03 海外研究 Provider；测试没有调用真实模型，真实分类准确率需在受控试运行中使用已知相关、无关和边界对照集评估。
- `NOT_RELEVANT` 不等于人工拒绝，自动化不能直接把它发布或删除。
- 下一项为 C06：实现融资字段抽取和来源证据关联。

### C06 实现融资字段抽取与来源证据关联

- 建立融资抽取领域服务，只允许 C05 已判断为 `RELEVANT` 的候选进入抽取。
- 抽取公司名称、轮次、金额、币种、投资方、公告日期、地区和技术方向，并把结果写回候选的 `extractedFacts`。
- 将反幻觉规则写入抽取请求：未披露金额必须使用 `amountDisclosed=false` 且金额、币种为 `null`；模糊日期必须保持 `null`；未披露投资方使用空数组。
- 扩展 C03 OpenAI 来源契约，为每个来源新增严格 `supportsFacts` 字段，逐项标注该来源实际支持的融资事实。
- 对模型返回事实执行证据收敛：没有任何来源声明支持的字段会被清空，而不是直接相信模型生成值。
- 已披露金额只有在金额和币种均有来源支持且同时存在时才保留，否则整体降级为未披露，避免半结构化金额。
- 将来源转换为带固定访问时间的 `SourceEvidence`，保留来源类型、等级、标题、发布时间和支持字段。
- 冲突中的每个值必须引用结果来源，且该来源必须声明支持冲突字段；未知或不支持的来源会使抽取失败。
- 抽取结果记录实际模型名称；正文在交给 Provider 前再次限长，Provider 异常转换为稳定脱敏错误。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 19 个测试文件、180 个测试。
- 已披露 USD 融资样本完整抽取并关联逐字段来源。
- 未披露样本保持 `amountDisclosed=false`、金额与币种为 `null`、投资方为空数组。
- EUR 与多个投资方样本正确保留，证明固定币种和多投资方数组兼容。
- “近日”等模糊日期样本保持 `announcedAt=null`，没有补造日期。
- 模型提供但来源未在 `supportsFacts` 标注的轮次、金额、投资方、日期、地区和技术标签被安全清空。
- 未知来源或来源未支持对应字段的冲突被稳定拒绝。
- 测试验证非相关候选不能进入抽取、正文长度受限、反幻觉规则进入请求且底层 Provider 错误不泄露。
- OpenAI 严格 JSON Schema 与本地 Zod Schema 均同步要求 `supportsFacts`，原有结构完整性测试继续通过。
- `pnpm build` 通过，静态首页和 404 页面成功生成。
- `git diff --check` 通过。
- 用户于 2026-08-01 确认 C06 测试通过。

**重要限制与后续工作：**

- C06 不进行公司实体合并、事件去重、最终置信度评分、公开摘要或正式飞书事件写入。
- `supportsFacts` 是模型输出的结构化来源声明；真实准确性仍需结合来源正文、C08 冲突处理和人工审核验证。
- 当前抽取面向融资事实；技术、产品和商业化动态需要专用事实契约后才能进行同等级结构化抽取。
- 来源证据目前作为 C06 结果返回；后续候选持久化和正式事件转换必须把证据映射到“信息来源”表及事件关联，不能只保留摘要。
- 下一项为 C07：实现公司名称、别名与官网域名归一化。

### C07 实现公司归一化与匹配建议

- 建立公司归一化领域服务，从公司目录中按中文名、英文名、已知别名和官网域名识别标准公司。
- 精确名称匹配仅执行保守的 Unicode、大小写和连续空白归一化，不删除名称内部有业务意义的标点。
- 已知中英文名、别名或官网域名唯一命中时返回标准公司 ID，并记录命中依据。
- 名称与域名分别指向不同公司，或同一精确名称命中多个公司时返回 `AMBIGUOUS`，禁止自动合并。
- 对非精确名称计算相似度并返回排序后的人工建议；模糊结果的 `companyId` 始终为 `null`。
- 明确区分 `MATCHED`、`SUGGESTED`、`AMBIGUOUS` 和 `UNMATCHED`，后续流水线不能把建议误当成确认匹配。
- 公司目录记录在参与匹配前重新通过 Company Schema；不合法官网或畸形公司记录被稳定拒绝。
- 建立飞书公司 Repository 的只读目录 Adapter；C07 不创建、更新或合并任何飞书公司记录。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 20 个测试文件、191 个测试。
- 中文标准名、英文名大小写和已知中文别名均正确自动命中。
- 去除 `www` 后完全一致的官网域名正确命中。
- `Galboti` 等模糊名称只产生人工建议，不返回自动公司 ID。
- `Galbot X Robotics` 与既有相似公司不会自动合并。
- `Robot X-1` 与 `Robot X1` 不会因删除标点而被自动视为同一公司。
- 名称和官网分别指向两个公司时返回 `AMBIGUOUS` 及两条待核对建议。
- 完全不同公司返回 `UNMATCHED`；危险官网和畸形目录记录被拒绝。
- 飞书目录 Adapter 只调用 Repository `list`，不产生任何写操作。
- `pnpm build` 通过，静态首页和 404 页面成功生成。
- `git diff --check` 通过。
- 用户于 2026-08-01 确认 C07 测试通过。

**重要限制与后续工作：**

- C07 不创建新公司，也不自动写入或合并飞书公司记录；模糊和冲突结果必须由人工确认。
- 官网匹配当前要求规范化后的主机名完全一致，不使用公共后缀猜测或任意子域归并。
- 相似度只用于候选排序，不是身份依据；公司身份仍以标准公司 ID 和人工维护的别名、官网为准。
- 下一项为 C08：实现事件级去重与事实冲突处理。

### C08 实现事件去重、来源合并与冲突处理

- 建立事件级去重服务，接收 C06 融资事实、来源证据和 C07 已确认的标准公司 ID。
- 将 canonical URL 相同作为强重复信号，直接返回 `URL_DUPLICATE` 并关联已有候选。
- 在相同标准公司 ID 下比较公告日期、轮次和金额；至少两个强信号一致时返回 `EVENT_DUPLICATE`。
- 同一公司但日期、轮次和金额均显示为不同融资时保持 `NEW`，避免把多轮融资合并为一条。
- 同一公告日期出现轮次、金额、币种或日期事实冲突时返回 `CONFLICT`，关联已有候选并进入 `NEEDS_RESEARCH`。
- 冲突保留双方原始值和各自来源 URL，不静默覆盖或选择任一模型值。
- 合并重复报道的全部来源，并按来源 URL 去重；同一来源重复出现时合并支持字段并保留较新的访问时间。
- 相同候选 ID 再次执行返回 `EXISTING`，不产生第二条候选，建立重跑幂等边界。
- 不同标准公司 ID 的候选不会进行事件合并，即使日期、轮次和金额相同。
- 新输入和历史处理目录均通过严格运行时 Schema，危险 URL、畸形来源和不合法历史记录会在比较前被拒绝。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 21 个测试文件、199 个测试。
- 相同 canonical URL 正确标记 `URL_DUPLICATE`。
- 不同 URL 转载的同公司、同日期、同轮次和同金额事件正确标记 `EVENT_DUPLICATE`，且两个来源均被保留。
- 同公司后续不同日期、轮次和金额的融资保持 `NEW`。
- 同日期但轮次和金额不同的样本生成两个结构化冲突，逐值关联各自来源并进入 `NEEDS_RESEARCH`。
- 相同候选 ID 重跑返回 `EXISTING`，不会改变审核状态或创建新候选。
- 相同事实但标准公司 ID 不同的样本保持独立。
- 非法来源和包含云元数据地址的历史目录记录在比较前被拒绝。
- `pnpm build` 通过，静态首页和 404 页面成功生成。
- `git diff --check` 通过。
- 用户于 2026-08-01 确认 C08 测试通过。

**重要限制与后续工作：**

- C08 返回确定性的去重结论和更新后的候选状态，但当前不直接写飞书；持久化必须通过后续统一候选流水线和 Repository 幂等执行。
- 只有 C07 `MATCHED` 的标准公司 ID 能进入事件级自动去重；模糊或冲突公司匹配必须先人工确认。
- 两个强信号的规则是 MVP 保守基线，真实试运行需抽检误合并和漏合并，再受控调整阈值。
- `CONFLICT` 只是待复核状态，不能自动决定真实金额、轮次或日期。
- 下一项为 C09：按来源等级、数量、一致性和原文可访问性计算置信度并生成可追溯摘要。

### C09 实现置信度评分与可追溯摘要

- 建立确定性置信度与摘要服务，接收 C08 处理后的候选、融资事实、来源证据、冲突和已验证可访问来源。
- 置信度综合来源最高等级、去重来源数量、事实证据覆盖率、原文可访问率和冲突惩罚计算，并输出 HIGH、MEDIUM 或 LOW。
- 一手来源、投资机构或其他权威来源增加时评分保持单调不下降；同一 URL 重复出现不会重复计算来源数量。
- 同一来源 URL 的重复证据会合并支持字段、保留较高来源等级和较新的访问时间，评分不受输入顺序影响。
- 仅有 LEAD 搜索线索、原文全部不可访问或事实覆盖率不足一半时，置信度强制限制为 LOW。
- 存在任何结构化事实冲突时，置信度最高限制为 MEDIUM，并将候选送入 `NEEDS_RESEARCH`。
- 低置信度候选进入 `NEEDS_RESEARCH`；重复候选继续保持 `DUPLICATE`，不会被评分服务恢复。
- 公开摘要采用确定性事实句生成，不调用模型自由改写；公司、轮次、金额、投资方、日期和技术方向分别形成可追溯 claim。
- 每个 claim 同时记录使用的事实字段和支持它的来源 URL；没有来源支持或存在冲突的字段不会进入确定性摘要。
- 可访问来源 URL 必须属于证据集合，避免通过额外未校验链接抬高可访问率。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 22 个测试文件、208 个测试。
- 完整、可访问的一手来源得到 HIGH 置信度和 0.945 分。
- 完整二手来源得到 MEDIUM 置信度；仅搜索线索和不可访问来源均得到 LOW 并进入待研究。
- 增加权威支持来源后评分不下降并提升至 HIGH。
- 同一 URL 的强弱重复来源以任意顺序输入均得到相同评分，来源数保持 1。
- 冲突样本被限制在 0.69 以下、进入 `NEEDS_RESEARCH`，且冲突字段不会出现在确定性摘要。
- 每条摘要 claim 均能从其来源 URL 找到支持对应字段的证据。
- 无证据的轮次和投资方不会进入摘要；未知可访问 URL 被拒绝。
- `pnpm build` 通过，静态首页和 404 页面成功生成。
- `git diff --check` 通过。
- 用户于 2026-08-01 确认 C09 测试通过。

**重要限制与后续工作：**

- C09 生成的是基于融资事实的确定性公开摘要草稿，不替代人工审核，也不生成投资建议或估值判断。
- 当前权重和阈值是 MVP 保守基线；七天试运行需要使用已知事件对照集评估高低置信度分布和人工采纳率。
- 技术、产品和商业化动态需要专用事实与摘要模板后才能使用同等级评分链路。
- 阶段 C 核心链路已完成；下一步在进入 D01 前确认首批观察名单与 WorkBuddy/OpenAI 搜索分工，然后建立飞书候选审核视图。

## 2026-08-03

### D01 建立飞书候选审核视图

- 在飞书“研究候选”表中配置“今日待审核”“高置信度”“低置信度”“待复核”和“重复候选”五个内部审核视图；“全部研究候选”保留为完整记录入口。
- “今日待审核”使用动态今天与 `PENDING` 两个条件；“高置信度”使用 `HIGH + PENDING`；其余视图分别按 `LOW`、`NEEDS_RESEARCH` 和 `DUPLICATE` 分流。
- 每个审核视图保留候选 ID、标题、原始来源 URL、来源等级、冲突提示和审核状态等核验字段，并隐藏 16 个非审核字段。
- 扩充机器可读视图契约和离线分流测试，覆盖今日、高置信度、低置信度、待复核、重复、已通过和已拒绝候选。
- 更新飞书 Schema 文档，明确六个子表是同一张候选表的筛选视图，不是独立数据副本。

**验证结果：**

- 飞书逐视图检索验证通过：“今日待审核”和“高置信度”各保存 2 条筛选条件，其余三个视图各保存 1 条筛选条件；五个视图均保存 16 个隐藏字段。
- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 22 个测试文件、211 个测试。
- `pnpm build` 通过，静态首页和 404 页面成功生成。
- 用户于 2026-08-03 在飞书确认六个视图及筛选条件一致，D01 验收通过。

**重要限制与后续工作：**

- 当前尚未导入研究候选，因此六个视图为空是预期状态；首次候选导入时仍需用真实记录确认飞书端自动分流。
- 当前飞书把“相关性”设为不可隐藏的界面主字段，因此各审核视图会额外显示该字段；领域业务主键仍为候选 ID。
- 飞书视图 API 不提供项目所需的排序配置能力；“今日待审核”的置信度分数降序由飞书界面维护。
- D01 不创建、审核或发布任何候选；下一项为 D02：实现审核转正式融资事件。

### D02 实现审核转正式融资事件

- 新增独立候选审核转换服务，将候选层与正式融资事件层隔离。
- 只有 `APPROVED` 候选进入正式事件转换；`PENDING`、`REJECTED`、`NEEDS_RESEARCH` 和 `DUPLICATE` 均无写入地返回跳过。
- 由候选 ID 确定性生成事件 ID，保证同一候选重复执行返回既有事件，不创建第二条。
- 正式事件初始固定为 `DRAFT` 且 `isPublic=false`，避免把候选批准误当成公开发布批准。
- 审核转换要求已确认公司、融资事实、融资日期、地区、至少一个来源、置信度、公开摘要、公开意义和重要性字段；信息不完整时拒绝写入。
- 新增飞书正式事件 Store Adapter，将公司和来源稳定业务 ID 解析成飞书记录 ID，再通过 B05 Repository 幂等写入。
- 禁止审核转换覆盖已存在但内容不同的正式事件，内容变化必须进入后续显式人工修改流程。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 24 个测试文件、225 个测试。
- 五种审核状态行为正确，四种非批准状态均未调用正式事件 Store。
- 批准候选正确生成不公开草稿，缺少必要审核字段的样本全部拒绝且无写入。
- 相同候选重复转换返回 `EXISTING`，内存正式事件数量保持 1。
- 飞书 Adapter 正确解析公司和来源关联记录 ID，并拒绝可能覆盖既有事件的更新结果。
- `pnpm build` 通过，静态首页和 404 页面成功生成。
- 用户于 2026-08-03 确认 D02 测试通过。

**重要限制与后续工作：**

- D02 当前提供领域服务和飞书 Store Adapter，没有创建会从不完整候选字段猜测公司或来源的 CLI 命令。
- 本轮只使用离线 Store 和 Repository 替身，没有向真实飞书写入测试融资事件。
- 首次真实候选转换前，统一处理流水线必须提供审核确认后的公司业务 ID、来源业务 ID、公开摘要和重要性字段。
- 并发 Runner 的互斥和任务锁由后续 F02 自动化任务状态机负责；D02 保证确定性 ID 和顺序重跑幂等。
- 下一项为 D03：按 Asia/Shanghai 业务日期生成唯一日报草稿与初始排序。

### D03 实现每日日报草稿生成

- 新增日报生成服务，使用 `Asia/Shanghai` 从运行时钟确定唯一业务日期，不依赖 Runner 本地时区。
- 从正式融资事件和公司动态中选择当天且非 `WITHDRAWN` 的内容，分别形成“今日融资”“技术与产品”“商业化进展”三个板块。
- `TECHNOLOGY` 和 `PRODUCT` 合并进入技术与产品板块，`COMMERCIALIZATION` 单独进入商业化板块。
- 三个板块默认按重要性评分降序；同分时按稳定业务 ID 排序，并生成每个板块从 1 开始的初始顺序。
- 使用 `digest-YYYY-MM-DD` 作为稳定日报 ID；同一天重复生成返回既有日报，不创建第二条。
- 支持无任何当日内容的合法空日报，用于区分无新闻与任务未运行。
- 日报草稿固定为 `PENDING + DRAFT`、`autoPublished=false`，不会自动公开。
- 新增飞书日报 Store Adapter，将融资事件和公司动态业务 ID 解析成飞书关联记录 ID，并拒绝静默覆盖已有日报。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 26 个测试文件、233 个测试。
- 固定时钟验证 UTC 16:00 对应上海次日 00:00 的日期边界正确。
- 三个板块正确分类，非当天记录与 `WITHDRAWN` 内容被排除。
- 重要性降序、同分稳定 ID 和各板块独立排名符合 Fixture 预期。
- 空内容正确生成一条空日报，同一业务日重复生成保持一条日报。
- 飞书 Adapter 正确解析三个板块的关系记录 ID，并支持空关系数组。
- `pnpm build` 通过，静态首页和 404 页面成功生成。
- 用户于 2026-08-03 确认 D03 测试通过。

**重要限制与后续工作：**

- D03 只组织已进入正式表的内容，不从研究候选直接生成日报，也不决定允许公开。
- 本轮使用离线内容源和 Store，没有向真实飞书创建测试日报。
- 当前重跑只接受完全相同的日报内容；新增或修正内容后的显式草稿更新流程将在后续审核与自动化串联阶段处理。
- 飞书 Schema 当前不保存领域对象中的 `marketObservation` 字段，D03 生成空值且飞书 Adapter 不写该字段；如未来需要市场观察，必须先单独确认 Schema 变更。
- 下一项为 D04：实现飞书审核提醒、发布、失败和恢复四类纯文本通知。

### D04 实现飞书个人纯文本通知

- 根据用户最新决定，将 MVP 通知目标固定为一个个人飞书用户，不向任何群聊发送消息。
- 将生产配置从群聊 `FEISHU_NOTIFICATION_CHAT_ID` 替换为个人 `FEISHU_NOTIFICATION_RECIPIENT_OPEN_ID`；仅配置群聊 ID 无法通过生产配置校验。
- 新增飞书通知 Provider，严格校验个人 `open_id`、文本长度和幂等键，并对供应商失败进行脱敏。
- SDK Transport 固定使用 `receive_id_type=open_id` 和 `msg_type=text`，调用方无法切换为 `chat_id` 或消息卡片。
- 实现待审核提醒、日报生成/发布、任务失败和任务恢复四类纯文本消息，并为每类消息生成基于业务日期的稳定幂等键。
- 所有链接必须通过安全公开 URL 校验；通知失败只返回可重试错误，不修改任何正式数据。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 28 个测试文件、246 个测试。
- 四类消息内容、计数、审核状态、错误代码和普通链接符合 Fixture 预期。
- 个人 `open_id` 正确传入私聊 Transport；群聊 ID、空 ID 和畸形 ID 均被拒绝。
- 测试载荷不包含 `chat_id`、`interactive` 或消息卡片结构。
- 供应商异常和空响应转换为稳定、可重试且不泄密的通知错误。
- 通知发送失败时，正式发布状态和版本保持不变。
- `pnpm build` 通过，静态首页和 404 页面成功生成。
- 用户于 2026-08-03 确认 D04 测试通过。
- 用户通过飞书“通过手机号或邮箱获取用户 ID”接口，以当前自建应用身份取得个人 `open_id`，并将其保存在本地 `.env.local`。
- 使用现有 `FeishuMessageSdkTransport` 和 `FeishuNotificationProvider` 完成一次受控真实私聊测试；飞书 API 确认送达，用户于 2026-08-03 确认本人收到，未向群聊发送消息，也未读写飞书多维表格。

**重要限制与后续工作：**

- 自动化测试仍全部使用 Mock Transport；真实发送仅用于本次明确授权的人工验收，不进入自动化测试。
- 本地个人 `open_id` 已配置，但上线前仍需将同一配置迁移到公司 GitHub Secrets 或受保护环境，不能依赖个人电脑上的 `.env.local`。
- 生产运行仍要求飞书应用启用机器人能力、具备发送消息权限，且目标用户处于应用可用范围。
- 当前仅支持一个明确收件人；增加多人或群聊属于后续产品变更，必须由用户再次确认。
- 阶段 D 的四项任务已完成；下一阶段为 E01：实现严格的公开字段投影。

### D05.1 指定日期海外候选发现（离线实现与测试验收）

- 在实施计划中冻结 D05 首版真实日报受控试跑的四段边界：指定日期候选发现、飞书人工审核、批准候选转正式数据、生成并私聊日报。
- 新增 `openai-discover` CLI 命令，接受 Asia/Shanghai 业务日期和版本化查询文件；首次试跑查询文件覆盖重点公司、模型与世界模型、本体与全栈、仿真与数据、末端与传感器、执行器与零部件、算力能源散热及新玩家发现。
- 海外发现只接受原始来源发布时间属于指定上海业务日期的结果；日期不明、其他日期、`SEARCH_SNIPPET` 或 `LEAD` 单独支撑的结果不得写入候选。
- 合格结果只能幂等写入飞书“研究候选”，固定为 `OVERSEAS + OPENAI + FUNDING + PENDING`；相同规范 URL 重跑时跳过，不覆盖已有候选，也不能写正式事件、日报或发布状态。
- 为 WorkBuddy 准备 2026-08-02 国内试跑交接说明，继续沿用 C01 固定 JSON 与 C02 导入入口，禁止建立试跑旁路格式。
- 修正飞书 Repository 的日期边界：领域层继续使用 ISO 日期或日期时间字符串，飞书 `date` 和 `dateTime` 字段统一编码为毫秒时间戳；读取时再分别恢复为上海业务日期和 UTC ISO 时间。
- 当部分 OpenAI 查询失败时继续处理其余查询并报告安全计数；全部查询失败时返回可重试的 `CLI_OPENAI_UNAVAILABLE`，不得以成功状态掩盖服务中断。

**验证结果：**

- `pnpm typecheck` 通过。
- `pnpm lint` 通过且无警告。
- `pnpm test` 通过，共 31 个测试文件、264 个测试。
- Mock 验证了指定上海日期、合法候选创建、其他日期拒绝、线索来源拒绝、不相关结果拒绝、规范 URL 重跑去重、部分失败继续和全部失败快速报错。
- Repository 测试确认飞书日期字段发送毫秒时间戳，并能无损恢复领域日期；非法领域日期和畸形飞书日期均被拒绝。
- `pnpm build` 通过，静态首页和 404 页面成功生成。
- `pnpm cli -- help` 正确展示 `openai-discover` 命令。
- 敏感信息扫描未发现真实密钥、访问令牌或完整模型响应进入受版本控制文件。
- 用户于 2026-08-03 确认 D05.1 离线测试通过。

**重要限制与后续工作：**

- 本轮未调用真实 OpenAI、WorkBuddy 或生产飞书，尚未创建 2026-08-02 真实候选；真实试跑仍需用户明确授权并配置 `OPENAI_API_KEY` 与 `OPENAI_MODEL`。
- 当前 C03 OpenAI 严格输出契约只覆盖融资研究，因此海外入口本轮只创建 `FUNDING` 候选；海外技术、产品和商业化发现必须在后续单独扩展领域 Schema 后接入。
- WorkBuddy 国内四类候选需要按照交接说明生成独立 JSON，再由项目 CLI 校验和导入；WorkBuddy 不能直接写飞书。
- D05.2 只能在首批真实候选写入后开始，届时由用户在飞书逐条作出审核决定。

### 2026-08-03 WorkBuddy 国内搜索逻辑宽召回修订

- 将每日国内发现从“逐一监控重点公司 + Physical AI 强相关 + 一手原文门禁”调整为“FA/VC/专业媒体优先 + 高命中事件词宽召回 + 线索触发补搜 + 证据分级”。
- 候选范围扩展到有价值的 AI、科技与硬科技动态，Physical AI、具身智能及其上下游保持排序优先。
- 融资、并购、新产品、新技术、商业化、订单、交付、部署、量产和产线等类型统一使用先发现、后补充搜索的逻辑。
- 明确每条候选必须保留可访问网址；公司、投资方、参投方、FA 或其他事件参与方的明确披露可标记“已确认”，36氪、投资界和可靠专业媒体报道可直接正常收录。
- 允许“数千万元”“数亿元”“近亿元”及未披露金额等非精确表述，要求保留原文、不猜测具体数字。
- 将报道日期作为每日归档的必填依据；事件日期仅在来源明确时记录，未披露时不得猜测。
- 要求补充公司中英文名、一级/二级赛道、主要业务、核心产品/技术、应用场景与官网；信息不足时保留“待补充”。

**验证结果：**

- 已对 `workbuddy-domestic-observation-list.md` 与 `docs/pilot/workbuddy-daily-search-task.md` 执行关键规则搜索检查，确认新的来源、日期、金额、公司画像和执行顺序已写入。
- 本轮只修改搜索策略文档，未执行搜索、未调用 WorkBuddy 或 OpenAI、未读写飞书、未生成日报。

**重要限制与后续工作：**

- C01 JSON 当前没有独立的事件日期、证据状态和公司画像字段；在不修改工程契约前，WorkBuddy 暂将这些信息简要写入 `preliminarySummary`。
- OpenAI 海外搜索仍执行 D05.1 的 Physical AI 融资严格门禁，本轮尚未连带修改；需在用户单独确认后同步实现与测试。

**同日精简补充：**

- 根据用户反馈，彻底取消重点公司的每日与每周定点搜索；公司名只用于已发现线索的补充核验。
- 将 `workbuddy-domestic-observation-list.md` 从 500 余行的详细矩阵精简为执行版：保留优先信源、赛道排序参考、四组高命中事件词、线索补搜、证据分级和交付规则。
- 赛道名单不再触发定时搜索，只用于候选分类和 Physical AI 优先排序；删除公司名单、公司定向模板和六维查询矩阵。
- 补充用户指定的国内高价值信源：华映资本、晚点 LatePost、智财社、品牌方舟和 Startup 推荐官；投资界、创业邦和雷峰网原已在精简名单中。Startup 推荐官保留账号名称、认证主体、原创比例与准确性待核验标记。

### 2026-08-03 搜索策略基线与当日候选复核

- 新增 `search-strategy.md`，将用户确认的国内外搜索要求整理为共享策略，并明确区分 MVP 人工触发/受控导入流程与长期可交接自动化方案。
- 固化宽科技候选、Physical AI 优先排序、必须保留网址、参与方披露可标记已确认、可信专业媒体可正常收录、模糊金额可收录和事件日期可选等原则。
- 固化“不做公司或赛道的日/周定点轮询”；赛道只用于分类和排序，公司名只在发现线索后用于补充搜索。
- 固化融资、并购、技术、产品和商业化使用同一套发现、补搜、证据分级、Schema 校验、去重和人工审核流程。
- 对 WorkBuddy 修订后的 `d05-workbuddy-candidates-2026-08-03.json` 重新执行 C01 运行时 Schema 校验：21 条候选、21 个唯一 URL、0 个重复 URL、0 个乱码，Schema 通过；类型分布为 13 条融资、3 条技术、2 条产品和 3 条商业化。
- 业务日期复核发现 1 条不合格：“中科原动力完成 B2 轮数亿元融资”的 `publishedAt` 为 `2026-08-02T07:52:00+08:00`，不属于 2026-08-03 批次。该条需移入 8 月 2 日批次，或替换为 8 月 3 日的独立来源后再校验。

**验证结果：**

- `search-strategy.md` 已覆盖搜索范围、事件类型、来源与证据状态、报道/事件日期、模糊金额、公司补充、MVP 国内外分工、排序、长期自动化、契约演进和安全边界。
- 本轮只更新文档并执行候选离线校验；未修改业务代码，未调用 OpenAI 或 WorkBuddy 搜索，未将任何候选写入飞书。

**重要限制与后续工作：**

- `openai-discover` 当前仍是 Physical AI 融资严格实现，尚未按新策略扩展为海外宽科技、技术、产品和商业化搜索。
- C01 尚无独立的证据状态、事件日期、金额类型和公司画像字段；MVP 暂写入 `preliminarySummary`，长期需修改领域 Schema、飞书字段和测试后再结构化。
- 8 月 3 日候选批次在修正业务日期异常前不应整批直接导入飞书。

### 2026-08-03 主 Agent / 子 Agent 交接基线补齐

- 新增 `main-agent-handoff.md`，为新主 Agent 提供当前实施位置、已验证结果、真实候选批次状态、未解决日期问题、下一步执行顺序和建议的子 Agent 分工。
- 更新 `AGENTS.md`：主 Agent 接管时必须先阅读交接页；搜索、观察清单和候选任务必须阅读 `search-strategy.md`；国内 WorkBuddy 任务同时阅读精简观察清单。
- 更新 `agent-coordination.md`：Research Operations 改为宽召回、信源优先、线索触发补搜和搜索审计；删除“维护全量查询矩阵”的旧职责，明确不恢复公司或赛道定点轮询。
- 将 `search-strategy.md` 和 `main-agent-handoff.md` 列入 Codex 统一维护的共享文档，并将搜索子 Agent 的必读输入和权限边界写入任务分配规则。

**验证结果：**

- 交接页已明确当前只允许执行“修正 8 月 3 日候选业务日异常 → 重新离线校验 → 用户确认 → 受控导入 → 用户验收后进入 D05.2”的顺序。
- 已搜索检查 `AGENTS.md`、`agent-coordination.md`、`main-agent-handoff.md` 中的必读文件、Research Operations 职责、禁止定点轮询、D05 当前状态和下一步门禁，内容已对齐。
- 本轮只修改 Markdown，未修改业务代码，未导入候选，未读写飞书，未进入 D05.2。

**重要限制与后续工作：**

- 主 Agent 仍需在每个子任务开始前冻结允许修改文件、禁止范围和验收测试，不得仅依赖角色名称授权。
- `main-agent-handoff.md` 是活文档；完成真实导入、D05.2 或其他重大里程碑后，主 Agent 必须同步更新其当前状态和下一步。
## 2026-08-03（MVP 与 Agent 协作基线重置）

- 将开发组织冻结为 1 个主 Agent + 6 个专职子 Agent：代码开发、Git 管理、每日搜索管理、契约与集成测试、E2E/安全/可靠性测试和 UI 设计。
- 明确主 Agent 负责分解、契约、调度和最终集成，子 Agent 按文件范围和权限执行，避免多个开发者并发修改共享契约。
- 将 MVP 重新定义为“搜索→候选→校验→人工审核→日报→私聊通知→静态网站→失败补跑”的七天可运行闭环。
- 将完整公司档案、90 天回填、丰富看板和高级筛选移至 MVP 之后，不再阻塞最小闭环验收。
- 新增北极星指标 WVID：每周按时交付的有效情报条数；只计入人工批准、去重、有可访问原始来源、重要性不低于 3 且 24 小时内发布的内容。

**验证结果：**

- 已完成 `architecture.md`、`PRD.md` 和 `agent-coordination.md` 的边界一致性修订。
- 本次只修改文档，未修改业务代码、飞书数据、生产配置或公开站点。

**重要限制与后续工作：**

- WVID 的首个目标值必须在 7 天真实试运行后依据基线设定，当前不伪造无数据支持的数量目标。
- `SPEC.md` 和 `implementation-plan.md` 仍保留 90 天回填与公司档案的完整实现任务，但它们现在是 MVP 后续增强，需在下一次实施计划排期时重新标注优先级。

### 2026-08-03 D05.1 WorkBuddy 候选业务日校正

- 根据人工核验，将“农业机器人领域国家专精特新'小巨人'，拿下数亿元融资”从2026-08-03候选批次移出；事件发生日为2026-07-31，来源页面实际发布时间为2026-08-02 07:52（北京时间），未改写任何原始时间。
- 将该条按C01现有字段追加到既有 `d05-workbuddy-candidates-2026-08-02.json`；8月2日批次由5条变为6条，原有5条保持不变。
- 8月3日批次由21条变为20条，目标来源URL仅保留在8月2日批次。

**验证结果：**

- Contract & Integration QA 独立复核通过；2026-08-04 主 Agent 再次使用项目实际 C01 strict Schema 复核。
- 2026-08-02 批次：6 条候选、6 个唯一 URL、批内重复 0、业务日异常 0。
- 2026-08-03 批次：20 条候选、20 个唯一 URL、批内重复 0、业务日异常 0。
- 两批次跨文件重复 URL 为 0；控制字符和常见敏感模式检查未发现问题。
- 海外轻量融资查询模板通过 query-file strict Schema：3 个查询、3 个唯一 `queryId`。
- `pnpm test -- tests/pipeline/overseas-discovery/query-file.test.ts tests/providers/workbuddy/candidate-format.test.ts` 实际由 Vitest 运行全量测试：31 个测试文件、264 项测试通过。
- `git diff --check` 通过。
- 本次未修改业务代码、运行时 Schema、搜索策略或正式数据，未写入飞书，未进入D05.2。

**重要限制与后续工作：**

- QA 通过后仍只能建议进入真实导入；真实写入飞书必须等待用户明确授权。
- 实时 URL 完整可访问性尚未全部复验，建议在请求导入授权前完成只读 smoke check。

### 2026-08-03 海外搜索轻量化与国内优先调整

- 根据用户确认，将搜索运营优先级调整为国内为主、海外为轻量补充；WorkBuddy继续负责国内，OpenAI继续负责海外，不改变Provider地域职责。
- 海外搜索采用与WorkBuddy一致的方法：优先信源、精简事件词、宽发现、线索触发补搜；取消公司/赛道定点轮询和细粒度查询矩阵。
- 保留日期化首次试跑查询文件作为历史基线，新增3条宽泛融资意图的轻量查询模板 `docs/pilot/d05-overseas-queries-lightweight.json`。
- 海外公司和赛道清单降级为已发现线索的分类、排序和补搜参考，不再作为逐项执行清单。

**验证结果：**

- 本次只调整搜索策略文档和查询配置；未修改 `openai-discover`、Provider、运行时Schema、飞书字段或业务代码。
- 轻量查询JSON已通过项目实际query-file strict Schema：`schemaVersion=1`、3个查询、3个唯一queryId；Contract & Integration QA独立复核文档一致性通过。
- 未运行真实OpenAI搜索，未写入飞书，未创建、批准或发布任何候选。

**重要限制与后续工作：**

- 当前 `openai-discover` 仍只支持Physical AI融资候选；轻量化不代表已支持海外技术、产品、商业化或宽科技候选。
- 若未来取消每日海外覆盖、改变国内外MVP范围或调整Provider地域职责，必须另行确认并更新架构与产品文档。

### 2026-08-03 海外 Physical AI 量产、模型与产品搜索补充

- 根据用户补充要求，海外轻量搜索从融资扩展为三类概览意图：融资/资本，量产/交付/部署，新模型/新产品/重要版本与开源。
- 允许每日从观察清单轮换选择少量高信号Physical AI公司做概览检查，同时保留不预设公司名的宽泛发现；仍不恢复全公司逐项轮询或公司×赛道×事件矩阵。
- 新增 `docs/pilot/d05-overseas-lightweight-search-task.md`，固化搜索范围、精简事件词、来源门禁和交接要求。

**验证结果：**

- 本次只修改搜索运营Markdown，未修改业务代码、Provider、运行时Schema、飞书字段或融资查询JSON。
- 文档明确融资可走现有 `openai-discover`；量产、新模型和新产品在专用Schema完成前只形成带来源的Research Operations研究线索。
- 未运行真实OpenAI搜索，未写入飞书，未创建、批准或发布候选。

**重要限制与后续工作：**

- 当前自动入口仍只支持Physical AI融资。若要让量产、新模型和新产品自动创建候选，需要由主Agent另行安排领域Schema、Provider输出和测试实现。

### 2026-08-03 WorkBuddy 观察清单执行引用补齐

- 修正 `search-strategy.md` 的国内WorkBuddy段落，明确 `workbuddy-domestic-observation-list.md` 是实际信源、公众号、赛道排序、精简事件词和补搜执行清单。
- 明确总策略只维护方法论，不重复维护容易漂移的完整账号名单；具体FA、VC/CVC、产业资本、财经/科技/产业媒体、政府基金与园区名称以观察清单当前版本为准。
- 要求WorkBuddy任务按观察清单“每日优先信源”顺序搜索，并如实处理其中标记为待核验的候选信源。

**验证结果：**

- `search-strategy.md`、`docs/pilot/workbuddy-daily-search-task.md` 与 `AGENTS.md` 现在均明确引用同一份国内观察清单，职责一致。
- 本次只修改搜索运营Markdown，未运行WorkBuddy、未修改候选、未写入飞书。

**重要限制与后续工作：**

- 观察清单是执行参考而不是公司/赛道定点轮询矩阵；新增或降级信源仍需依据实际命中率、原创比例和审核通过率复盘。

## 2026-08-04

### 每日搜索、人工核验与自然周周报口径冻结

- 将内容节奏正式调整为：每天由 Research Operations 搜索，随后执行确定性格式清洗、URL 规范化、完全重复合并和疑似同事件分组，最后由用户人工核验；自动步骤不得替用户裁决冲突事实或删除无法确认的跨媒体证据。
- 报告周期改为北京时间自然周周一至周日，次周一汇总上一周已批准的唯一资本事件，完成人工发布门禁后生成周报、更新静态网站并发送周报链接。
- 主北极星继续使用 WVID；前两个自然周增加人工基准召回率校准。基准集由 Agent 与用户人工搜索结果合并、人工确认并事件级去重后形成；Agent 独立发现且人工确认有效的事件也进入基准集。
- 召回率定义为：Agent 找到且经人工确认的唯一有效事件数，除以联合人工基准集中的唯一有效事件总数。
- 明确静态网站仍可支持链接、导航、前后周切换、筛选、排序、展开收起和图表等浏览器端交互；“静态”只表示不依赖在线服务端实时生成或写入正式数据。
- 精简 `docs/pilot/codex-daily-capital-search-task.md`：只保留单次运行参数和交付路径，搜索、收录、异常、清洗、去重及人工门禁统一由 `search-strategy.md` 维护。

**验证结果：**

- 同步更新 `PRD.md`、`architecture.md`、`SPEC.md`、`TECH-STACK.md`、`implementation-plan.md`、`AGENTS.md`、`agent-coordination.md`、`search-strategy.md`、飞书 Schema 说明和主 Agent 交接。
- 本轮只修改 Markdown，未修改 DailyDigest、飞书真实表、公开 DTO、通知、网站路由或业务代码；周报不能标记为已实现。

**重要限制与后续工作：**

- 新增 D06 周报迁移任务，必须同时迁移 DailyDigest、飞书“日报”表、Repository、公开 DTO、通知、网站 `/daily` 路由、归档、Fixture 和测试。
- 下一步仍需与用户冻结详细清洗与事件级去重决策树，然后才能对 34 条试跑线索执行一致的机器整理与人工核验。

### 搜索架构正式切换为 Codex 国内网站主链路

- 根据用户明确授权，正式取消 WorkBuddy 国内、OpenAI 海外的地域搜索职责分工；Codex 项目内 Research Operations 改为国内网站搜索主责，海外搜索当前暂停且未来恢复后仍由该角色执行。
- 当前搜索 MVP 收敛为融资与广义资本动态，使用融资、获投、并购、收购、IPO 等大事件词逐站宽搜；技术、产品、量产和商业化等非资本搜索暂不执行，整个产品 MVP 是否同步收缩留待专项对齐。
- 网站成为国内主渠道；微信公众号不直接爬取。wechat-mp-rss 尚未产生可验证结果，只作为实验性补充，空结果、延迟或频控不影响网站搜索完成判定。
- OpenAI 继续承担候选后的相关性判断、事实抽取、冲突核验和摘要；既有 `openai-discover` 暂停。WorkBuddy C01/C02 与 `workbuddy-import` 仅作为历史兼容能力保留。
- 将 `workbuddy-domestic-observation-list.md` 和 `openai-overseas-observation-list.md` 移至 `docs/archive/search/`，未删除内容；现行国内入口改为 `domestic-website-search-list.md` 和 `docs/pilot/codex-daily-capital-search-task.md`。
- 同步更新架构、产品、工程规格、技术栈、实施计划、Agent 协作、搜索策略、状态交接及相关运行文档，避免后续 Agent 恢复旧分工。

**验证结果：**

- Research Operations 对全仓 Markdown 完成独立只读审计，区分了必须修改的现行契约、可保留的历史记录和需要归档的观察清单。
- `git diff --check` 与全仓旧现行引用扫描纳入本轮最终验证；本次未修改业务代码、Schema、候选 JSON、飞书数据或 CLI 行为。
- 未运行 WorkBuddy、OpenAI 搜索、真实网站搜索、RSS 抓取或飞书写入。

**重要限制与后续工作：**

- Codex 定时任务机制可用于人工监督下的试验；生产 SLA 仍要求公司可交接运行环境、无个人登录态、确定性入口、覆盖审计、失败补跑和幂等。当前尚未实现或验收该自动化。
- wechat-mp-rss 尚未验证稳定性、频控、条目日期和链接可访问性，不能计入覆盖率或作为网站搜索替代品。
- 在继续后续 `implementation-plan.md` 前，需要与用户专项冻结原始线索清洗、URL 规范化、跨媒体事件合并、主来源选择和重复录入处理规则。

### 国内媒体资本动态高召回策略校正

- 完成 `docs/pilot/codex-media-capital-recall-2026-08-03.md` 试跑复盘：指定 10 家媒体均留下覆盖记录，原始池包含 34 个唯一 URL，其中 36氪 11 条。
- 发现初轮只检查 36氪融资快报会漏掉普通文章；后续固定使用普通“文章”站内搜索 + 快讯/融资快报双渠道，必要时用 `site:` 查询补漏。
- 取消 20 条或任何数量上限/停止参考；每个指定媒体翻页至明确越过目标日，或连续一整页无目标日结果，并记录渠道、翻页范围、停止原因和访问限制。
- 将搜索产物明确分为“原始线索池”和“正式研究候选”两层。搜索/列表显示目标日即可进入原始池；业务日、来源、Schema、事实和去重门禁后移到用户初筛后的正式候选转换。
- 原始池可保留已完成、进行中和融资意向；正文日期冲突、无日期、不可访问、标题/正文冲突和广义资本事件均保留并标记，不得猜测缺失字段或改写融资状态。
- 跨媒体疑似同一事件保留全部 URL 并分组，不在原始池提前删除证据。
- 同步更新 `search-strategy.md`、`workbuddy-website-search-list.md`、`docs/pilot/workbuddy-daily-search-task.md` 和 `main-agent-handoff.md`。

**验证结果：**

- 试跑报告统计：正文日期未明确 10 条、`BODY_UNAVAILABLE=0`、`DATE_CONFLICT=2`、`CONTENT_MISMATCH=1`、疑似重复事件组 3 组。
- 文档回归例已明确覆盖：已完成但字段不全、正在推进融资、寻求融资、标题与正文状态冲突，以及 36氪双渠道搜索。
- 本轮只修改搜索运营和状态 Markdown，未修改 `architecture.md`、`PRD.md`、Provider 职责、业务代码、Schema、飞书数据或正式候选。

**重要限制与后续工作：**

- 高召回结果不代表绝对零遗漏；机器之心、高工机器人和智东西等站点仍存在搜索或访问限制。
- 下一步由用户人工初筛 34 条原始线索；只有用户选中的线索才进入正式候选转换和 QA。
- 是否将国内搜索正式主责调整为 Codex/Research Operations，仍待用户根据试跑质量单独决定。

### Agent 精简协作与人工升级约定

- 新增 `docs/agent-working-agreement.md`，将六个子 Agent 定义为按需启用的能力池，小任务默认使用一个执行 Agent，只在有独立验收价值时增加一个 QA。
- 固化人机分工：Agent 优先处理 Schema、计数、重复、批量 URL 和离线验证；来源事实匹配、日期口径、事实冲突、信源可信度和飞书审核优先交给用户。
- 固化“先询问用户”触发条件：两轮合理尝试仍无法确认、需要登录/验证码/付费或新权限、多方案影响结果、来源冲突、外部写入/发布、契约变更或继续搜索价值过低。
- 收紧文档权限：子 Agent 默认不修改 `progress.md`、`main-agent-handoff.md` 和共享契约，由主 Agent 在集成后统一更新。
- 将协作约定引用加入 `AGENTS.md`、`agent-coordination.md` 和 `main-agent-handoff.md`，避免各文档重复复制长规则。

**验证结果：**

- 文档明确覆盖默认调度、人工升级、文件权限、Token 控制、外部操作门禁和精简交接模板。
- 本轮只修改 Markdown，未修改业务代码、候选 JSON、Schema、飞书数据或生产配置。

**重要限制与后续工作：**

- 协作约定不改变 `architecture.md`、`PRD.md`、`SPEC.md` 和 `implementation-plan.md` 中的产品、架构与工程契约。
- 下一个任务从 D05.1 URL 可访问性 smoke check 开始，并按本约定使用 Research Operations 执行、Contract & Integration QA 只复核异常项。

### 过期研究过程文档清理

- 经用户明确确认，删除 `codex-observation-list.md` 和 `observation-list-v1.md`；两者均为旧版观察名单草案，已由当前国内外观察清单和 `search-strategy.md` 取代，且无仓库内引用。
- 删除 `workbuddy-domestic-observation-list-finalization-guidance.md`；该一次性收口说明依赖已删除的旧核验文档，当前执行流程已由精简观察清单和 Agent 协作约定替代。
- 删除 `workbuddy-candidate-revision-prompt-2026-08-03.md`；候选修订已完成，最终结果保留在候选 JSON、修订报告、`progress.md` 和 `main-agent-handoff.md` 中。
- 同步移除 `main-agent-handoff.md` 中对已删除修订提示的引用。

**验证结果：**

- 仓库 Markdown 引用检查未发现对上述四个已删除文件的残留引用。
- 本次只清理文档，未修改业务代码、候选 JSON、Schema、飞书数据或生产配置。

**重要限制与后续工作：**

- 工作区中原已处于删除状态的其他旧 WorkBuddy/微信过程文档本轮没有恢复或扩大处理。
