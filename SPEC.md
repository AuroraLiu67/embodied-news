# Embodied Intelligence Intelligence Tracker — Agent Project Spec

> 本文件供 AI agent、工程师和自动化工具快速理解项目及协作边界。
> 产品范围以[`PRD.md`](./PRD.md)为准，8月7日页面、流程与验收以[`design-document.md`](./design-document.md)为准。如有冲突，以用户最新明确决定为准。

## 1. Mission

构建一个面向具身智能与 AI 行业的全球情报追踪系统。系统将分散在公司官网、论文、媒体、社交平台、视频、播客及中国内容平台的信息，整理为可追溯、可排序、可检索的行业事件，并生成每日早报、Timeline、融资数据看板和 Notes。

首要用户是战投、投资及产品/战略人员；次要用户是研究人员。MVP 先由产品所有者单人内测，未来可能团队使用或公开。

目标日期：2026-08-07 交付可在每个中国大陆法定工作日真实使用的 MVP；上线后连续4周验证质量指标。

## 2. Current Status

- 当前阶段：产品需求、产品设计、融资规格、技术选型、多Agent协作规则、实施计划和进度机制已确认，尚未开始工程实现。
- 已有文档：`PRD.md`、`design-document.md`、`FINANCING_DASHBOARD_SPEC.md`、`TECH_STACK.md`、`architecture.md`、`AGENTS.md`、`implementation-plan.md`、`progress.md`。
- 下一步：按`implementation-plan.md`先完成决策清障与工程基线，再依赖顺序开发；UI视觉风格后置。
- 当前不应直接开始大规模功能开发，除非用户明确授权。
- 实际进度、验证证据、阻塞项和下一步统一记录于`progress.md`。

## 3. Product Outcomes

系统必须帮助用户：

1. 尽量不遗漏重大行业事件；
2. 在 5–10 分钟内读完每日核心动态；
3. 从原始出处验证每条信息；
4. 追踪公司、技术和赛道的历史演进；
5. 通过 Notes、划线和双向链接形成个人判断；
6. 在长期积累后，为研究 agent 提供可信数据基础。

## 4. MVP Scope

必须覆盖的主题：

- 人形机器人；
- VLA、机器人基础模型与世界模型；
- 数据采集、数据生产与仿真；
- 产业落地与商业化；
- 融资、IPO、并购与公司重大动态；
- arXiv、顶会、研究机构、开源模型、代码和数据集。

目标来源类型：

- 公司官网、博客、新闻稿和开源仓库；
- 论文、顶会和研究机构网站；
- 国内外媒体、交易所和监管披露；
- 微信公众号、B站、小红书；
- X、LinkedIn；
- 视频和播客。

MVP 产品载体：

- 手机适配的响应式网页；
- 每日 PDF 早报；
- 飞书Agent私聊推送与只读问答、网页入口及每日PDF；首选Hermes对话层，生产接入不稳定时允许直接飞书应用/机器人调用相同受限只读工具作为降级。

MVP 不包含：通用Timeline、Notes/划线/双链、完整全局搜索、原生App、全自动高频登录态爬取、全部音视频全文转录、复杂多组织权限、自动完整投资报告、自主执行外部行动的agent、高级推荐和外部网页划线扩展。

## 5. Non-negotiable Product Rules

所有 agent 必须遵守：

1. **重要信息召回优先**：不要为了表面精简过早过滤相关候选内容。
2. **事件中心**：同一事件的多个来源应聚合，不得作为多条新闻重复展示。
3. **来源可追溯**：进入早报的信息必须具有至少一个有效原始来源。
4. **不编造**：无来源的信息不得作为事实输出。
5. **状态明确**：事件必须区分已证实、多个来源但未获官方确认、单一来源待核实、已被否认或纠正。
6. **原始数据不可覆盖**：原文、AI 结果、人工编辑和用户 Notes 分开保存。
7. **版本可审计**：摘要、评分、提示词、模型和人工修改应可追踪。
8. **软删除**：事件默认不永久删除；已发布 Brief 不得被无痕覆盖。
9. **数据归用户所有**：所有核心数据必须可导出、备份和迁移。
10. **低成本**：运行成本尽量为零，且不超过 500 元/月，不含已有 AI 工具订阅。
11. **可替换**：不得把核心业务锁定在单一模型、采集器、通知渠道或云平台。
12. **凭证隔离**：第三方平台密码、Cookie 和登录令牌不得进入中央数据库、代码仓库或普通日志。
13. **版权边界**：公开页面仅展示原创摘要、必要短摘录、结构化信息和原始链接，不默认公开完整第三方正文。

## 6. Canonical Domain Model

以下对象名称是跨 agent 沟通时的标准术语：

- `Source`：文章、帖子、论文、视频、播客或上传文件；
- `Event`：由一个或多个 Source 支持的行业事件；
- `Entity`：公司、人物、机构、产品、模型或技术；
- `Topic`：内容主题；
- `Brief`：每日简报及其发布版本；
- `Note`：关联 Event、Source、Entity 或其他 Note 的笔记；
- `Watchlist`：重点对象、赛道和关键词；
- `Assessment`：重要性、可信度等评分及依据；
- `Feedback`：用户对内容和系统判断的反馈；
- `IngestionRun`：一次来源采集任务及结果；
- `ProcessingRun`：一次抽取、分类、聚类、评分或摘要任务及版本。
- `FinancingEvent`：与 `Event` 关联的融资领域聚合；金额、估值、投资方、字段证据、状态、审核和统计口径见 `FINANCING_DASHBOARD_SPEC.md`。

核心关系：

```text
Source ──支持/讨论──> Event <──涉及──> Entity
                         │
                         ├──归类──> Topic
                         ├──进入──> Brief
                         ├──具有──> Assessment
                         └──关联──> Note

Note <──双向链接──> Note / Event / Source / Entity
Watchlist ──关注──> Entity / Topic / keyword
Event <──融资子类型──> FinancingEvent
```

## 7. Required Processing Pipeline

```text
采集
→ 正文与元数据提取
→ URL/内容去重
→ Event 聚类
→ Entity 识别与消歧
→ Topic 分类
→ 事实状态判断
→ Assessment 分项评分
→ 中文摘要
→ 人工审核或自动发布
→ Brief / Timeline
```

Assessment 至少包含：相关性、事件影响、来源可信度、新颖性、watchlist 相关性、多来源验证。综合分必须能解释，不能只保存一个黑盒总分。

来源可信度分级：A为90–100的官方一手、论文原文和官方代码仓库；B为75–89的主流/专业媒体与知名研究机构；C为55–74的垂直小媒体、行业自媒体、已验证KOL或员工账号；D为0–54的匿名、聚合转载、来源链或历史准确性未知来源。A级通过校验可单独确认，两个不同证据链的B级可多来源确认，两个C级不能自动确认，D级只能待核实。等级调整保留历史版本。

MVP各分项使用0–100，相关性、事件影响、来源可信度、新颖性、watchlist相关性、多来源验证的初始权重依次为25%、25%、20%、10%、10%、10%。综合分只用于排序和栏目。满足证据门槛、相关性不低于70、影响力不低于60且无冲突或风险标记时，可自动进入Brief；已确认但影响力不足的内容进入“更多值得关注”。待核实、冲突、单一KOL或实体不明必须审核；IPO、并购、重大融资和重要模型开源优先审核。未达阈值的相关候选仍须保存。

分级发布：

- 高分且来源明确：允许自动进入 Brief；
- 单一来源、冲突、传闻或低置信：进入审核队列；
- 相关但低分：进入“更多值得关注的信息”；
- 明显无关：不发布，但保留必要处理记录。

## 8. Daily Brief Contract

- 时区：Asia/Shanghai（UTC+8）；
- 仅按中国大陆官方工作日历生成，包括调休补班日；非工作日不生成也不发送；
- 非工作日仍持续采集、处理、核验和更新正式融资看板；早报入口显示上一份Brief和下一次发布时间；
- 08:30 截止编排，09:00 发布；
- 覆盖上一份 Brief 截止后至当天 08:30；
- 08:30 后及非工作日事件进入下一个工作日 Brief；
- 迟到的重要信息标记“后补”；
- 部分来源失败时仍按时发布，并明确标注缺失覆盖和重试状态。
- 长假后核心Brief仍保持15–25条和3–5条重点；超出部分进入可展开的“假期更多动态”，展示累计候选数、核心收录数和覆盖区间，不删除相关Event。

Brief 顺序：

1. 今日最重要的 3–5 件事；
2. 公司与融资动态；
3. 技术、模型与开源进展；
4. 论文与顶会动态；
5. 产业落地与商业化；
6. 更多值得关注的信息；
7. 待核实消息。

每条内容至少包括：中文标题和摘要、相关 Entity、综合评分与分项依据、事实状态、Topic、watchlist 标记，以及原文标题、链接、作者/账号、平台和发布时间。

外文内容使用中文摘要，保留外文原标题和链接；公司名、模型名和技术术语优先保留原文。

## 9. Notification Contract

MVP不实现突发提醒、夜间提醒或免打扰汇总。IPO、重大融资、并购、监管变化和重要模型开源仍进入优先审核，但只随下一个中国大陆法定工作日的09:00 Brief递送。

## 10. Post-MVP Notes and General Timeline Contract

Notes在8月7日后实现：TipTap结构化JSON主存储、Markdown导入导出、标签、后续研究标记、全文搜索、双向链接、反向链接，以及系统内正文/摘要划线摘录。默认仅自己可见，可切换团队或公开。

通用Timeline后续优先支持：

- 公司历史；
- 技术演进；
- 赛道的新玩家、融资、产品及商业化动态。

冷启动：重点 watchlist 回溯 12 个月，其他领域回溯 3 个月。

## 11. Ingestion Ownership

采用三层采集：

1. 稳定开放源：自动采集；
2. 搜索与公开页面：发现候选信息；
3. 本地登录态采集：Codex与WorkBuddy半自动补足中国 App、X、LinkedIn与其他封闭平台。

职责边界：

- WorkBuddy：微信、小红书、B站等中国 App 生态；
- Codex：系统开发、开放来源处理、清洗、聚类、评分、摘要和产品生成，同时负责X、LinkedIn及其他需要登录的平台；
- Codex与WorkBuddy：均可在各自本地可信环境使用登录态，并通过同一受限Ingestion API提交规范化候选和必要证据；
- 候选提交：Codex与WorkBuddy可自动提交，无需Admin逐条预批准，但不能绕过去重、核验、评分、审核或发布门槛；
- 中央情报库：唯一可信数据源；
- 任一采集器均可替换，且不得成为唯一数据仓库。

采集交换记录至少包含：标题、正文或必要摘录、链接、作者/账号、平台、发布时间、采集时间、媒体引用、采集器、原始副本引用和处理状态。

## 12. Agent Workstreams

可以并行但需通过已定义接口协作的工作流：

- Product：维护 PRD、范围、验收标准和用户决策；
- Architecture：技术选型、模块边界、部署、成本和安全；
- Data Model：数据库 schema、迁移、版本与审计；
- Ingestion：来源 registry、采集器和标准交换格式；
- Intelligence：去重、聚类、实体消歧、评分和摘要；
- Backend：API、任务调度、权限、搜索和导出；
- Frontend：Brief、Timeline、Notes、审核和管理界面；
- Delivery：工作日PDF与Hermes/飞书Agent递送；
- Quality：测试、评估数据集、五项指标、失败与成本监控；
- Research：来源候选清单、watchlist 冷启动与历史回溯。

各 workstream 必须遵循 `TECH_STACK.md` 的已确认边界；未确认的开放项只能产出调研、接口提案或验收标准，不应擅自写入相互冲突的实现。

## 13. Multi-agent Collaboration Protocol

每个 agent 开始工作前必须：

1. 阅读本文件和 `PRD.md`；
2. 检查仓库中的 `AGENTS.md`、架构文档和已有决策记录；
3. 明确自己的任务边界、输入、输出和不处理事项；
4. 检查其他 agent 是否正在修改相同文件或接口；
5. 不覆盖、不撤销用户或其他 agent 的无关修改。

每个 agent 的交付必须包含：

- 完成了什么；
- 修改了哪些文件或接口；
- 做了哪些假设；
- 如何验证；
- 已知限制与风险；
- 是否产生新的决策或待办。

跨 workstream 决策应记录为 ADR 或显式文档，不得只存在于聊天记录。需要影响 PRD、成本上限、隐私边界、版权边界或 MVP 范围的变更，必须先获得用户确认。

## 14. Definition of Done

单项任务完成至少要求：

- 满足对应 PRD 条款和验收条件；
- 不破坏本文件的非协商规则；
- 有与风险相称的测试或验证；
- 失败行为明确且可观测；
- 不泄露密钥或第三方登录态；
- 新增依赖、成本和迁移风险已记录；
- 文档与实现保持一致。

MVP于2026-08-07按`design-document.md`完成上线验收；产品成功验证要求上线后连续4周达到：

| 指标 | 目标 |
| --- | --- |
| 重大事件 24 小时内召回率 | ≥ 90% |
| Brief 前 10 条有效信息率 | ≥ 70% |
| 来源可追溯率 | 100% |
| 明显重复事件比例 | ≤ 10% |
| 10 分钟内完成主要阅读的 Brief 比例 | ≥ 80% |

## 15. Decisions Already Made

- 事件中心而非文章中心；
- 独立中央情报库，数据由用户拥有；
- 第一轮核心技术选型已冻结，完整记录见 `TECH_STACK.md`；
- PostgreSQL 使用 Supabase Free 托管，保持标准 PostgreSQL 可迁移性；
- 手机网页 + PDF + Hermes/飞书Agent，暂不做原生App；
- 开放源自动化、封闭源半自动化；
- 本地保存登录态，云端处理情报数据；
- 分层模型与规则结合，暂不自建 GPU；
- 高可信事件自动发布，风险事件人工审核；
- 为未来公开、团队协作和研究 agent 预留接口。

## 16. Decisions Still Open

未经用户确认不得擅自锁定：

- UI 视觉风格与设计规范；
- 国内具体模型供应商与模型，等待固定评测集验证；
- Codex 与 WorkBuddy 的交换协议细节；
- 首批具体Source Registry及31家公司之外的后续Watchlist；
- MVP后研究Agent及飞书写操作的能力、权限和操作范围；
- 公开发布时间、商业模式和增长策略。

## 17. Source of Truth Order

出现冲突时按以下优先级处理：

1. 用户最新明确指令；
2. `PRD.md`；
3. `design-document.md` 与 `FINANCING_DASHBOARD_SPEC.md`；
4. `TECH_STACK.md`、`architecture.md`与已确认ADR；
5. 本 `SPEC.md`；
6. `AGENTS.md` 中的工程协作规则；
7. 代码中的默认行为。

不得通过修改低优先级文件绕过高优先级约束。
