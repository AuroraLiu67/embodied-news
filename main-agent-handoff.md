# 主 Agent 当前交接

> 交接日期：2026-08-06
> 当前实施位置：D05.1A 已完成，准备进入 D05.1B Codex 来源溯源与中性候选入口修复
> 下一个允许的里程碑：Implementation Engineer 独立完成 D05.1B 及测试；验收前不得将 Codex 线索导入飞书
> 当前状态：每天搜索、自动清洗去重、用户最终核验；北京时间周一至周日汇总并在次周一发布周报。现有日报代码、飞书表和网站路由尚未迁移，不得宣称周报已实现

## 1. 主 Agent 接管时的阅读顺序

1. `AGENTS.md`
2. `architecture.md`
3. `PRD.md`
4. `SPEC.md`
5. `TECH-STACK.md`
6. `implementation-plan.md`
7. `agent-coordination.md`
8. `search-strategy.md`
9. `progress.md`
10. 本文列出的当前交付物。

本页只是当前状态导航，不改变 `AGENTS.md` 中的文档优先级。

日常任务调度使用 `docs/agent-working-agreement.md`：默认单执行 Agent，必要时增加一个独立 QA；日期/事实冲突、需要登录或付费权限、两轮尝试仍无法确认等情况应优先整理证据并询问用户。

## 2. 已完成的工作

### 2.1 工程与候选基础

- A01–A06、B01–B06、C01–C09、D01–D04 已按 `progress.md` 记录完成并由用户逐步验收。
- D05.1 的 `openai-discover` 、WorkBuddy C01 JSON、C02 `workbuddy-import` 和飞书日期边界已完成离线实现与测试。
- 上次全量记录的工程验证为：`pnpm typecheck`、`pnpm lint`、`pnpm test` 和 `pnpm build` 通过；具体测试数量和日期见 `progress.md`。
- 飞书个人纯文本通知已完成真实受控发送验收，用户已确认收到。

### 2.2 搜索策略

- `search-strategy.md` 是国内外搜索的当前执行基线。
- 当前搜索 MVP 只关注融资与广义资本动态，以国内媒体网站为主渠道。
- “国内”仅指当前搜索渠道；境外公司或交易主体不是自动排除条件。只要业务日、资本事件范围和来源合格，就可进入 clean 和后续核验。
- Codex 项目内 Research Operations 负责国内搜索；海外当前暂停，未来恢复后仍由该角色执行。
- WorkBuddy 暂停；既有 C01/C02 与 `workbuddy-import` 仅作兼容能力。
- OpenAI 不再负责地域发现，继续承担候选后的相关性、抽取、冲突核验和摘要；`openai-discover` 当前暂停。
- 不直接爬取微信公众号；wechat-mp-rss 尚未验证，只能补充实际抓到的可访问条目。
- 日常流程固定为搜索 Agent 完成搜索，再执行确定性清洗、URL 去重和疑似同事件分组，最后由用户人工核验。
- 前两个自然周使用 Agent 与人工搜索结果的联合人工基准集测量召回率；Agent 独立发现且经用户确认有效的事件也计入基准集。
- 报告节奏是北京时间自然周周一至周日，次周一生成并发布周报；现有 DailyDigest、飞书“日报”表和 `/daily` 路由仍待 D06 迁移。
- 每条候选必须有可访问网址。参与方明确披露可标记“已确认”；36氪、投资界等可信专业媒体可正常进入候选。
- 模糊融资金额和未披露金额可收录；保留原文，不得猜测精确数字。
- 报道日期是候选批次归档依据；事件日期可选，未披露时留空。
- 不执行公司或赛道的每日/每周定点轮询。先用融资、获投、并购、收购、IPO 等事件词逐站宽搜，发现线索后再补充搜索。
- 国内媒体高召回试跑保留 raw 原始线索审计层；clean 和当日交付必须严格按用户指定业务日归属。相对时间按 Asia/Shanghai 实际执行时间换算并打开正文核日期；正文明确为其他日期时仅留 raw 排除记录，日期仍不确定时询问用户而不默认进入 clean。
- 人工审核 Markdown 只展示日期合格的 clean 来源和事件；非目标日只报告排除数量，`DATE_UNCERTAIN` 在文件外最小化询问。用户只审核事件合并、主来源、广义资本边界和关键字段冲突。
- 原始线索可包含已完成、进行中和融资意向，但不得改写状态；公司、轮次、金额和投资方不全时仍保留并标记人工处理。
- D05.1A 清洗与事件去重决策树已冻结：搜索阶段只做原始收集；搜索后输出一行一个建议事件并保留全部来源，机器先提取建议值，用户确认事件边界、字段、主来源和候选准入。
- 清洗输出使用原始 JSON、独立清洗 JSON 和人工审核 Markdown；详细 URL、重复、历史补充、日期、审计、更正、召回率与重要性规则以 `search-strategy.md` 为准。

### 2.3 2026-08-03 国内媒体高召回试跑

- 报告：`docs/pilot/codex-media-capital-recall-2026-08-03.md`。
- 指定 10 家媒体全部留下覆盖记录；在当前访问条件下获得 34 个唯一 URL。
- 36氪 11 条；初轮只看融资快报会漏掉普通文章，后续必须实施文章搜索 + 快讯/融资快报双渠道。
- 正文日期未明确 10 条，`DATE_CONFLICT` 2 条，`CONTENT_MISMATCH` 1 条，疑似重复事件组 3 组，`BODY_UNAVAILABLE` 0 条。
- 本报告是原始线索池，不代表事实审核、C01 候选或完整市场覆盖。

## 3. 当前真实候选批次

当前文件：

- `d05-workbuddy-candidates-2026-08-02.json`
- `d05-workbuddy-candidates-2026-08-03.json`
- `d05-workbuddy-candidates-2026-08-03-revision-report.md`

已完成的离线复核：

- 中科原动力条目已按来源实际发布时间 `2026-08-02T07:52:00+08:00` 从 8 月 3 日批次移入 8 月 2 日批次，未改写事件日期或来源发布时间。
- 2026-08-02：6 条候选、6 个唯一 URL、业务日异常 0 条。
- 2026-08-03：20 条候选、20 个唯一 URL、业务日异常 0 条。
- 两批次跨文件重复 URL 为 0；两个文件均通过 C01 strict Schema、控制字符和敏感模式离线检查。
- 海外轻量融资查询模板通过项目 query-file strict Schema：3 个查询、3 个唯一 `queryId`。
- 2026-08-04 主 Agent 复核：31 个测试文件、264 项测试通过；`git diff --check` 通过。

未解决问题：

- 既有两批候选的 26 个 URL 已完成只读 smoke check：8 月 2 日为 `PASS 3`、`NEEDS_HUMAN 3`；8 月 3 日为 `PASS 17`、`REDIRECT_PASS 2`、`BLOCKED 1`。编号 2、3、4 的头条通用壳页和编号 26 的连接超时仍需人工核验。
- 未经用户明确授权，不得将任一批次导入真实飞书。

尚未发生：

- 尚未将这批候选导入飞书。
- 尚未进入 D05.2 真实审核。
- 当前进度记录中没有证据表明已完成真实 OpenAI 海外候选批次或真实导入；不得仅根据 D05.1 离线测试通过就标记真实 OpenAI 试跑完成。

## 4. 下一步执行顺序

必须一步一步执行，不得跳过用户验收。

### 步骤 1：与用户冻结清洗和重复处理规则（已完成）

- 2026-08-05 已冻结搜索后清洗、URL 规范化、事件合并、跨来源提取、主来源、历史重复、人工审计、召回率、重要性和正式事件更正规则。
- 详细规则集中维护在 `search-strategy.md`，其他任务文档不得复制形成第二套标准。

### 步骤 2：修正 Codex 来源溯源和候选入口

- Claude 只读审核确认：当前 `workbuddy-import` 会把输入硬编码为 `WORKBUDDY` 并生成 `candidate-wb-*`，不能用于新的 Codex 搜索批次。
- Implementation Engineer 按 D05.1B 增加 `CODEX_RESEARCH` 等可审计来源与中性候选入口，并补齐回归测试。
- 历史真实 WorkBuddy 候选保持原值，默认不回填；修复完成前禁止把 Codex 线索导入飞书。

### 步骤 3：实现并离线试跑清洗与疑似事件分组

- D05.1B 验收后按 D05.1C 实现确定性清洗；强匹配来源合并为一个待核验事件行并保留全部证据，无法确定的事件保持分行并用 `POSSIBLE_DUPLICATE` 关联。
- 使用 2026-08-03 的 34 条唯一 URL 和 2026-08-04 搜索结果离线试跑。
- 搜索 Agent 可继续生成原始报告，但不得写飞书。

### 步骤 4：用户人工初筛原始线索

- 以 `docs/pilot/codex-media-capital-recall-2026-08-03.md` 为输入。
- 用户确定保留、拒绝、需补搜、广义资本边界项和疑似重复组。
- 人工只处理 `DATE_UNCERTAIN`、`CONTENT_MISMATCH`、融资状态非“已完成”和其他仍无法自动裁决的条目；正文已经明确为非目标日的 `DATE_CONFLICT` 只留 raw 排除记录，不进入 clean 人工审核清单。

### 步骤 5：串联候选处理 CLI 并转换用户选中线索

- 另立任务执行业务日、可访问来源、C01 Schema、事实、URL/事件去重和 QA 检查。
- 不得把原始池的目标日直接写成 C01 `publishedAt`。
- 不得把“正在融资”或“寻求融资”改写为“已完成融资”。

### 步骤 6：受控导入飞书研究候选

- 必须使用 D05.1B 验收后的中性候选入口；不得再用 `workbuddy-import` 导入 Codex 线索。
- 真实导入会写飞书；必须在导入前获得用户对该批次和该次真实写入的明确确认。
- 导入只能写“研究候选”，不能写正式融资事件、公司动态、日报或公开状态。
- 导入后报告总数、新建数、重复数和错误；不打印 Token 或完整外部正文。

### 步骤 7：用户验收后进入 D05.2

- 用户在飞书确认候选条数、来源链接和摘要可见且无异常。
- 只有用户明确说明 D05.1 真实候选导入验收通过，主 Agent 才能分配 D05.2。
- D05.2 仅进行飞书人工审核；不得自动批准候选。

## 5. 建议的主/子 Agent 分工

### 主 Agent

- 冻结当前小步任务、文件边界和验收标准。
- 核对架构、产品、搜索策略与 C01 契约是否一致。
- 集成子 Agent 结果、运行必要的总体验证并向用户报告。
- 不与 Implementation Engineer 并发修改同一业务代码范围。

### Research Operations

- 负责指定国内媒体网站的高召回搜索、覆盖审计、异常标记、疑似重复分组和原始线索报告。
- 必读 `search-strategy.md`、`domestic-website-search-list.md` 和本轮任务单；只有兼容候选导入时才读取 `docs/workbuddy-candidate-format.md`。
- 不写飞书、不批准候选、不修改业务代码。

### Contract & Integration QA

- 独立复核 Schema、业务日、URL、重复、乱码和敏感信息。
- 不通过删除测试、降低断言或修改生产实现来制造通过结果。

### Implementation Engineer

- 当前日期异常只是候选数据问题，默认不需要代码修改。
- 只有主 Agent 发现可重现的契约或导入器缺陷后，才分配独立代码任务。

### 其他子 Agent

- Git、UI、E2E/安全 QA 和 Claude Code 在当前候选日期修正步骤中无必要参与。
- 仅在进入代码、网站、自动化、Git 交付或重大安全审核时由主 Agent 按需启用。

## 6. 当前已知限制

- `openai-discover` 当前只支持 Physical AI 融资严格实现，现已暂停，不是当前搜索主链路。
- C01 尚无独立的证据状态、事件日期、金额类型和公司画像字段；MVP 暂写入 `preliminarySummary`。
- wechat-mp-rss 尚无真实条目和稳定性证据，当前不能纳入完成标准。
- Codex 定时任务可用于试验；生产 SLA 仍要求公司可交接运行环境、无个人登录态、确定性入口、审计、补跑和幂等。
- 生产自动化不得依赖个人电脑、个人平台账号或交互式 Codex 会话。
- 未来扩展候选 Schema 必须同时更新领域契约、飞书字段、Fixture、导入器和自动化测试，不得只修改搜索报告格式。
- Claude 阶段性审核报告位于 `docs/review/architecture-review-2026-08-05.md`；其中来源溯源缺陷是当前真实导入阻塞项，调度守卫、公开状态门禁和 CLI 串联已进入后续实施计划。
- 原始线索清洗与事件级去重决策树已经冻结；D05.1C 仍必须等待 D05.1B 来源溯源任务独立验收后再开始，避免并行修改共享候选契约。

## 7. 禁止的假设

- 不得把“Schema 通过”解读为“业务日正确”。
- 不得把“D05.1 离线测试通过”解读为“真实海外搜索已运行”。
- 不得把候选导入解读为正式事件批准或日报发布。
- 不得在用户未确认前进入 D05.2。
- 不得让外部 Agent 读取或修改内部战投备注。
