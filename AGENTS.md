# AGENTS.md

本文件适用于整个仓库。所有 AI Agent、自动化开发工具和人工开发者都必须遵守。

## 1. 开始编码前

在编写、修改或删除任何代码之前，必须完整阅读：

1. `architecture.md`
2. `PRD.md`

涉及工程契约、技术选型或具体任务时，还必须阅读：

3. `SPEC.md`
4. `TECH-STACK.md`
5. `implementation-plan.md`
6. `agent-coordination.md`

未阅读 `architecture.md` 和 `PRD.md`，不得开始代码修改。

涉及任务分配、并行开发、审核或交接时，必须遵守 `agent-coordination.md`。

所有主/子 Agent 的日常任务必须同时遵守 `docs/agent-working-agreement.md`，使用精简单 Agent 或“一执行 + 一 QA”模式，并在规定触发条件下先询问用户。

接管主 Agent 职责时，必须先阅读 `main-agent-handoff.md`，了解当前实施位置、下一个允许任务、未解决问题和已验证结果。交接页不改变本文的文档优先级。

涉及网站搜索、RSS 补充、人工链接或研究候选时，还必须完整阅读 `search-strategy.md`。国内网站任务同时阅读 `domestic-website-search-list.md`。

## 2. 文档优先级

发生冲突时，按以下顺序处理：

1. 用户当前明确指令
2. `architecture.md` 中的架构不变量
3. `PRD.md` 中的产品范围与要求
4. `SPEC.md` 中的工程契约
5. `TECH-STACK.md` 中的技术选型
6. `implementation-plan.md` 中的任务顺序
7. 本文件中的通用开发规范

不得自行绕过或修改 `architecture.md` 中的架构不变量。需要改变架构、产品范围、正式数据源、公开边界或 Provider 职责时，必须先停止实现并请求用户确认。

## 3. 固定架构约束

- 飞书多维表格是唯一正式数据源。
- Codex 项目内 Research Operations 负责国内网站搜索；海外搜索在当前 MVP 暂停，未来恢复后仍由该角色执行。
- OpenAI负责候选后的相关性判断、抽取、核验和摘要，不再承担当前国内外发现主责。
- 不直接爬取微信公众号；wechat-mp-rss 仅是尚未验证的补充输入，不是当前搜索完成条件。
- 外部 Agent只能创建候选或建议，不能直接发布或修改内部战投数据。
- 项目飞书 CLI 是自动化与人工维护的统一数据操作入口。
- Next.js 根据公开投影生成静态 HTML。
- GitHub Actions负责定时任务、补跑、构建和发布。
- GitHub Pages只负责托管公开静态网站。
- 网站和公开构建不得包含内部战投备注、待审核候选、密钥或完整模型响应。
- 自动任务不得依赖个人电脑或交互式 Codex 会话持续在线。
- 搜索策略以 `search-strategy.md` 为执行基线；当前搜索 MVP 只覆盖融资与广义资本动态。原始线索可宽召回，正式数据和发布仍必须经过 Schema、去重与人工审核。

## 4. 实现规则

- 一次只实现一个清晰、可验证的任务。
- 修改前声明本次允许修改的文件范围。
- 不覆盖或删除其他人的未提交修改。
- 外部输入必须经过运行时 Schema、安全和长度校验。
- 领域层不得直接依赖飞书、OpenAI、WorkBuddy或 Next.js 的具体实现。
- 新行为必须添加对应的自动化测试。
- 不得跳过失败测试、降低断言或删除测试来完成任务。
- 测试不得访问真实 OpenAI、WorkBuddy或生产飞书。
- 密钥、访问令牌、完整原文和内部数据不得写入 Git、日志或静态产物。

## 5. Progress 更新规则

完成任何重大功能后，必须更新 `progress.md`。

重大功能包括但不限于：

- 完成一个 `implementation-plan.md` 阶段或关键任务组。
- 建立或修改核心领域契约。
- 完成飞书数据层或飞书 CLI 的主要能力。
- 完成 WorkBuddy 或 OpenAI Provider 接入。
- 完成候选处理、审核、周报或发布流水线。
- 完成一个主要网站页面或数据看板。
- 完成 GitHub Actions 自动化或 GitHub Pages 发布。
- 完成安全、备份、回填、真实试运行或生产验收。
- 修复影响数据正确性、安全或生产发布的重大缺陷。

每条进度记录至少包含：

- 日期
- 完成内容
- 验证结果
- 重要限制或后续工作

如果重大功能完成但尚未更新 `progress.md`，任务不得标记为完成。

## 6. 完成与交接

任务交接必须说明：

- 完成的任务和结果
- 修改的文件
- 是否改变公共契约
- 新增或更新的测试
- 实际验证结果
- 已知限制和后续工作
- 是否已按要求更新 `progress.md`

“已经生成代码”不等于完成。只有实现、测试、文档和必要的进度记录全部完成后，任务才算完成。
